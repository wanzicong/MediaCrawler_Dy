# 抖音采集工作台 · 前端全量分析报告（AI 对话成果提炼）

> **本报告是什么**：`docs/前端项目分析.md`（75,392 行 / 2.4MB）是一次 Claude Code 会话的完整导出。本报告把该会话里**用户提出的每一个问题**与 **AI 给出的每一条结论、建议、缺陷判定、产品方案**全部提炼归并，一条不漏。
>
> **数据来源**：文档 9 段并行提取（见附录 8.3），合计 **1,740 条**条目。
>
> **三条使用约定**：
> 1. 每条都标注了**出处行号**与**源文件:行号**，可直接回查。
> 2. 每条都带 **现状核对** 标记——`✅ 仍存在`（我在本地源码逐行验证过）/ `⚠️ 需修正`（AI 的判定有偏差）/ `⬜ 未核对`。
> 3. 凡 AI 的**建议**与 AI 的**发现**已彻底分开。原文把两者混在一张严重度表里，本报告拆成两卷：**第二～四卷 = 发现（问题）**，**第五～六卷 = 建议（方案）**，前者可直接排期修，后者需要先决策。

---

## 目录

| 卷 | 内容 | 章节 |
|---|---|---|
| 卷首 | 如何使用 | — |
| **第一卷** | **项目事实基线**（可放心引用的硬数据） | 一 |
| **第二卷** | **缺陷清单**（真 bug / 安全 / 横切债务 / 页面级） | 二 |
| **第三卷** | **性能问题全清单**（P0–P3 + 根因 + 量化） | 三 |
| **第四卷** | **数据列表架构专题**（12 维度 × 20 列表 + 25 条通病） | 四 |
| **第五卷** | **架构方案**（模块拆分 / Skin×Layout / 列表基建 / 实施计划） | 五 |
| **第六卷** | **产品与体验优化**（A1–A16 / O1–O20 / 产品架构师 8 视角 / PRD） | 六 |
| **第七卷** | **AI 明确判定「不是问题」的清单**（避免误改） | 七 |
| **第八卷** | **附录**（对话轮次 / 损坏区 / 口径对照 / 提取索引） | 八 |

---

# 第一卷 · 项目事实基线

> 本卷全部是**可核对的事实**，不是判断。AI 在这一层的准确率最高，可直接作为后续所有讨论的基础。

## 1.1 仓库与分支

- **[事实]** 主项目 `https://atomgit.com/qq_41251172/MediaCrawler_Dy_V2`（会话中 AI 克隆到 `/workspace/MediaCrawler_Dy_V2`）。本地 `origin` 为 `github.com/wanzicong/MediaCrawler_Dy.git`——**镜像不同、代码相同**。｜出处：A-§0
- **[事实]** 该仓库**不存在 `main` 分支**。默认分支为 `refactor/uv-workspace-modules`，另有 `dev`。会话中用户 16 轮里**有 11 轮都写了「分支：main」**，AI 每一轮都做了纠正。｜出处：A-§0、G-§二
- **[事实]** 远程分支清单：`dev`、`refactor/uv-workspace-modules`，外加 **15 个 dependabot 分支**（bun: lucide-react / radix-ui/react-scroll-area / sonner / tailwindcss/vite / tanstack/react-query-devtools；docker: frontend / playwright；github_actions: checkout-7 / download-artifact-8 / setup-python-7 / dorny/paths-filter-4 / tiangolo/latest-changes-0.7.2；uv: alembic / pwdlib / pydantic / pydantic-settings / ruff）。｜出处：A-§0（源 1153-1191 行）
- **[事实]** `dev` 与默认分支 `refactor/uv-workspace-modules` 的前端差异**仅 2 个文件**：`frontend/src/routes/login.tsx`（4 行）、`frontend/tsconfig.json`（1 行），共 3 插入 2 删除。**因此本文所有前端结论对两个分支通用**。｜出处：A-§0（源 1235-1238 行）
- **[事实]** 两分支 `frontend/src/routes` 下文件数均为 **26**。｜出处：A-§0
- **[事实]** 仓库根目录共 27 个条目，含 `frontend/`、`modules/`、`docs/`、`scripts/`、`tests/`、`compose.yml`、`compose.infra.yml`、`uv.lock`、`bun.lock`。｜出处：A-§0

## 1.2 后端架构（决定前端契约的部分）

- **[事实]** 项目全称「抖音采集工作台（Douyin Crawler Full Stack）」，基于 FastAPI 官方 `full-stack-fastapi-template` **0.10.0** 构建。｜出处：A-§1.1
- **[事实]** 后端把 MediaCrawler 的抖音请求逻辑重构为**纯 HTTP API 服务**，面向搜索、作品详情、创作者主页、点赞/收藏等采集场景。｜出处：A-§1.1
- **[事实]** 后端采用 **uv workspace 多项目架构**，`modules/` 下**六个**独立 Python 项目共享 `crawler.*` 命名空间（PEP 420）。｜出处：A-§1.1（源 408-409 行）
- **[事实]** 依赖方向被**打包元数据 + `tests/architecture/` 契约测试双重强制**，固定为：
  `api → business → douyin-client → browser → bootstrap`，`mcp → bootstrap`。**禁止反向依赖**。
  `crawler/api/main.py` 是运行时组合根，`crawler/bootstrap/settings.py` 提供全局配置。｜出处：A-§1.1（源 415-477 行）
- **[事实]** API 路由**禁止**直接使用 SQLAlchemy/SQLModel 查询（登记的运维脚本除外）、MinIO、Playwright、文件路径判断或事务操作；MinIO SDK 只允许出现在 `business/resources/storage/` 驱动中。**唯一登记的越界例外**：`routes/system_docs.py` 自省 `crawler.mcp.server` 的工具元数据。｜出处：A-§1.1
- **[事实]** 后端核心能力：五类采集任务 `search`/`detail`/`creator`/`liked`/`collected`；CDP-only 浏览器控制；扫码/Cookie 登录；断点恢复；媒体流水线（视频下载 + 远程 Whisper 字幕 + 本地/MinIO 双存储）；赛道归类（`track_id`）；MCP 接入（Streamable HTTP/stdio）；公平限流与账号槽位；隐私脱敏（HMAC + 昵称打码）。｜出处：A-§1.1（源 374-386 行）
- **[事实]** 合规声明：抖音适配代码沿用 MediaCrawler 的 **NON-COMMERCIAL LEARNING LICENSE 1.1**，仅限非商业学习与研究；官方模板部分遵循根目录 MIT LICENSE。｜出处：A-§1.1（源 343-345 行）
- **[事实]** 硬约束：Cookie、Token 和原始账号 ID **不得**写入数据库、日志或 API 响应；创作者/评论用户信息必须经脱敏映射后落库。｜出处：A-§1.1
- **[事实]** 前端消费的是**已脱敏数据**——创作者/评论用户信息经 HMAC 脱敏 + 昵称打码后入库。｜出处：A-§1.1（源 1085 行）
- **[事实]** 前后端契约是**类型安全的**：前端 SDK 由后端 OpenAPI 自动生成，配合后端 `tests/architecture/` 的 **OpenAPI 哈希契约测试**，前后端不会静默脱节。｜出处：A-§1.1（源 1083 行）

## 1.3 前端技术栈（版本号精确）

| 类别 | 内容 |
|---|---|
| 框架 | React **19** + TypeScript **5.9** |
| 构建 | Vite **7**，插件 `@vitejs/plugin-react-swc` 4.2.2 |
| 样式 | Tailwind CSS **v4**（`@tailwindcss/vite` 4.1.18 + `tailwindcss` 4.1.17）+ tw-animate-css 1.4.0 |
| UI 库 | shadcn/ui（`components.json`: style `new-york`、rsc `false`、baseColor `neutral`、cssVariables `true`、iconLibrary `lucide`、**无 tailwind.config**——v4 无配置文件） |
| 组件基座 | Radix UI **14 个** `@radix-ui/react-*` 包 |
| 路由 | TanStack Router **1.142.11**（`@tanstack/router-plugin` 1.140.0，`@tanstack/router-devtools` 1.142.11） |
| 数据 | TanStack Query **5.90.12** |
| 表格 | TanStack Table **8.21.3** |
| 表单 | react-hook-form **7.68.0** + zod **4.2.1** + `@hookform/resolvers` 5.2.2 |
| 请求 | axios 1.13.2 + `@hey-api/openapi-ts` **0.73.0**（自动生成 OpenAPI 客户端） |
| 图标 | lucide-react **0.562.0**；另有 **react-icons 5.5.0** |
| 其他 | next-themes 0.4.6、sonner 2.0.7、class-variance-authority 0.7.1、clsx 2.1.1、tailwind-merge 3.4.0、react-error-boundary 6.0.0、form-data 4.0.5 |
| devDeps | `@biomejs/biome` 2.3.10、`@playwright/test` 1.57.0、`@types/node` 25.0.2、dotenv 17.2.3、typescript 5.9.3、vite 7.3.0 |
| 包管理 / 规范 / E2E | **Bun** / **Biome** / **Playwright** |

出处：A-§1.2（源 585-635、576-584、687 行）

- **[事实]** `package.json` scripts：`dev: vite`、`build: tsc -p tsconfig.build.json && vite build`、`lint: biome check --write --unsafe ...`、`preview: vite preview`、`generate-client: openapi-ts`、`test: bunx playwright test`、`test:ui: bunx playwright test --ui`。｜出处：A-§1.2
- **[事实]** 前端 OpenAPI 客户端生成：自动 `bash ./scripts/generate-client.sh`，或手动从 `http://localhost/api/v1/openapi.json` 下载后 `bun run generate-client`。**后端 OpenAPI schema 每次变更都需重新生成并提交**。｜出处：A-§1.2
- **[事实]** 远程 API 通过 `VITE_API_URL` 指定（`frontend/.env`）。｜出处：A-§1.2
- **[事实]** `vite.config.ts`：别名 `@` → `./src`；dev server 代理 `/api` → `VITE_API_URL || "http://localhost:8000"`；插件 `tanstackRouter({ target: "react", autoCodeSplitting: true })` + `react()` + `tailwindcss()`。｜出处：A-§1.2（源 1290-1324 行）
- **[事实]** `tsconfig.json`：target ES2020、module ESNext、`moduleResolution: bundler`、`strict: true`、`noUnusedLocals`、`noUnusedParameters`、`noFallthroughCasesInSwitch`、`paths: {"@/*": ["./src/*"]}`。｜出处：A-§1.2（源 1342-1371 行）

## 1.4 目录结构与规模

- **[事实]** `frontend/src/` 共 **10** 个条目：`client/`、`components/`、`hooks/`、`index.css`、`lib/`、`main.tsx`、`routes/`、`routeTree.gen.ts`、`utils.ts`、`vite-env.d.ts`。
- **[事实]** `src/routes/` 共 **7** 个条目：`__root.tsx`、`_layout.tsx`、`_layout/`、`login.tsx`、`recover-password.tsx`、`reset-password.tsx`、`signup.tsx`。
- **[事实]** `src/routes/_layout/` 共 **20** 个业务页面：`admin.tsx`、`developer-tools.tsx`、`douyin.tsx`、`douyin_.$taskId.tsx`、`douyin_.$taskId.feed.tsx`、`douyin-accounts.tsx`、`douyin-browsers.tsx`、`douyin-comments.tsx`、`douyin-creators.tsx`、`douyin-interactions.tsx`、`douyin-keywords.tsx`、`douyin-library.tsx`、`douyin-library.feed.tsx`、`douyin-request-logs.tsx`、`douyin-tags.tsx`、`douyin-tracks.tsx`、`douyin-tracks_.$trackId.tsx`、`index.tsx`、`items.tsx`、`settings.tsx`。
- **[事实]** `src/components/` 共 **10** 个目录/文件：`Admin/`、`Common/`、`Douyin/`、`Items/`、`Navigation/`、`Pending/`、`Sidebar/`、`theme-provider.tsx`、`ui/`、`UserSettings/`。
- **[事实]** `src/components/Douyin/` 共 **25** 个：`AwemeActions.tsx`、`BatchCommentDialog.tsx`、`CreateTaskDialog.tsx`、`CreatorAvatar.tsx`、`InteractionComposerDialog.tsx`、`InteractionContentSummary.tsx`、`InteractionLiveMonitor.tsx`、`InteractionStatusBadge.tsx`、`MediaMigrationDialog.tsx`、`MediaPipelinePanel.tsx`、`MediaTaskManagement.tsx`、`presentation.ts`、`ProcessMediaDialog.tsx`、`ResumeTaskDialog.tsx`、`SourceSelect.tsx`、`SubtitlePanel.tsx`、`TaskExecutionProgress.tsx`、`TaskIdentity.tsx`、`TaskInteractionsPanel.tsx`、`taskParameters.ts`、`TaskResults.tsx`、`TaskStatusBadge.tsx`、`TrackSelect.tsx`、`UnifiedWorksPanel.tsx`、`VideoPreviewDialog.tsx`。
- **[事实]** `src/components/Common/` 共 **10** 个：`Appearance.tsx`、`AuthLayout.tsx`、`DataTable.tsx`、`ErrorComponent.tsx`、`Footer.tsx`、`Logo.tsx`、`NotFound.tsx`、`PageShell.tsx`、`QueryErrorState.tsx`、`ViewModeToggle.tsx`。
- **[事实]** `src/components/Sidebar/` 共 **3** 个：`AppSidebar.tsx`、`Main.tsx`、`User.tsx`。
- **[事实]** `src/components/ui/`（shadcn 组件）共 **25** 个。
- **[事实]** `src/hooks/` **仅 4 个**：`useAuth.ts`、`useCopyToClipboard.ts`、`useCustomToast.ts`、`useMobile.ts`。
- **[事实]** `src/lib/` **仅 1 个文件**：`utils.ts`——**共享层极薄**。
- **[事实]** `src/client/` 共 5 个：`core/`、`index.ts`、`schemas.gen.ts`、`sdk.gen.ts`、`types.gen.ts`。

出处：A-§1.3

### 文件行数排行（`_layout` 页面，合计 16,018 行）

| 文件 | 行数 |
|---|---:|
| `douyin-tracks_.$trackId.tsx` | **2,205**（全仓最大） |
| `douyin-tracks.tsx` | 1,936 |
| `douyin-library.tsx` | 1,627 |
| `douyin-keywords.tsx` | 1,245 |
| `douyin-comments.tsx` | 1,206 |
| `douyin-creators.tsx` | 1,101 |
| `douyin-accounts.tsx` | 934 |
| `douyin.tsx` | 874 |
| `douyin-interactions.tsx` | 753 |
| `douyin_.$taskId.tsx` | 649 |
| `douyin-request-logs.tsx` | 606 |
| `developer-tools.tsx` | 573 |
| `douyin_.$taskId.feed.tsx` | 309 |
| `index.tsx` | 278 |
| `douyin-tags.tsx` | 249 |
| `douyin-browsers.tsx` | 233 |
| `douyin-library.feed.tsx` | 221 |
| `signup.tsx` | 190 |
| `reset-password.tsx` | 167 |
| `_layout.tsx` | 148 |
| `login.tsx` | 147 |
| `recover-password.tsx` | 133 |
| `admin.tsx` | 73 |
| `items.tsx` | 70 |

### 文件行数排行（`components/Douyin/`，合计 7,715 行）

| 文件 | 行数 |
|---|---:|
| `UnifiedWorksPanel.tsx` | **1,479**（全项目最大单文件） |
| `CreateTaskDialog.tsx` | 828 |
| `MediaTaskManagement.tsx` | 571 |
| `MediaPipelinePanel.tsx` | 487 |
| `AwemeActions.tsx` | 487 |
| `TaskExecutionProgress.tsx` | 417 |
| `InteractionComposerDialog.tsx` | 413 |
| `BatchCommentDialog.tsx` | 408 |
| `InteractionLiveMonitor.tsx` | 331 |
| `VideoPreviewDialog.tsx` | 321 |
| `TaskResults.tsx` | 313 |
| `ResumeTaskDialog.tsx` | 268 |
| `TaskInteractionsPanel.tsx` | 236 |
| `Common/PageShell.tsx` | 192 |
| `Sidebar/Main.tsx` | 87 |
| `Navigation/HorizontalNavigation.tsx` | 79 |

出处：A-§1.3、B-§一、D-§一

## 1.5 关键架构决策（前端运行时组合根）

- **[事实]** 单 Vite 应用、单入口（`index.html` → `src/main.tsx`）；TanStack **文件路由**，`routeTree.gen.ts` 由插件自动生成；`autoCodeSplitting: true` 已实现**按路由自动代码分割**。｜出处：A-§1.4（源 1734-1735 行）
- **[事实]** `main.tsx` 是前端运行时组合根：
  `OpenAPI.BASE = import.meta.env.VITE_API_URL`；`OpenAPI.TOKEN` 从 `localStorage.getItem("access_token")` 异步取；创建 `QueryClient` 并挂 `queryCache/mutationCache` 的 `onError: handleApiError`；`createRouter({ routeTree })`。
  渲染树：`StrictMode > ThemeProvider(defaultTheme="light", storageKey="vite-ui-theme") > QueryClientProvider > RouterProvider + Toaster(richColors, closeButton)`。｜出处：A-§1.4（源 1389-1452 行）
- **[事实]** `handleApiError`：仅在 `error instanceof ApiError` 时处理；会话失效条件 = `status ∈ [401,403]` 或 `status === 404 && detail === "User not found"`；命中后 `localStorage.removeItem("access_token")` 并 **`window.location.href = "/login"` 硬跳转**。｜出处：A-§1.4（源 1409-1426 行）
- **[事实]** `_layout.tsx` 的 `beforeLoad` 用 `isLoggedIn()` 做路由守卫，未登录 `throw redirect({ to: "/login" })`。｜出处：A-§1.4
- **[事实]** `__root.tsx`：`notFoundComponent: NotFound`、`errorComponent: ErrorComponent`；devtools **仅**在 `import.meta.env.DEV && import.meta.env.VITE_ENABLE_DEVTOOLS === "true"` 时挂载。｜出处：A-§1.4
- **[事实]** 布局双模式：`_layout.tsx` 用 `useState<NavigationLayout>` + `localStorage`（key `NAVIGATION_LAYOUT_STORAGE_KEY`）在 `horizontal`/`sidebar` 间切换，`usesSidebar` 条件渲染 `AppSidebar` 或 `HorizontalNavigation`。｜出处：A-§1.4（源 1552-1659 行）

### 导航体系（`components/Navigation/navigation.ts`，源 4000-4133 行）

- **[事实]** 5 个 `NavigationModule`：
  - `overview` — 工作台 `/`
  - `collection` — 赛道管理 `/douyin-tracks`、抖音任务 `/douyin`、关键词管理 `/douyin-keywords`
  - `content` — 视频资源库、评论管理、达人列表、标签管理
  - `operations` — 互动任务、账号池、浏览器监控、请求日志
  - `system` — 开发者中心、用户管理 `/admin`（带 `requiresSuperuser: true`）
- **[事实]** 导出 `getNavigationModules(isSuperuser)`、`isNavigationItemActive(pathname, itemPath)`、`findActiveNavigation(pathname, isSuperuser)`。**`findActiveNavigation` 未被 `_layout.tsx` 使用**——这是 H22 缺陷的根源。｜出处：A-§1.4

### 外观系统现状（源 2722-2729 行）

- **[事实]** 已有 **4 个外观维度**：
  - 明暗 `theme` = light/dark/system（`.dark` class，**3 值**）
  - 配色 `preset` = ocean/graphite/violet（`data-preset` + CSS 覆盖 `--primary` 等，**3 值**）
  - 密度 `density` = comfortable/compact（`data-density` + CSS 覆盖 card/table 间距，**2 值**）
  - 布局 `layout` = horizontal/sidebar（`_layout.tsx` 里 `usesSidebar` if/else，**2 值**）
- **[事实]** 理论 **3×3×2×2 = 36** 种组合（源 2729 行；源 2614 行表述为 24 种，**口径差异为是否计入 system**）。
- **[事实]** `theme-provider.tsx` 自定义实现：类型 `Theme`/`ThemePreset`/`Density`；持久化 key 为 `storageKey`、`${storageKey}-preset`、`${storageKey}-density`；`useEffect` 把 `preset`/`density` 写到 `document.documentElement.dataset`；`resolvedTheme` 通过 `matchMedia("(prefers-color-scheme: dark)")` 计算并监听 change。
- **[事实]** `index.css` **338 行**，分层：`@import "tailwindcss"` + `tw-animate-css` → `@custom-variant dark (&:is(.dark *))` → `@theme inline`（radius + 全部颜色 token 映射）→ `:root`（40+ 个 oklch 变量，`--radius: 0.875rem`）→ 3 个 `[data-preset]` 覆盖块 → `.dark` 全量覆盖 → 2 个 `.dark[data-preset]` 覆盖块 → 4 条 `[data-density="compact"]` 覆盖规则 → `@layer base`（reset + 字体栈 Inter/"Noto Sans SC"/"Microsoft YaHei UI" + 三层 radial-gradient 光晕 + `::selection`）→ `@layer components`（`.page-stack`/`.page-hero`/`.page-hero-glow*`/`.eyebrow`/`.filter-panel`/`.status-pulse`）→ `@keyframes status-pulse` → 两个媒体查询降级。
- **[事实]** `AuthLayout.tsx` 桌面端左右分栏 `lg:grid-cols-[1.08fr_0.92fr]`，左侧品牌区含渐变 + 装饰圆 + 卖点文案（「任务状态实时更新」「账号与隐私安全隔离」），右侧卡片区右上角放 `Appearance` 组件、底部放 `Footer`。｜出处：A-§1.4（源 4808-4853 行）

## 1.6 环境与工具结论（影响所有后续验证）

- **[事实]** 审查环境：**`bun` 不可用**，`node` **v22.11.0**；用 `npm install` 替代 bun 安装成功（EXIT:0，约 180s）。｜出处：A-§4.0（源 2981-2983 行）
- **[事实]** **Vite 7 要求 Node ≥ 20.19 或 ≥ 22.12**，当前 22.11.0，启动时打印版本不符警告，但仍能 ready（Vite v7.3.6，1658ms，`http://127.0.0.1:5173` 返回 200）。｜出处：A-§4.0（源 4381-4387 行）
- **[⚠️ 重要]** **Playwright MCP 截图失败**：Chromium 沙箱限制（`Running as root without --no-sandbox is not supported`，`Target page, context or browser has been closed`）。**因此原会话的全部结论都基于源码审查，没有任何视觉截图佐证**。｜出处：A-§4.0（源 5921-5990 行）
- **[事实]** 性能专项时 vite dev server 已不可用（`curl http://127.0.0.1:5173/` 返回 `000`，`ps aux | grep vite` 计数 0）→ **无法做 mcp-chrome 运行时性能 trace**，性能结论全部来自静态审查。｜出处：C-§结论
- **[事实]** AI 判断 playwright 已因 chromium sandbox（root 环境）失败，mcp-chrome 很可能同样失败，因此改为「2 个智能体静态深挖 + 尝试运行时 trace」双轨。｜出处：C-§结论
- **[事实]** **原会话自始至终没有修改过任何一行项目源码**。唯一一次改动是给 `src/main.tsx` 加 `defaultOptions`，用户在源 74403 行说「你不用直接修改给出来计划就行了」，AI 随即**完整回退**（曾加的是 `defaultOptions: { queries: { staleTime: 30_000, gcTime: 5*60_000, refetchIntervalInBackground: false, retry: 1 }, mutations: { retry: 0 } }`），工作区恢复原状。｜出处：I-§补充（源 74403-74442 行）

---

# 第二卷 · 缺陷清单

> 原文把 212 条原始缺陷去重成 **高 22 / 中 48 / 低 60+**，另有 **11 项横切系统性问题**。
> **诚实说明**：原文档在汇总报告里**只逐条列出了 22 条高 + 30 条中**（M1–M30）；低 60+ 条在原文中本身就是**按类别归并**的（原文如此，非提取丢失）。本卷按此如实呈现，并在 §2.5 补齐 22 条高严重度的完整清单与 30 条中严重度清单。

## 2.1 A 类 · 真正的一行可修真 bug（3 条）

> AI 反复强调的「快赢项」——这三条各 1 行改动，且影响面最大。

- **[BUG·A1]** `src/components/Common/PageShell.tsx:47, 53-95` — `PageHero` 声明了 `description?: string` prop，但组件体**从未渲染它**（只渲染 eyebrow/title/actions/children）。
  **影响**：**全站所有页面的 hero 描述文案全部丢失**。传了却看不见的调用点：`index.tsx:79`、`settings.tsx:44`、`admin.tsx:67`、`douyin_.$taskId.tsx:155`、`douyin-browsers.tsx:44`。
  ｜出处：A-§4.1 F1、B-§三 H1、G-§二 ｜**现状核对：✅ 仍存在**（本地 `PageShell.tsx:47` 声明 `description?: string`，函数体 53-96 行仅渲染 eyebrow/title/actions/children；注意 `SectionHeading.tsx:173-175` **确实**渲染 description，**不要看错**）
  ｜**修复**：1 行——在 `<h1>` 下方补 `<p>{description}</p>`。

- **[BUG·A2]** `src/routes/_layout/settings.tsx:30-32` — `const finalTabs = currentUser?.is_superuser ? tabsConfig.slice(0, 3) : tabsConfig`，而 `tabsConfig` **恰好 3 项**，`slice(0,3)` 与原数组等长，**两个分支结果完全相同**。
  **影响**：超管分支判定完全失效；普通用户也能看到「危险操作」（删除账号）tab。
  ｜出处：A-§4.2 I1、B-§三 H2、G-§二 H2 ｜**现状核对：✅ 仍存在**（本地 `settings.tsx:31` 就是 `tabsConfig.slice(0, 3)`）
  ｜**修复**：3 行以内的 1 行——改为 `slice(0, 2)`。

- **[BUG·A3]** `src/routes/_layout/douyin-library.tsx:362` — `invalidateQueries({ queryKey: ["douyin-tasks"] })`，但该页列表实际 queryKey 是 **`["douyin-library-tasks", trackId, sourceValue]`**（同文件 202 行）。
  **影响**：批量评论补采后，**任务下拉框不刷新**。
  ｜出处：B-§三 H9、G-§二 H9 ｜**现状核对：✅ 仍存在**（202 行与 362 行已逐字确认）
  ｜**修复**：1 行——改 key。

## 2.2 B 类 · 安全与鉴权（8 条，AI 定性为「鉴权安全基石薄弱」）

> **核对提示**：本地 git 历史里有 `f6bd043 feat: prefill default admin credentials in login form` ——**H3 是刻意为之的功能，不是疏忽**。可以保留结论，但排期时应先确认产品意图（是否只在 dev/演示环境需要）。

### `src/hooks/useAuth.ts` 的 4 条（H4–H7）

- **[安全·H4]** `useAuth.ts:45` — `access_token` **明文存 localStorage**。**影响**：XSS 可直接窃取长期 token。｜**现状核对：✅ 仍存在**（本地 `useAuth.ts:45` `localStorage.setItem("access_token", response.access_token)`）
- **[BUG·H5]** `useAuth.ts:50-52` — 登录成功后**不 `invalidate` `["currentUser"]`**（`onSuccess` 只做 `navigate({ to: "/" })`）。**影响**：登录后 user 可能长期为 null，导航/菜单不渲染。｜**现状核对：✅ 仍存在**
- **[安全·H6]** `useAuth.ts:56-59` — `logout` **不清 `queryClient` 缓存**（只 `removeItem` + `navigate`）。**影响**：退出后内存残留上一用户数据。｜**现状核对：✅ 仍存在**
- **[安全·H7]** `useAuth.ts:14-16` — `isLoggedIn()` **仅检查 token 是否存在、不验证有效性**（`return localStorage.getItem("access_token") !== null`）。**影响**：过期/伪造 token 可进入受保护页面。｜**现状核对：✅ 仍存在**

### 其余 4 条

- **[安全·H3]** `src/routes/login.tsx:57-58` — `defaultValues` 硬编码 `username: "admin@example.com"`、`password: "changethis"`。**影响**：生产环境表单预填默认凭据。｜**现状核对：⚠️ 仍存在，但性质需修正**——本地 git commit `f6bd043` 明确写着「prefill default admin credentials in login form」，**这是有意添加的**。建议：保留结论，但把「疏漏」改写为「需评估是否按环境开关」。
- **[安全·H8]** `src/routes/_layout/developer-tools.tsx:49` — **无任何权限控制**，所有登录用户可见全量 API/MCP 文档。**影响**：信息泄露、攻击面暴露。与 `admin.tsx` 的 `beforeLoad` 路由级鉴权形成对比。｜**现状核对：⬜ 未核对**
- **[安全·M17]** `src/routes/_layout/douyin-browsers.tsx:210` — iframe 只有 `allow="clipboard-read; clipboard-write; fullscreen"`，**无 `sandbox` 属性**。｜**现状核对：⬜ 未核对**
- **[安全·D-1]** 多处「临时登录凭据（cookie）」以 `Textarea` 粘贴，**明文保存于组件 state**：`UnifiedWorksPanel.tsx:200-209`、`AwemeActions.tsx:389-403`、`BatchCommentDialog.tsx` 等。提示文案为「登录凭据只用于新任务，任务完成后自动清除」。`AwemeActions.tsx:395-398` 已加 `autoComplete="off"`，但**无其它脱敏/清空保护**。｜**现状核对：⬜ 未核对**

### 横切 C8 · 手动 fetch + token 绕过 client（6+ 处）

- **[安全]** 二维码、视频预览、导出、媒体下载等直接用 `localStorage.getItem("access_token")` + 原生 `fetch`，绕过统一 OpenAPI client 与全局错误处理（`main.tsx` 的 401 跳登录逻辑**不生效**），token 读取逻辑重复。命中点：
  - `douyin_.$taskId.tsx:439-466`（二维码 `TaskQrCode` 拉 `${OpenAPI.BASE}/api/v1/douyin/tasks/${taskId}/qrcode`）
  - `douyin-comments.tsx:1169-1177`（`downloadSelectedComments` → `${browserApiBase()}/api/v1/douyin/comments/export`，还手动解析 `Content-Disposition` 的 `filename*=UTF-8''` 与 `filename=`）
  - `MediaPipelinePanel.tsx:472-475`（`downloadMedia` → `${OpenAPI.BASE}/api/v1/douyin/tasks/${taskId}/media/${asset.id}/file`）
  - `douyin-interactions.tsx:669-673`（`InteractionEvidence`，baseURL 取 `interactionApiBase`，见 749-753 行）
  - `VideoPreviewDialog.tsx:64-70`
  - `douyin_.$taskId.feed.tsx:191,198`、`UnifiedWorksPanel.tsx:1399-1455`（`downloadExport` → `fetch POST ${browserApiBase()}/api/v1/douyin/tasks/${taskId}/exports/${kind}`）
  ｜出处：B-§二 C8、D-§七、G-§二、I-§补充 ｜**现状核对：⬜ 未核对**
- **[BUG]** `MediaPipelinePanel.tsx:474` — `fetch(\`${OpenAPI.BASE}/...\`)` **无兜底**，BASE 为空则 URL 变成 `undefined/...`。**影响**：生产环境下载失败。｜出处：B-§三 H19

## 2.3 C 类 · 横切一致性问题（C1–C11，AI 定性为「一致性债务较重」）

> AI 明确说：「这 11 项是『多风格 / 换肤』目标的主要阻碍」。其中 **②⑤⑦⑧⑨⑪ 六项直接命中数据列表**。

- **[架构·C1｜高]** **表单模式分裂** — `react-hook-form` + `zod` 已安装且 `login` 在用，但 `CreateTaskDialog`、`InteractionComposerDialog`、`BatchCommentDialog`、tracks/keywords 创建表单**全部手写 `useState`**，校验散落在 `submit()` 内、无 `onBlur` 实时反馈、重置不完整。**涉及 6+ 核心表单**。
  ｜`CreateTaskDialog.tsx:127-204`（手写 `useState<FormState>` + `update()`，校验散落在 `submit()` 的 `showErrorToast` 链式校验）
  ｜出处：B-§二 C1、A-§4.2 I6
- **[性能·C2｜高]** **轮询策略不一** — 轮询间隔 1s/2s/3s/5s/10s 混用；多数 query 未加 `refetchIntervalInBackground: false`；任务进入终态后仍在轮询（`InteractionLiveMonitor` 1s 永不停）。**涉及 15+ query**。→ 详见第三卷。
- **[交互·C3｜中]** **确认对话框分裂** — `window.confirm` 与 shadcn `Dialog`/`AlertDialog` 混用 **10+ 处**。命中：删除账号（`douyin-accounts.tsx:379-385`、`:498-504`）、删除赛道、删除关键词（`douyin-keywords.tsx:260-272`）、批量重试（`douyin-interactions.tsx:237`、`:602-615`）、迁移、超 20 条确认（`douyin-library.tsx:819`）、任务列表删除/从头重启（`douyin.tsx:646-648`、`:820-865`）、批量补采（`UnifiedWorksPanel.tsx:494-528`）、标签删除（`douyin-tags.tsx:270`）、互动重试确认（`TaskInteractionsPanel.tsx:179-186`）。
- **[架构·C4｜中]** **错误处理分裂** — `handleError.bind(showErrorToast)` vs `handleError.call(showErrorToast, error as ApiError)` vs `error as never` **三种风格**；`error as ApiError` 对非 ApiError 不安全。命中全站 mutation。具体：`douyin-accounts.tsx:132,151`。
- **[架构·C5｜中]** **重复代码** — `formatDate`/`formatUnix`/`compact` **6+ 处**、`Pager` **2 份**（`douyin-keywords.tsx:1197-1231` vs `douyin.tsx:1556-1588`）、`crawlTypeLabels` **2 份**（`douyin.tsx:89-96` vs `douyin_.$taskId.tsx:64-71`，六项映射完全相同）、`browserApiBase` **2 份**（`douyin_.$taskId.feed.tsx:296-301` 等）、`taskLabel` 2 份、`parseKeywords`/`parseCreatorTargets` 重复、`formatKeywordNotes` 重复（`douyin-keywords.tsx:623-627` vs `douyin-tracks_.$trackId.tsx:501-503`）。
  **`formatDate` 的完整重复清单（本报告核对后修正为 24 份，AI 原文写「6 处」）**：`douyin.tsx:867-874`、`douyin.tsx:1598-1610`（`formatUnix` + `formatDateTimeText`）、`douyin-tags.tsx:242-249`、`douyin-accounts.tsx:877-934`、`douyin-request-logs.tsx:601-606`、`douyin-comments.tsx:1154-1166`、`douyin-keywords.tsx:1238-1245`、`MediaTaskManagement.tsx:564-571`、`TaskInteractionsPanel.tsx:231-236`、`TaskResults.tsx:302-313`、`douyin-interactions.tsx:617`、`douyin-library.tsx:1598-1626`、`douyin_.$taskId.feed.tsx:302-308`、`douyin-creators` 等。
- **[风格·C6｜中]** **CSS 变量 vs 原生色混用** — `MetricCard`（`PageShell.tsx:8-33` 的 `toneStyles` 用 violet/blue/emerald/orange/rose/slate-500）、`StatusPill`（`index.tsx:262-270`）、`CreatorAvatar`（6 色调色板）、`InteractionLiveMonitor`、`AuthLayout.tsx:14,16` 渐变（`from-violet-700 via-primary to-blue-500` + `bg-cyan-300/20`）、`TaskExecutionProgress`（cyan/emerald）、feed 的 `bg-[#050507]`。**涉及 10+ 组件**。
  - 其它原生色点：`HeaderUserMenu` 的 `bg-zinc-600`、`VideoPreviewDialog` 的 `text-red-300`（`:229` 同 feed）、`douyin-browsers.tsx:107` 的 `bg-emerald-500`/`bg-rose-500`、`douyin-browsers.tsx:209` iframe `bg-slate-950`、`MediaTaskManagement.tsx:65-99` 的 `mediaStatusCopy` 6 种状态各一套 Tailwind 硬编码色值（amber/sky/violet/blue/red/emerald + dark 变体）、Logo SVG `stopColor`。
- **[架构·C7｜中]** **localStorage 散落** — `useAuth`/`ViewModeToggle`/`theme-provider`/`VideoPreviewDialog`/feed，**key 硬编码**；`ViewModeToggle.tsx:10,16` **无 try-catch**（隐私模式崩溃）。涉及 5+ 处。
  **视图持久化的三套实现并存**：① `usePersistentViewMode` hook（正确版）；② `douyin-library.tsx:174-181` 手写 `localStorage.getItem("douyin-library-view")` **且无 try/catch**；③ `douyin-tracks.tsx:87-102` 手写 `localStorage`（key `douyin-tracks-view`）**带 try/catch**。
- **[安全·C8｜中]** **手动 fetch + token 绕过 client** → 见 §2.2。
- **[A11Y·C9｜中]** **a11y 系统性缺失** — `Label` 无 `htmlFor`、`SelectTrigger` 无 `aria-label`（10+ 处）、搜索 `Input` 无 `aria-label`（5+ 处）、`img alt=""`、进度条缺 `role="progressbar"`。**涉及 30+ 处**。
- **[i18n·C10｜中]** **i18n 缺失** — 全站中文硬编码无框架；`DataTable` 英文混用（`"No results found."` / `"Showing… entries"` / `"Rows per page"`）。
- **[性能·C11｜高]** **分页不一** — 任务/账号/创作者/评论/互动页 `limit:100` **无分页**（数据直接丢失）；library/comments/creators/tags/request-logs 有分页但**仅上/下一页、无页码跳转**。**涉及一半页面**。

出处：B-§二（完整）、G-§二、A-§4

## 2.4 D 类 · 高严重度缺陷完整清单（H1–H22）

> 除上述 A/B 类已展开者外，其余 11 条如下。

- **[BUG·H10/性能]** `douyin-library.tsx:419` / `douyin-comments.tsx:986` — 导出用 `while` 全量加载，**无上限、无取消**。**影响**：1 万条发 100 次请求 + 10MB 字符串，主线程卡死。→ 详见第三卷 P0-1/P0-2。
- **[性能·H11]** `douyin-keywords.tsx:146` / `douyin-creators.tsx:124` — `limit: 500` 拉全量到前端算指标。**影响**：超 500 时统计不准且浪费带宽。
- **[交互·H12]** `douyin-tracks.tsx:247-485` — **首次加载无 loading 骨架**，直接渲染空表。**影响**：白屏无提示。
- **[交互·H13]** `douyin-tracks.tsx:254-321` — table 视图操作列**只有「运行」**（`:308-314`），缺编辑/停用/删除，而 rows/cards 视图提供。**影响**：**三视图功能不对等**——用户切换视图即丢失操作能力。这是「视图切换」维度最严重的缺陷。
- **[BUG·H14]** `douyin_.$taskId.feed.tsx:244-250` — 字幕 data URI 塞进**单个 `99:59:59` cue**（`WEBVTT\n\n00:00:00.000 --> 99:59:59.000\n${text}`）。**影响**：**字幕时间轴全丢，无法同步高亮**。
- **[BUG·H15]** `douyin_.$taskId.feed.tsx:237` — `<video autoPlay>` **未加 `muted`**。**影响**：浏览器阻止有声自动播放。
- **[A11Y·H16]** `Appearance.tsx:44-128` — 用 `div` 包裹 `DropdownMenuItem`，**破坏 Radix 键盘导航**。**影响**：键盘用户无法用方向键移动焦点。**修复**：改用 `DropdownMenuGroup`（小改）。
- **[BUG·H17]** `BatchCommentDialog.tsx:143-147` — 延迟输入无校验，`Number("abc") = NaN` **仍可提交**。**影响**：向后端提交 NaN。
- **[性能·H18]** `MediaPipelinePanel.tsx:209-210` — 全局 `isPending` 传给所有行，**单行操作禁用全部按钮**。**影响**：批量列表体验差。
- **[性能·H19]** `douyin-interactions.tsx:156-202` — `retryAll` 前端 `do-while` 全量拉取 + `Promise.allSettled` 并发重试，**无进度、无取消**。→ 详见第三卷 P0-3。
- **[BUG·H20]** （与上条同源，AI 在汇总报告里编号 H20）——即 `retryAll`。
- **[BUG·H21]** `_layout.tsx:148 行硬编码` — 布局壳硬编码，**多风格多布局无法落地**。→ 详见第五卷。
- **[BUG·H22]** `_layout.tsx:54-80` — section 映射**遗漏 `/douyin-creators`、`/douyin-request-logs`**，**这两页 header 标题回退为「运营工作台」**。
  ｜**现状核对：✅ 仍存在**（本地 `_layout.tsx:52-80` 的 section map 逐条确认无这两项；同文件用 `useRouterState({ select: state => state.location.pathname })`）
  ｜**修复**：补两行，或改用已存在但未被调用的 `findActiveNavigation()`。
- **[BUG·H23/遗漏项]** `_layout.tsx:116-121` — 「**服务运行正常**」**硬编码绿色**，无健康检查。**影响**：后端故障时仍显示正常，**误导用户**。

出处：B-§三（完整 22 条）

## 2.5 D 类 · 中严重度缺陷完整清单（M1–M30）

| # | 类型 | 位置 | 问题与影响 |
|---|---|---|---|
| M1 | 性能 | tracks/keywords/library/comments/creators/tags | 多处搜索 Input **无防抖** |
| M2 | 性能 | 全局 15+ query | 未加 `refetchIntervalInBackground: false` |
| M3 | 性能 | `douyin-tracks_.$trackId.tsx:124-137` | **3 个 query 同时 10s 轮询** + tasks tab 3s 轮询；另 `:924` 有 3s |
| M4 | BUG | `douyin-tracks_.$trackId.tsx:707` | 达人空状态用 `search` 而非 `searchCreators`，**误报** |
| M5 | BUG | `douyin-tracks_.$trackId.tsx:769-810` | `useEffect` 依赖 `[open, track]`，**track 引用变化即重置用户输入** |
| M6 | 架构 | `douyin-tracks.tsx:648-1654` | `TrackWorkspaceDialog` 单组件 **1000+ 行、20+ `useState`** |
| M7 | 交互 | `douyin-library.tsx:159-187` | 筛选状态**不写回 URL**，刷新/后退/分享即丢失 |
| M8 | 性能 | `douyin-library.tsx:264` | worksQuery **无条件 5s 轮询**，不区分是否有活跃任务 |
| M9 | 交互 | `douyin-library.tsx:916-923` | **缺 `isError` 分支**，请求失败显示「没有符合条件」**误导** |
| M10 | 交互 | `douyin-comments.tsx:283-324` | 筛选应用时机不一：**赛道即时生效，关键词需点「查询」** |
| M11 | 交互 | `douyin-accounts.tsx:380` 等多处 | 用 `window.confirm` 做删除确认（同 C3） |
| M12 | A11Y | `douyin-interactions.tsx:275-307` | 状态/类型 `SelectTrigger` **无 `aria-label`** |
| M13 | 性能 | `douyin-request-logs.tsx:87-92` | `taskMap = new Map(...)` **每次 render 重建**，未 memo |
| M14 | 交互 | `douyin-request-logs.tsx:146` | taskId Select **即时改 state 却不触发查询**，需再点「查询」 |
| M15 | 优化 | `douyin-tags.tsx:30-32` | Route **无 `head` meta title** |
| M16 | A11Y | `douyin-tags.tsx:111-119` | 搜索 Input **无 `aria-label`** |
| M17 | 安全 | `douyin-browsers.tsx:210` | iframe **无 `sandbox` 属性** |
| M18 | 风格 | `douyin-browsers.tsx:107` | 状态点 `bg-emerald-500`/`bg-rose-500` **硬编码，不走 token** |
| M19 | A11Y | `CreateTaskDialog.tsx:807-827` | `CheckField` Checkbox **无 `id`**、`Label` **无 `htmlFor`**，点击文字不联动 |
| M20 | A11Y | `UnifiedWorksPanel.tsx:376-465` | 搜索框 + 4 个 Select **无 `aria-label`** |
| M21 | 优化 | `UnifiedWorksPanel.tsx:193-222` | `recrawlComments` **串行 for 循环**，无进度 |
| M22 | 安全 | `UnifiedWorksPanel.tsx:1399-1455` | 手动 localStorage token + fetch，**绕过 client** |
| M23 | BUG | `ProcessMediaDialog.tsx:74-81` | `useEffect` 在 trackQuery 数据到达时**覆盖用户已修改的值** |
| M24 | BUG | `ResumeTaskDialog.tsx:59-60` | `Number(taskInterval)` **未校验 NaN** |
| M25 | 交互 | `InteractionComposerDialog.tsx:303-318` | 模板 Select `value=""` 恒空，**选中后无视觉反馈** |
| M26 | 性能 | `InteractionLiveMonitor.tsx:68,74` | detail 1s + slots 2s 双高频轮询，**无退避** |
| M27 | 性能 | `InteractionLiveMonitor.tsx:279-305` | 执行步骤**全量渲染，无虚拟滚动** |
| M28 | i18n | `DataTable.tsx:86-186` | **英文硬编码**与中文界面混用 |
| M29 | 性能 | `Sidebar/Main.tsx:33` | `useRouterState()` **无 `select`**，全量订阅致不必要重渲染。｜**现状核对：✅ 仍存在**（本地 `:33` `const router = useRouterState()` 无参） |
| M30 | 风格 | `theme-provider.tsx:47-89` | **无 FOUC 防护**，首屏主题闪烁 |

出处：B-§四（完整 30 条）

## 2.6 D 类 · 低严重度缺陷（60+ 条，原文按类归并）

> **这是原文本身的处理方式**——低严重度条目在汇总报告中未被逐条编号，只给出了归类统计。以下为原文给出的全部类别与实例。

- **[架构]** 重复代码 — `formatDate`/`formatUnix` 6 处（实为 24 处，见 §2.3 C5）、`Pager` 2 份、`crawlTypeLabels` 2 份、`browserApiBase` 2 份、`handleError` 调用方式 3 种。
- **[风格]** 硬编码魔法数字 — `maxLength 10000`、关键词上限 200、`pageSize` 32/50/200 不一、`delayLevel` 区间。
- **[风格]** 原生色不走 token — `TaskExecutionProgress` cyan/emerald、`CreatorAvatar` 6 色调色板、`HeaderUserMenu` `bg-zinc-600`、`VideoPreviewDialog` `text-red-300`、Logo SVG `stopColor`。
- **[安全]** 外链 rel — 多处缺显式 `noopener`（仅 `noreferrer`）。命中：`douyin-interactions.tsx:348`、`douyin-browsers.tsx:181,194`、`TaskInteractionsPanel.tsx` 的 `target_video_url`。
- **[架构]** 类型安全 — `Sidebar`/`User` 的 `user: any`、`utils.ts` 的 `err.body as any`、`taskParameters` 用 `as` 断言。
- **[A11Y]** 可访问性 — 图标无 `aria-hidden`、`HorizontalNavigation` 隐藏滚动条无指示、`blockquote` 无 `cite`、`Pager` 无 `aria-label`。
- **[性能]** 性能 — 多处 `filter`/`reduce`/`map` 未 memo、`selected` 用数组 O(n) 而非 `Set`、每行挂载 Dialog 实例。
- **[BUG]** 健壮性 — localStorage 无 try-catch、`setTimeout` 未清理、`URL.revokeObjectURL` 同步调用。

出处：B-§五

## 2.7 D 类 · 未进入汇总报告的独有发现（AI 自补第三组审查）

> 这些是 AI 在自补审查中发现、但**未进入最终汇总报告**的条目——属于「原文里有、报告里没有」的增量信息，本报告专门保留。

- **[BUG]** `douyin-interactions.tsx:110-120` — detail query 使用 `detailId!` **非空断言，类型不安全**。
- **[BUG]** `douyin-interactions.tsx:84` — `monitorId` state **被设置但后续用途不明，疑似死状态**。
- **[交互]** `douyin-interactions.tsx:188-191` — `retryAll.onError` 拼接**超长 Error message** 后用 `showErrorToast` 展示，**体验差**。
- **[产品]** `douyin-interactions.tsx` — 列表**缺空状态 / 错误态处理**（AI 未看全）。
- **[架构]** `douyin-accounts.tsx:343-358` — 登录/验证按钮 `disabled` 条件在**两处完全重复**，可提取公共判断。
- **[性能]** `douyin-request-logs.tsx:85` — tasks query `limit: 200` **且无 `staleTime`**，每次挂载都重新请求。
- **[性能]** `douyin-request-logs.tsx:94-97` — logs query **既无 `refetchInterval` 也无 `staleTime`**，策略缺失（非有意）。
- **[交互]** `douyin-request-logs.tsx:329-332` — 加载态文案「加载请求日志...」与全站风格不统一。
- **[产品]** `douyin-request-logs.tsx` — **无导出功能**（疑似有意缺失）。
- **[性能]** `douyin_.$taskId.feed.tsx:210` — preview URL 用 `?v=${Date.now()}` 做 cache busting，**effect 重跑即换 URL，导致 video 反复重新加载**。
- **[交互]** `douyin_.$taskId.feed.tsx:72-79` — keydown 处理**未 `preventDefault`**，方向键可能同时滚动页面。
- **[交互]** `douyin_.$taskId.feed.tsx:34` — `index` state **无 URL 同步，刷新即丢失当前播放位置**。
- **[交互]** `douyin_.$taskId.feed.tsx:63-70` — `start` 参数**只在初始化用一次**，后续切换不写回 URL。
- **[BUG]** `douyin-browsers.tsx:28-34` — `useEffect` 依赖 `[selectedName, slots]`，**slots 每次轮询都是新引用，effect 每轮都执行**。
- **[风格]** `douyin-browsers.tsx:69` — `xl:sticky xl:top-0` **存在与 header 重叠风险**。
- **[性能]** `douyin-tags.tsx:102,106` — `rows.reduce` **两次遍历**计算本页视频/任务数，未 memo。
- **[交互]** `douyin-tags.tsx:179-193` — `Link search` 传入**一堆 `undefined` 参数，URL 脏**。
- **[交互]** `douyin-tags.tsx:84-91` — 同步按钮**无二次确认**。
- **[BUG]** `douyin-tags.tsx:208` — **仅判 `isLoading`，缺 `isError` 分支**，失败时表格显示空。
- **正面项（AI 明确标注「非遗漏」）**：`douyin-accounts.tsx:125` `window.open(viewer_url)` 已正确带 `noopener,noreferrer`；`douyin-accounts.tsx:83-88` Set state 用法正确；`douyin-accounts.tsx:231` Tabs 使用正常；`douyin-request-logs.tsx:184-203` `inputMode="numeric"` + `isNaN` 检查正确；`douyin-request-logs.tsx:475-486` `StatusBadge` 实现合理；`douyin_.$taskId.feed.tsx:85-92` wheel 滚动锁实现合理；`douyin-browsers.tsx:126-130` 空状态有处理；`douyin-tags.tsx:34` `pageSize=50` 有分页；`douyin-tags.tsx:60` `placeholderData: previous` 使用正确。

出处：B-§九

## 2.8 页面级缺陷（F1–F6 / I1–I8 / P1–P8，第 4 轮「风格·交互·页面」审查）

> 这一轮早于 212 条审查，是后者的输入。F1/I1/I2/I3/I4/I5/I6/I7/I8/P1/P2/P3/P4/P5/P6/P7/P8 全部列出。

### 风格缺陷 F1–F6

- **[风格·F1]** `Common/PageShell.tsx:47,53-95` — `PageHero` 不渲染 `description` → 见 §2.1 A1（**已核对仍在**）。
- **[风格·F2]** `Common/AuthLayout.tsx:14` — 左侧大渐变硬编码 `from-violet-700 via-primary to-blue-500` + 装饰圆 `bg-cyan-300/20`。**影响**：切换 ocean/graphite skin 时登录页左侧仍是紫蓝渐变，**与主题割裂**。｜**现状核对：✅ 仍存在**（本地 `:14` `bg-gradient-to-br from-violet-700 via-primary to-blue-500`、`:16` `bg-cyan-300/20` 逐字确认）
- **[风格·F3]** `PageShell.tsx:8-33`（`toneStyles`）+ `index.tsx:262-270`（`StatusPill`）— 卡片/药丸配色**不响应 skin 切换**，永远固定色；与走 token 的主色系并存，**视觉不统一**。
- **[风格·F4]** `Common/Appearance.tsx:88-93` — 主题风格选项 `[ocean→"清透蓝", graphite→"专业灰", violet→"跃动紫"]` **硬编码在组件里**。**影响**：加新 skin 必须改此组件，**无注册表**。｜**现状核对：✅ 仍存在**（本地 `:90-92` 三个硬编码选项）
- **[交互·F5]** `Appearance.tsx:134-169`（`Appearance`，仅明暗 3 项）vs `:23-132`（`UserMenuAppearance`，明暗 + 主题风格 + 信息密度）— **登录页无法切换 skin/density**，登录前后外观控制能力不一致。
- **[风格·F6]** `index.css:199-216` — `body` 的三层 `radial-gradient` 装饰光晕**写死在 `@layer base`**。**影响**：所有 skin 共享同一背景光晕，skin 无法定义自己的装饰语言。

### 交互缺陷 I1–I8

- **[BUG/安全·I1]** `settings.tsx:30-32` → 见 §2.1 A2（**已核对仍在**）。
- **[安全·I2]** `login.tsx:57-58` → 见 §2.2 H3。
- **[BUG·I3]** `admin.tsx:23-29` — `beforeLoad` 直接 `await UsersService.readUserMe()` 做鉴权。**影响**：token 失效时路由抛错；与 `_layout` 的 `isLoggedIn()` **双重鉴权**，网络慢则白屏卡住。
- **[BUG·I4]** `douyin_.$taskId.tsx:439-466` — 二维码用原生 `fetch` + `localStorage` 手动拼 `Authorization` → 见 §2.2 C8。
- **[交互·I5]** `douyin_.$taskId.tsx:465` — 二维码 `window.setInterval(load, 15_000)` 固定刷新，**无 loading 态、无失败重试退避**；二维码过期/网络抖动时体验差。
- **[交互·I6]** `CreateTaskDialog.tsx:127-204` — 手写 `useState<FormState>` + `update()`，校验散落在 `submit()` → 见 §2.3 C1。
- **[性能·I7]** 多处轮询策略不一致：任务列表 3s（`douyin.tsx:127`）、任务详情 2s（`douyin_.$taskId.tsx:86`，仅 active）、工作台 5s（`index.tsx:41`）、任务分片 2s（`douyin_.$taskId.tsx:373`）→ 见第三卷。
- **[BUG/交互·I8]** `douyin.tsx:183-186` — `Tabs defaultValue="crawl"` + `onValueChange` 设置 `businessTab`，但 `businessTab` **仅用于控制 `CreateTaskDialog` 显隐**，`TabsContent` 两块是静态的。**影响**：state 与 Tabs 重复控制，逻辑绕。根因：`douyin.tsx:108` 的 `businessTab` state 与 `Tabs defaultValue` 两处 state 同源不同步。

### 页面缺陷 P1–P8

- **[产品/BUG·P1]** 多处 `limit: 100` **硬编码，无分页/无限滚动**：`douyin.tsx:124`、`admin.tsx:16`、`index.tsx:39,45`、`CreateTaskDialog.tsx:137,151`。**影响**：超过 100 条的数据**直接丢失且无提示**；工作台用 `dataScope = "近 100 个任务" / "全部任务"` 文案掩盖但**未提供查看全部入口**。
- **[架构/BUG·P2]** `_layout.tsx:54-80` vs `navigation.ts:119-128` — pathname→section 标题映射在 `_layout` 硬编码 **27 行嵌套三元 if/else**；`navigation.ts` 已有 `findActiveNavigation()` 却未被使用。**影响**：两套导航逻辑并存，加菜单项要改两处，易不同步。→ 同 H22。
- **[i18n/风格·P3]** **品牌名三处不一致**：`login.tsx:44`/`settings.tsx:22`/`admin.tsx:34`/任务详情用「**灵感采集台**」；`index.tsx:32` 用「**运营工作台**」；`AuthLayout.tsx:39` 底部用「**抖音内容运营工作室**」。**无统一品牌常量**。
- **[架构·P4]** `douyin.tsx:89-96` 与 `douyin_.$taskId.tsx:64-71` — `crawlTypeLabels`（search/detail/creator/creator_from_aweme/liked/collected **六项映射**）**完全相同的表在两个文件各定义一份**。
- **[BUG/数据·P5]** `index.tsx:54` vs `douyin.tsx:131-134,143-145` — **「需关注」状态集合定义不一致**：工作台用 `["failed","interrupted","waiting_login"]`，任务列表用 `["failed","cancelled","interrupted","waiting_login"]`（**多 `cancelled`**）。**影响**：跨页面「需关注」计数口径不一致，**dashboard 和任务列表数字对不上**。
- **[A11Y/交互·P6]** `douyin_.$taskId.tsx:229-251` — 详情页 `TabsList` 写死 `grid grid-cols-3`，`TabsTrigger` 有 `min-h-11` 但**无横向滚动**。**影响**：窄屏三个 tab（任务概览/作品数据/互动记录）挤一行，标签 + 计数 badge 会换行/截断。
- **[BUG·P7]** `douyin.tsx:135-155` — `filteredTasks` 的 `useMemo` 依赖数组为 `[searchTerm, statusFilter, tasks]`，**未列 `trackId/sourceValue`**；搜索匹配里调用了 `taskBrowserMode(task)` 却未在依赖中体现。**影响**：潜在陈旧闭包（当前因 `tasks` 已含筛选结果而影响轻微）。
- **[BUG·P8]** `vite.config.ts` + 运行环境 — **Vite 7 要求 Node ≥22.12，当前 22.11.0**。**影响**：非代码缺陷，但 **CI/容器需固定 Node ≥22.12**，否则构建可能异常。

### 第 4 轮优先级排序（最该先修的 5 项）

1. **F1** `PageHero` 不渲染 `description` — 一行修复，**影响全站 hero 文案**。
2. **I1** `settings.tsx` 超管分支逻辑无效 — **安全相关**，普通用户可见删除账号。
3. **I2** `login.tsx` 默认凭据预填 — 生产安全。
4. **P1** 列表无分页 — **功能性数据丢失**。
5. **F2/F3** `AuthLayout`/`MetricCard` 不响应 skin — **直接阻碍「多风格」目标**。

**AI 结论：F1/I1/I2 均为一两行的小修，可立刻动手。**

### 第 4 轮确认但未单列为缺陷的观察

- **[优化·正面]** `douyin.tsx` 列表刷新按钮 `RefreshCw` 与 `isFetching` 联动（`animate-spin` + `disabled`），**已实现加载反馈**。
- **[A11Y·正面]** 任务列表筛选具备完整 A11Y：`<fieldset>` + `<legend className="sr-only">任务状态筛选</legend>`、按钮 `aria-pressed`、`<h2 id="task-list-heading" className="sr-only">` + `<section aria-labelledby>`、`aria-label="按赛道筛选任务"`/`"按关键词或作者筛选任务"`/`"刷新任务"`/`"全选可删除任务"`（源 3718-3805 行）。
- **[交互·正面]** 任务列表支持三种视图模式（`ViewModeToggle` + `usePersistentViewMode("douyin-tasks-view")`：`cards`/`rows`/表格），并有空状态 `EmptyState`（无任务 / 无匹配任务）与 `QueryErrorState` 重试态。
- **[产品·正面]** 工作台首页有「X 项任务需要处理」告警卡（`attentionCount > 0` 时显示，引导跳 `/douyin`）与三个 `StatusPill` 状态摘要、四个 `MetricCard`（任务总数/作品/评论/可用账号）。

出处：A-§4.1–4.5

## 2.9 ⚠️ AI 判定需要修正的地方（本报告最关键的一节）

> 用户问的是「AI 的答案和我们的项目是否匹配」。以下是我逐项核对后的**偏差清单**——AI 的报告**准确率很高**（行号基本逐字命中），但存在三类系统性偏差。

### 偏差 1：计数系统性**低估**

AI 报的数字普遍是**保守下界**，实际更多：

| 项 | AI 报的数 | 实际核对结果 |
|---|---|---|
| `formatDate` 重复 | 「6 处」 | **24 处**（见 §2.3 C5 清单） |
| `refetchInterval` 全局 | 「34 处」（E 段实测） | 与 E 段一致，但 AI 在最终报告里只写了「12 处需改」 |
| 未 memo 派生计算 | 「18 处」 | 分文件统计至少 **40+ 处** |
| `limit:100` 无分页页面 | 「约一半页面」 | 明确命中 **10 个列表** |
| 横切关注 | 「11 项」 | 保留，准确 |

### 偏差 2：**H1 的对照物容易被误读**

`PageShell.tsx` 里 **有两个组件都叫 description**：`PageHero`（47 行声明、**不渲染**）与 `SectionHeading`（158-182 行、**173-175 行确实渲染**）。
**AI 的判定是对的**（PageHero 确实不渲染），但任何复核者若只 grep `description` 看到 `SectionHeading` 那一段，会误以为「已经修好了」。**这一点我在本次核对中亲自踩过坑，特此标记。**

### 偏差 3：一条「bug」其实是**刻意的功能**

- **H3**（`login.tsx:57-58` 默认凭据预填）：本地 git commit `f6bd043 feat: prefill default admin credentials in login form` 证明这是**主动加的功能**。结论本身没错（确实有安全风险），但**排期前应先确认产品意图**，而不是当成疏漏去「修」。

### 偏差 4：原会话**没有做任何运行时验证**

所有性能结论都是**静态审查**。AI 自己也明确写了：Playwright 因 Chromium 沙箱失败、dev server 已停、无运行时 trace。**「必现卡顿」「卡死 20-50s」这类量化描述的置信度应低于其字面表述**——它是基于代码结构的合理推演，不是实测数据。

### 偏差 5：文档本身**有严重损坏区**

导出工具在多个区间做了「逐行交错」污染，把两段无关文本拼到同一物理行。已知损坏区：
- **源 62838–68000 行**（列表专题中段）：`> 💭 思考过程` 与正文交替，句子被逐字拆散（残片如 `douyin819-key行`、`-inter/actions`、`jiao`）。部分行号因此**不可确证**。
- **源 68000–73350 行**：同一物理行里两段文本被拼接（例如「请求日志列表」表格与「任务列表」表格按列交错）。
- **源 75220 行起（PRD 段）**：混入 `-trackId）`、`5>`、`8>`、`Dy_V2`、`同时转9字幕`、`A处理A`、`�` 等垃圾 token，且**在 `interface PipelineTemplate { id; name; description?` 之后中途截断**——会话导出就此结束。

**受影响的「行号不确定项」**（提取文件已就地标注）：`douyin-creators` 的 pageSize/refetchInterval、`douyin-library` 的排序字段行号、`Pager` 组件的归属文件与行号（1556-1588 / 1197-1231 / 130,136 三组混排）、`douyin-request-logs` 的部分维度行号、`douyin-browsers` 的 `invalidate` queryKey。

出处：H-§十七、I-§卷首

---
# 第三卷 · 性能问题全清单

> **前置说明（必须读）**：本卷所有内容来自**静态代码审查**。原会话的运行时验证全部失败——Playwright 因 Chromium 沙箱（root 环境）崩溃、vite dev server 已停（端口 5173 无响应）、mcp-chrome 未可用。
> **因此「卡死 20-50s」这类数字是代码结构推演，不是实测。** AI 自己也标注了这一点。修复仍是必要的，但排期时不要把推演数字当作 SLA 承诺。

## 3.0 量化底座（全仓 grep 实测，可直接引用）

| 指标 | 数值 | 含义 |
|---|---:|---|
| `refetchInterval` 出现次数 | **34** | 轮询点总数 |
| `staleTime` 出现次数 | **8** | 缓存策略覆盖率 = 8/34 ≈ **24%** |
| `refetchIntervalInBackground` | **0** | **后台标签页仍在全速轮询** |
| 高频轮询（≤5s）且无暂停条件 | **14 处** | P1 级 |
| 未 memo 的派生计算（`filter`/`map`/`reduce`/`sort`） | **18 处**（分文件统计达 40+） | P2 级 |
| 串行 `await` 循环 | **5 处** | P0/P1 级 |
| 前端全量加载（`while` 翻页） | **4 处** | P0 级 |
| 搜索无防抖 | **6 处** | P2 级 |
| `selected` 用数组 + `includes()` O(n) | **3 处** | P2 级 |
| 每行挂载重 hook/组件 | **2 处** | P2 级 |
| `React.memo` 使用次数（全 44,200 行） | **0** | 🔴 结构性缺失 |
| `useMemo` | **33** | 相对 16,018 行页面代码，密度极低 |
| `useCallback` | **9** | 同上 |
| `createObjectURL` / `revokeObjectURL` | 17 处 / **全部 revoke 时机正确** | ✅ AI 明确认定「此处实现正确，不需改」 |
| `setInterval` / `setTimeout` | **5 处** | — |
| `limit: 100` / `limit: 500` 硬编码 | 多个列表 | 数据丢失（见 P1 级 §2.2 P1） |

出处：C-§量化统计、E-§全量清单、F-§量化

## 3.1 P0 · 卡死级（4 条，必修）

- **[性能·P0-1｜🔴]** `douyin-comments.tsx:987` — **导出时 `while (items.length < total)` 全量翻页**，无上限、无取消、无进度。
  **推演后果**：1 万条评论 = 100 次请求 + 10MB 字符串拼接，主线程卡死 20-50s。
  ｜**修复**：改为后端一次性导出接口（`douyin.tsx` 已有 exports 机制可复用）。
  ｜**现状核对：✅ 仍存在**（本地 `:987` `while (items.length < total)` 逐字确认）

- **[性能·P0-2｜🔴]** `douyin-library.tsx:419-421` — 同上模式：`while (works.length < total) { ... }` 串行翻页拉全量作品后前端拼接导出。
  ｜**修复**：同上，走后端导出。
  ｜**现状核对：✅ 仍存在**（本地 `:420` `while (works.length < total)` 逐字确认）

- **[性能·P0-3｜🔴]** `douyin-interactions.tsx:156-202` — `retryAll`：前端 `do { ... } while` **先全量拉取所有失败项**，再 `Promise.allSettled` **无节流并发重试**，无进度、无取消、无失败隔离。
  **推演后果**：几百条重试会瞬时打爆后端，并冻结 UI。
  ｜**修复**：后端批处理端点 + 前端进度条 + 可取消。
  ｜**现状核对：✅ 仍存在**（本地 `:156` `do {`、`:161` `while`、`:178` `Promise.allSettled` 位置确认）

- **[性能·P0-4｜🔴]** **零 `React.memo` + 零虚拟滚动**，在 16,018 行页面 + 1,479 行 `UnifiedWorksPanel` 的规模下，任何父组件 state 变更都会重渲染整棵树。
  ｜**修复**：优先给列表行组件加 `React.memo` + 引入虚拟滚动（见第五卷列表基建）。

## 3.2 P1 · 高频轮询必现问题（7 条）

- **[性能·P1-1]** `InteractionLiveMonitor.tsx:68,74` — **detail 每 1s 轮询 + slots 每 2s 轮询，无退避、无终态停止**（`refetchInterval: open ? 1_000 : false` / `open ? 2_000 : false`）。
  **推演后果**：这是全站最高频轮询；弹窗开着即持续 1s/次，任务完成后也不停。
  ｜**修复**：统一 `useSmartPolling`（见第五卷）。
  ｜**现状核对：✅ 仍存在**（本地 `:68`、`:74` 逐字确认）

- **[性能·P1-2]** `douyin-tracks_.$trackId.tsx:124,130,136,924` — **同一页面 4 个轮询**：三个 query 各 `10_000`（124/130/136 行），任务 tab 额外 `3_000`（924 行）。**无联动、无终态停止**。
  ｜**现状核对：✅ 仍存在**（本地四行逐一确认）

- **[性能·P1-3]** `Sidebar/Main.tsx:33` — `useRouterState()` **未传 `select`**，订阅**整个** router state；任何路由状态微变（含 pending、matches 数组重建）都触发侧边栏整棵重渲染。全站每页都挂侧边栏，**影响面 100%**。
  ｜**修复**：1 行——`useRouterState({ select: s => s.location.pathname })`。
  ｜**现状核对：✅ 仍存在**（本地 `:33` `const router = useRouterState()`）

- **[性能·P1-4]** 后台标签页不停轮询 — **全仓 `refetchIntervalInBackground` 出现次数为 0**。用户切到别的标签页，34 个轮询点照常全速跑，持续消耗 CPU 与后端配额。
  ｜**修复**：`QueryClient` 默认值里加 `refetchIntervalInBackground: false`（1 行，全局生效）。

- **[性能·P1-5]** **无全局 `staleTime`** — 34 个轮询点里只有 8 个设了 `staleTime`。其余每次挂载/窗口聚焦都重新请求（TanStack Query v5 默认 `staleTime: 0`）。
  ｜**修复**：`QueryClient` 加 `defaultOptions.queries.staleTime: 30_000`（**这正是 AI 曾尝试加的改动，后被用户叫停并要求只出计划**）。

- **[性能·P1-6]** `douyin-library.tsx:264` — worksQuery **无条件 5s 轮询**，不判断当前页是否有活跃任务。空列表也在跑。
  ｜**修复**：`refetchInterval: hasActiveTask ? 5_000 : false`。

- **[性能·P1-7]** **无 `retry` 退避策略** — 默认重试 3 次固定间隔；后端 500 时会雪上加霜。

## 3.3 P2 · 明显劣化（10 条）

- **[性能·P2-1]** **未 memo 派生计算**：`douyin-library.tsx:284-299`（**8 处**连续派生）、`douyin-request-logs.tsx:87-92`（`taskMap = new Map(...)` 每次 render 重建）、`douyin-tags.tsx:102,106`（`rows.reduce` 两次遍历）、`douyin-tracks.tsx`、`douyin-keywords.tsx`、`index.tsx` 均有。**总计 18–40+ 处**。
- **[性能·P2-2]** `UnifiedWorksPanel.tsx:226,228,239,646,794,829` — `selected` 用 `useState<string[]>([])` + `.includes()` → **6 处 O(n) 查找**，且每次全量复制数组。**改用 `Set` 即可**。
- **[性能·P2-3]** `UnifiedWorksPanel.tsx` `recrawlComments` — **串行 for 循环逐条调用**，无并发、无进度。数百条即分钟级等待。
- **[性能·P2-4]** **搜索无防抖 — 6 处**：`douyin-tracks.tsx`、`douyin-keywords.tsx`、`douyin-library.tsx`、`douyin-comments.tsx`、`douyin-creators.tsx`、`douyin-tags.tsx`。每次击键都触发 filter + 后端 query。
- **[性能·P2-5]** **每行挂载重组件 — 2 处**：表格每行渲染完整 `Dialog`（如 `MediaTaskManagement`、`TaskInteractionsPanel`），N 行 = N 个 Dialog 实例常驻 DOM。
- **[性能·P2-6]** `MediaPipelinePanel.tsx:209-210` — 全局 `isPending` 传给所有行 → **单行操作禁用全部按钮**。
- **[性能·P2-7]** `InteractionLiveMonitor.tsx:279-305` — 执行步骤**全量渲染，无虚拟滚动**；长任务步骤多时 DOM 爆炸。
- **[性能·P2-8]** `douyin-browsers.tsx:28-34` — `useEffect` 依赖 `[selectedName, slots]`，而 `slots` **每轮轮询都是新数组引用** → effect **每轮都执行**（典型"引用不稳定"陷阱）。
- **[性能·P2-9]** `douyin_.$taskId.feed.tsx:210` — preview URL 用 `?v=${Date.now()}` 做 cache busting，**effect 重跑就换 URL** → `<video>` 反复重新加载，播放位置丢失。
- **[性能·P2-10]** `douyin-request-logs.tsx:85` — tasks query `limit: 200` **且无 `staleTime`**，每次挂载重新拉 200 条。

## 3.4 P3 · 资源浪费（6 条）

- **[性能·P3-1]** 手动 `fetch` 绕过 axios 实例 — **6+ 处**（详见 §2.2 C8），每次都要重新读 token、无统一超时/重试。
- **[性能·P3-2]** `douyin_.$taskId.tsx:465` — 二维码 `setInterval(load, 15_000)` **固定间隔**，无退避、无失败停止。
- **[性能·P3-3]** `douyin-request-logs.tsx:94-97` — logs query **既无 `refetchInterval` 也无 `staleTime`**，策略完全缺失（非有意设计）。
- **[性能·P3-4]** 硬编码 `pageSize` 不一致 — 32 / 50 / 100 / 200 / 500 **混用**，无统一常量。
- **[性能·P3-5]** `douyin.tsx:135-155` — `useMemo` 依赖数组不完整（未列 `trackId`/`sourceValue`），潜在陈旧闭包。
- **[性能·P3-6]** `theme-provider.tsx:47-89` — **无 FOUC 防护**：主题在 `useEffect` 里才写入 `document.documentElement.dataset`，首屏会闪一下默认主题。

## 3.5 三大根因（AI 的归因分析）

- **[根因·G1]** **没有缓存层契约** — `staleTime` 覆盖率仅 24%，`QueryClient` 无 `defaultOptions`。所有页面各自决定缓存策略，导致 34 个轮询点各自为政。
  → **一处修全局**：补 `QueryClient.defaultOptions`。

- **[根因·G2]** **没有轮询抽象** — 34 处 `refetchInterval` 手写，无统一的「终态停止 / 后台暂停 / 指数退避 / 条件启用」逻辑。
  → **一处修全局**：抽 `useSmartPolling`（见第五卷）。

- **[根因·G3]** **没有列表抽象** — 分页、筛选、排序、选择、空/错/加载三态、虚拟滚动**每个页面各写一遍**（20 个列表页面）。
  → **一处修全局**：把 `DataTable` 扩成服务端列表骨架（见第五卷）。

**AI 的核心判断：这三个根因解释了 80% 的性能问题。修具体点只能止血，修这三处才能根治。**

## 3.6 性能问题分布热力图（按文件）

| 文件 | 问题类型 | 严重度 |
|---|---|---|
| `UnifiedWorksPanel.tsx` | O(n) selected ×6、串行循环、refetchInterval ×2、手动 fetch | **高** |
| `douyin-library.tsx` | P0 while 全量、无条件 5s 轮询、8 处未 memo、缺 isError | **高** |
| `douyin-comments.tsx` | P0 while 全量、无防抖、手动 fetch 导出 | **高** |
| `douyin-interactions.tsx` | P0 retryAll、无防抖、手动 fetch ×2 | **高** |
| `InteractionLiveMonitor.tsx` | 1s+2s 双高频轮询、无虚拟滚动 | **高** |
| `douyin-tracks_.$trackId.tsx` | 4 个轮询、2205 行单文件 | **高** |
| `douyin-tracks.tsx` | 1936 行、Dialog 1000+ 行、3 视图不对等 | 中 |
| `douyin-request-logs.tsx` | taskMap 未 memo、limit 200 无 staleTime | 中 |
| `douyin-keywords.tsx` | limit 500 全量、无防抖 | 中 |
| `douyin-creators.tsx` | limit 500 全量、无防抖 | 中 |
| `MediaPipelinePanel.tsx` | 全局 isPending、OpenAPI.BASE 无兜底 | 中 |
| `douyin-browsers.tsx` | effect 引用陷阱、iframe 无 sandbox | 低 |
| `douyin-tags.tsx` | reduce ×2 未 memo、缺 isError | 低 |
| `Sidebar/Main.tsx` | useRouterState 无 select | 中（**影响面 100%**） |

## 3.7 AI 明确认定「实现正确、不要动」的性能点（防误改）

- ✅ **`createObjectURL`/`revokeObjectURL` 17 处配对全部正确** — revoke 时机无泄漏。命中：`douyin-library.tsx:448,455`、`VideoPreviewDialog.tsx`、`MediaPipelinePanel.tsx` 等。
- ✅ `douyin-accounts.tsx:83-88` — `Set` state 用法正确（这是「正确实现」的范例，其他文件应向它看齐）。
- ✅ `douyin-request-logs.tsx:184-203` — `inputMode="numeric"` + `isNaN` 检查正确。
- ✅ `douyin_.$taskId.feed.tsx:85-92` — wheel 滚动锁实现合理。
- ✅ `douyin-tags.tsx:60` — `placeholderData: previous` 使用正确（保留上一页数据避免闪烁，这是**正面范例**）。
- ✅ `douyin.tsx` 列表刷新按钮与 `isFetching` 联动（`animate-spin` + `disabled`）— 加载反馈正确。
- ✅ 任务列表筛选的 A11Y 实现（`fieldset`/`legend`/`aria-pressed`/`aria-labelledby`）— **全站最佳实践范例**（源 3718-3805 行）。

出处：C-§结论、D-§九、E-§正面项、F-§七

## 3.8 八类卡顿场景（AI 的结构化推演）

| 场景 | 触发条件 | 卡顿来源 | 对应条目 |
|---|---|---|---|
| ① 打开互动监控弹窗 | 点任一互动任务 | 1s+2s 双轮询 + 步骤全量渲染 | P1-1、P2-7 |
| ② 导出评论 | 点「导出选中」 | while 全量翻页 + 10MB 字符串 | P0-1 |
| ③ 导出作品 | 同 ② | 同 ② | P0-2 |
| ④ 批量重试互动 | 点「重试全部」 | 全量拉取 + 无节流并发 | P0-3 |
| ⑤ 打开任一列表页 | 路由切换 | 34 轮询点重新挂载 + 无 staleTime | P1-5、G1 |
| ⑥ 在列表里打字搜索 | 输入关键词 | 无防抖 ×6 | P2-4 |
| ⑦ 切换赛道 Tab | 点赛道 | 2205 行页面 4 个轮询同时重挂 | P1-2 |
| ⑧ 长时间挂着页面 | 离开工位 | 后台标签页全速轮询（`refetchIntervalInBackground` 全仓为 0） | P1-4 |

## 3.9 性能修复的 10 步 ROI 路线图（AI 原方案）

> **这是「性能卷」的路线图。第六卷另有一份「列表基建」的阶段 0–4 计划，两者已在本报告 §5.5 中协调合并——不要分别执行。**

| 步 | 动作 | 改动量 | 收益 | 涉及 |
|---:|---|---|---|---|
| 1 | `QueryClient` 补 `defaultOptions`（`staleTime: 30_000`、`gcTime`、`refetchIntervalInBackground: false`、`retry: 1`） | **1 文件** | 全局降载 | G1、P1-4、P1-5、P1-7 |
| 2 | `Sidebar/Main.tsx` 补 `select` | **1 行** | 每页都受益 | P1-3 |
| 3 | `PageHero` 补 `description` 渲染 | **1 行** | 全站文案恢复 | §2.1 A1 |
| 4 | `settings.tsx` `slice(0,3)` → `slice(0,2)` | **1 行** | 修复权限漏洞 | §2.1 A2 |
| 5 | `douyin-library.tsx` invalidate key 修正 | **1 行** | 修复下拉不刷新 | §2.1 A3 |
| 6 | 修 4 个 P0（两个 while 导出、retryAll、虚拟滚动） | 4 处 | 消除卡死 | P0-1~4 |
| 7 | 抽 `useSmartPolling` 并替换 34 处 | 新建 1 + 改 34 | 根治轮询乱象 | G2 |
| 8 | 抽 `ListShell` + 共享 `Pager` + 扩 `DataTable` | 新建 3 | 20 个列表统一 | G3 |
| 9 | 6 处搜索加防抖 + `selected` 改 `Set` + 18 处补 `useMemo` | 30 处 | 交互流畅 | P2-1~4 |
| 10 | 6 处手动 `fetch` 收回统一 client | 6 处 | 错误处理统一 | P3-1、C8 |

**AI 强调：第 1–5 步合计只改 5 行代码，收益覆盖全站，应当在任何重构之前完成。**

---
# 第四卷 · 数据列表架构专题

> 这是原会话投入最重的一轮：**12 个维度 × 20 个列表**逐项判定，共 1,200+ 条检查记录。AI 的结论是：**「当前列表架构处于『每个列表各自为政』的作坊式状态」**——这是整个前端最大的架构债务。

## 4.1 审查框架：12 个维度（统一评分标准）

| # | 维度 | 检查内容 |
|---:|---|---|
| ① | **分页** | 服务端/客户端/无；pageSize 常量；页码跳转；硬编码 limit |
| ② | **筛选架构** | URL 同步 vs 本地 state；即时 vs 草稿；防抖；条件组合 |
| ③ | **排序** | 服务端/客户端/无；排序字段 |
| ④ | **选择与批量** | `Set` vs `Array+includes`；全选范围；批量二次确认；批量后是否清空 |
| ⑤ | **视图切换** | 卡片/横条/表格；持久化是否统一；**三视图功能是否对齐** |
| ⑥ | **三态完备** | 空/加载/错误；骨架屏 vs 文案；重试；各视图一致性 |
| ⑦ | **数据刷新** | 轮询策略；终态停止；`refetchIntervalInBackground: false`；手动刷新；`invalidate` queryKey 正确性 |
| ⑧ | **组件复用** | 通用 `DataTable` vs 手写 `Table`；列定义复用；操作列重复 |
| ⑨ | **性能** | 虚拟滚动；memo；每行重 hook；选中查找复杂度 |
| ⑩ | **无障碍** | 表格语义；`aria-label`；复选框标签；键盘 |
| ⑪ | **响应式** | 列隐藏；横向滚动；操作列溢出 |
| ⑫ | **行 key 稳定性** | `id` vs 索引 |

## 4.2 逐列表判定矩阵（20 个列表）

> 标记：✅ 通过 ｜ ⚠️ 部分 ｜ ❌ 缺陷 ｜ 🔴 严重

| 列表 | 文件 | 行数 | ①分页 | ②URL | ③排序 | ④选择 | ⑤视图 | ⑥三态 | ⑦刷新 | ⑧复用 | ⑨性能 | ⑩A11Y | ⑪响应 | ⑫key |
|---|---|---:|---|---|---|---|---|---|---|---|---|---|---|---|
| 任务管理 | `douyin.tsx` | 874 | ❌ limit:100 | ❌ | ❌ | ✅ Set | ✅ | ⚠️ 无骨架 | ❌ 3s 无终态 | ❌ 手写 | ⚠️ | ✅ 最佳 | ⚠️ | ✅ |
| 视频资源库 | `douyin-library.tsx` | 1627 | ✅ 服务端 | ⚠️ 只读 | ✅ 8种 | ❌ 数组+双份 | ✅ 手写LS | ❌ 无 isError | ❌ 无条件 5s | ❌ 三套视图 | ❌ 8处未memo | ⚠️ | ⚠️ | ✅ |
| 评论管理 | `douyin-comments.tsx` | 1206 | ⚠️ 无跳转 | ❌ | ❌ | ✅ Set | ✅ | 🔴 表头空态与加载态**合并** | ⚠️ | ❌ | ❌ | ⚠️ | ⚠️ 900px | ✅ |
| 达人列表 | `douyin-creators.tsx` | 1101 | 🔴 limit:200 | ❌ | ⚠️ 服务端无UI | ❌ 数组 | ✅ | 🔴 无重试 | ❌ 10s+15s 双轮询 | ❌ | ❌ | ⚠️ | ⚠️ | ✅ |
| 互动任务 | `douyin-interactions.tsx` | 753 | ❌ limit:100 | ❌ | ❌ | 🔴 **无选择列** | ✅ | 🔴 无 isError | ✅ 终态门控 | ❌ | 🔴 colSpan 6≠7 | ❌ 无 aria | ⚠️ | ✅ |
| 账号池 | `douyin-accounts.tsx` | 934 | ❌ limit:100 | ❌ | ❌ | ❌ `CreatePoolDialog` 数组 | ✅ | ⚠️ **两套三态** | ❌ 5s×2 | ❌ | ❌ 3处filter | ⚠️ | ✅ | ✅ |
| 请求日志 | `douyin-request-logs.tsx` | 606 | ✅ 服务端 | ❌ 草稿态 | ❌ | ❌ 无全选 | ✅ | ❌ 无 isError | ✅ **不轮询（正确）** | ❌ | ❌ taskMap | ❌ aria | ✅ | ✅ |
| 标签管理 | `douyin-tags.tsx` | 249 | ✅ 服务端 | ❌ | ✅ 4种 | ❌ 数组 | 🔴 **仅表格** | 🔴 无错误态 | ✅ 手动刷新 | ✅ **唯一复用 DataTable** | ❌ reduce×2 | ❌ aria | ⚠️ `overflow-hidden` | ✅ |
| 浏览器槽位 | `douyin-browsers.tsx` | 233 | ⚠️ 量少合理 | ❌ | ❌ | ❌ | 🔴 无视图切换 | ✅ 三态齐备 | ✅ 5s 合理 | ❌ | ❌ find | ✅ **最佳** | ✅ | ✅ |
| 赛道列表 | `douyin-tracks.tsx` | 1936 | 🔴 **全量拉取** | ⚠️ 仅 run | ❌ | ❌ | 🔴 **三视图操作不对等** | 🔴 无加载骨架 | ❌ 10s | ❌ | ❌ reduce | ⚠️ | ⚠️ | ✅ |
| 赛道详情 | `douyin-tracks_.$trackId.tsx` | 2205 | 🔴 关键词/达人全量 | ❌ | ❌ | ✅ Set（可见集） | ⚠️ 达人无选择 | ✅ 错误可重试 | ❌ 4 路轮询 | ❌ | ❌ 客户端 filter | ✅ **最佳** | ⚠️ | ✅ |
| 关键词 | `douyin-keywords.tsx` | 1245 | ✅ 服务端 | ❌ | ✅ | ❌ 数组 | ✅ | ⚠️ 合并 | ❌ 5s+10s | ❌ | ❌ 500 全量 | ❌ aria | ⚠️ `overflow-hidden` | ✅ |
| 用户管理 | `admin.tsx` | 73 | ❌ limit:100 | ❌ | ❌ | ❌ | ❌ | ✅ Suspense | ✅ | ✅ **用 DataTable** | ✅ | ⚠️ | ✅ | ✅ |
| items | `items.tsx` | 70 | ❌ limit:100 | ❌ | ❌ | ❌ | ❌ | ✅ 空态 | ✅ | ✅ **用 DataTable** | ✅ | ⚠️ | ✅ | ✅ |
| 通用表格 | `DataTable.tsx` | 194 | ⚠️ 仅客户端 | ❌ | 🔴 未启用 | 🔴 **无接口** | ❌ | 🔴 **无 loading** | ❌ | — | ⚠️ | ⚠️ | ❌ | ✅ |
| 任务结果 | `TaskResults.tsx` | 313 | ✅ 3 套独立页码 | ❌ | ❌ | ❌ | ❌ | ⚠️ | ✅ 终态停止 | ❌ | ✅ | ⚠️ | ⚠️ | ✅ |
| 作品面板 | `UnifiedWorksPanel.tsx` | **1479** | ✅ pageSize 20 | ❌ | ❌ | ❌ 数组 | ⚠️ Tab 未持久化 | 🔴 **空态仅处理 loading** | ❌ 非激活仍 5s | ❌ | 🔴 **每行 ~260 hook** | ❌ aria | ✅ | ✅ |
| 媒体任务 | `MediaTaskManagement.tsx` | 571 | ❌ limit:100 | ❌ | ❌ | ✅ `asset.id` | ✅ | ⚠️ | ❌ 3s | ❌ | ❌ counts | ⚠️ | ✅ | ✅ |
| 媒体管线 | `MediaPipelinePanel.tsx` | 487 | ❌ limit:100 | ❌ | ❌ | ✅ `asset.id` | ❌ | ❌ 无错误态 | ✅ **按状态门控（最佳）** | ❌ | ❌ | ✅ **最佳** | ✅ | ✅ |
| 任务互动 | `TaskInteractionsPanel.tsx` | 236 | ❌ limit:10 | ❌ | ❌ | ❌ | ❌ | ❌ 无错误态 | ✅ 终态 false | ❌ | ✅ | ✅ | ⚠️ | ✅ |

**列维度汇总**：⑨性能 **5/5 全部缺陷**（最普遍）｜⑫行 key **5/5 全部通过**（最健康）｜⑩A11Y 3✅/2❌｜⑪响应式 3缺陷/2✅

## 4.3 25 条架构通病（跨列表横向汇总，含完整命中清单）

- **[BUG·通病 1｜🔴 最高优先级] 分页缺失 / `limit` 硬编码 100**
  命中：`douyin.tsx:123-124`、`douyin-interactions.tsx:101`、`douyin-accounts.tsx:92`、`douyin-tracks.tsx`（无分页）、`douyin-tracks_.$trackId.tsx`（无分页）、`MediaTaskManagement.tsx:118-119`、`MediaPipelinePanel.tsx:45`、`TaskInteractionsPanel.tsx:42`；有分页 ✓：`UnifiedWorksPanel`（pageSize 20）、`douyin-browsers`（量少合理）。**超过 100 条的数据永久不可见。**

- **[BUG·通病 2] 筛选状态未同步 URL**
  命中：`douyin.tsx:109-112`、`douyin-interactions.tsx:79-82`、`douyin-comments`（无 `validateSearch`）、`douyin-library.tsx:127`（**单向：读 URL 不回写**）、`douyin-accounts.tsx:127-147`（单向）、`douyin-keywords`、`douyin-request-logs`。**刷新/分享/后退全部丢失筛选，无法深链。**

- **[BUG·通病 3] 搜索无防抖**
  命中：`douyin.tsx`、`douyin-interactions`、`douyin-library.tsx:243-253`、`douyin-tracks.tsx:27-28`、`douyin-creators.tsx:27-28`、`douyin-tags.tsx:28`、`MediaTaskManagement.tsx:205-211`、`UnifiedWorksPanel.tsx:376-385`、`douyin-request-logs`。**每次按键打一次请求。**

- **[优化·通病 4] 无排序**
  命中：`douyin.tsx`、`douyin-interactions`、`douyin-tracks`、`douyin-tracks_.$trackId`、`douyin-accounts`、`douyin-browsers`；**有服务端排序但前端无 UI**：`douyin-creators`、`douyin-request-logs`。

- **[结论·通病 5] 选择模型 O(n)**
  `Array<string>` + `includes` 命中：`douyin-accounts.tsx:807`、`douyin-tags.tsx:91`、`UnifiedWorksPanel.tsx:102`、`douyin-keywords`、`douyin-request-logs`；`Array` 存储 + `Set` 派生（半优化）：`douyin-library.tsx:279-280`。**大列表 toggle 卡顿。**

- **[结论·通病 6] 选择模型正确范例**（应推广为统一模式）
  `douyin.tsx:114`、`douyin-interactions`、`douyin-comments.tsx:132`、`douyin-tracks_.$trackId.tsx:104-106`、`VideoPendingIds`/`verifyPendingIds`、`MediaPipelinePanel.tsx`（`asset.id` key）。

- **[产品·通病 7] 全选语义不统一**
  「本页全选」：`douyin.tsx:170-179`、`douyin-library.tsx:490-492`、`UnifiedWorksPanel.tsx:231-237`、`douyin-comments.tsx:195-196`、`douyin-tracks_.$trackId.tsx:254-260`（**有 `aria-label` 说明**）；**完全无全选**：`douyin-interactions`、`douyin-tags`、`douyin-request-logs`、`douyin-accounts`。**用户可能误以为「全选 = 全量」。**

- **[产品·通病 8] 视图不持久化 / 不统一**
  统一用 `usePersistentViewMode` ✓：`douyin.tsx:113`、`douyin-comments.tsx:135`、`douyin-creators`、`douyin-accounts`、`douyin-tags`、`MediaTaskManagement.tsx:110-112`；**未持久化（本地 useState）**：`UnifiedWorksPanel.tsx:103,577-579`（Tabs）；**完全无视图切换**：`douyin-tracks`、`douyin-tracks_.$trackId`、`douyin-browsers`。

- **[BUG·通病 9｜🔴 严重] 三态（空/加载/错误）缺失或合并**
  **无错误态、无重试**：`douyin-comments.tsx:638-640`（表头空态与加载态**合并**）、`douyin-interactions`（无 `isError`）、`douyin-keywords.tsx:494-503`（合并）、`douyin-creators`（无 `isError`）；**空态缺失**：`douyin-accounts.tsx:474`（pools 分支）；**加载态非骨架屏**：`douyin.tsx`、`douyin-interactions`。
  **接口失败被静默吞成「无数据」文案——这是运维盲区，是本卷最严重的缺陷。**

- **[性能·通病 10] 轮询无终态停止 / 后台空转**
  命中：`douyin.tsx:127`（3s）、`douyin-interactions.tsx:103-108`（2s/5s）、`douyin-tracks.tsx:113`（10s）、`douyin-creators.tsx:122,133`（10s/15s）、`douyin-accounts.tsx:264`（5s）、`douyin-browsers.tsx:127`（3s）、`MediaTaskManagement.tsx:122`（3s）、`UnifiedWorksPanel.tsx:141,157`（2s/5s）；**普遍缺 `refetchIntervalInBackground: false`**。
  **正确范例**：`MediaPipelinePanel.tsx:46-62`（按数据状态门控，终态 `false`）、`TaskInteractionsPanel.tsx:43-48`。

- **[性能·通病 11] 派生计算未 memo**
  命中：`douyin-tracks.tsx:102,106`（`rows.reduce` 两次）、`MediaTaskManagement.tsx:125-140`（`counts` 未 memo）、`douyin-creators.tsx:102,106`、`douyin-comments`（`taskMap` 每次重建 Map）、`UnifiedWorksPanel.tsx:225-230,238-240,256-262`、`MediaPipelinePanel.tsx:103-108`、`douyin-browsers.tsx:36`（`slots.find`）、`douyin-accounts`（`selectedAssets`）。

- **[性能·通病 12｜🔴 严重] 行组件重 hook / 整页 loading 闪烁**
  `douyin-browsers.tsx:281` 的 `useEffect` 依赖 `slots`，**每次 3s 轮询触发整页加载态**。

- **[性能·通病 13] 无虚拟滚动**
  **全部列表均无。** pageSize 50/100 尚可接受，但**无上限保护**。

- **[交互·通病 14] `window.confirm` 代替组件级二次确认**
  命中：`douyin.tsx:851`（批量删除）、`douyin-interactions.tsx:851`、`douyin-tags.tsx:270`、`douyin-keywords`、`UnifiedWorksPanel.tsx:516-521`（批量补采）、`TaskInteractionsPanel.tsx:179-186`（重试）、`douyin-comments`（>1000 条导出）。**不可定制、不可 A11Y、不可测试。**

- **[风格·通病 15] 状态色硬编码**
  `MediaTaskManagement.tsx:65-99` **6 种状态各一份** Tailwind 色值（amber/sky/violet/blue/red/emerald + dark 变体）；各页 Badge 的 `variant` 选择（destructive/outline/secondary/default）也各自散落。**无统一状态色语义表。**

- **[风格·通病 16] `formatDate` 重复实现**
  至少 6 份：`douyin.tsx:1598-1610`（`formatUnix` + `formatDateTimeText`）、`douyin-comments.tsx:1150-1162`、`douyin-keywords.tsx:1238-1245`（带「从未」）、`douyin-creators`、`MediaTaskManagement.tsx:564-571`、`TaskInteractionsPanel.tsx:231-236`、`douyin-tracks`。**（本报告核对：实为 24 份，见 §2.3 C5）**

- **[架构·通病 17｜🔴 架构级] 手写 `Table`，未复用通用 `DataTable`**
  命中：`douyin.tsx:489-571`、`douyin-interactions`、`douyin-comments.tsx:589`、`douyin-keywords.tsx:506`、`douyin-tracks.tsx:430`、`douyin-tracks_.$trackId.tsx:447,616`、`douyin-accounts`、`douyin-browsers`、`UnifiedWorksPanel`、`MediaTaskManagement`、`MediaPipelinePanel`、`TaskInteractionsPanel`。
  **唯一复用 `DataTable` ✓**：`douyin-tags.tsx`（其能力已覆盖客户端分页/排序/搜索/行选择）。
  **每个列表都缺列排序、列可见性、行选择等基础能力。**

- **[架构·通病 18] 三视图（table/rows/cards）三套独立实现**
  `UnifiedWorksPanel`（`WorkRowItem`/`WorkCardItem`/内联 Table + `WorkQuickActions`）、`douyin-library`（`VideoTable`/`VideoCard`/`VideoRow`）、`douyin-keywords`（`KeywordPreview`）、`douyin-accounts`（`AccountPreview`）、`douyin-comments`（`CommentRow`/`CommentPreviewCard`）。**操作列逻辑三处重复维护。**

- **[风格·通病 19] 操作列拥挤**
  最多 5~6 个按钮：`UnifiedWorksPanel` 表格行（视频预览/字幕/下载/重试/抖音外链/进入任务）；`douyin-keywords` 行（任务/移动/启停/删除）；`MediaPipelinePanel` 行（预览/迁移/下载/重试/重译）。窄屏溢出依赖 `overflow-x-auto`。
  **正确做法**：`MediaTaskManagement` 用 `DropdownMenu` 收纳。

- **[A11Y·通病 20] `Checkbox` / 搜索框无 `aria-label`**
  行内 `Checkbox` 缺：`UnifiedWorksPanel.tsx:645-656`；搜索框缺：`douyin-comments`、`douyin-keywords`、`UnifiedWorksPanel`。
  **做得好**：`douyin-tracks_.$trackId.tsx`（451、482、528、537、602、674、683 行）、`MediaPipelinePanel.tsx:320-349`、`MediaTaskManagement.tsx:168-190`（`fieldset` + `legend`）、`douyin-browsers.tsx:214-220`。

- **[风格·通病 21] 响应式不统一**
  `overflow-x-auto` 正确：`douyin.tsx`、`douyin-comments.tsx:588-589`、`douyin-creators`、`douyin-tags`、`douyin-accounts`（`min-w-[900px]`）、`douyin-browsers.tsx:195-203`、`douyin-request-logs`、`UnifiedWorksPanel.tsx:606`、`MediaTaskManagement`；
  **非 `overflow-x-auto`（可能截断）**：`douyin-tracks`、`douyin-keywords`。

- **[结论·通病 22] ✅ key 稳定性普遍合格**
  `task.id`/`item.id`/`row.id`/`creator.id`/`keyword.id`/`asset.id`/`aweme.id`/`slotKey(slot)`/`source_task_id`；`douyin-library` 用 `slotKey(slot)`、`Admin` 用 `key={item.id}`。**判定：通过（这是全卷唯一整体健康的维度）。**

- **[数据·通病 23] 其它硬编码 `limit`**
  `UnifiedWorksPanel.tsx:150` `listTags({limit: 500})`；`douyin-creators.tsx:202-213` overview 额外拉 500 条；`douyin-library` `limit: 200`；`douyin-tracks_.$trackId` `listAccounts({limit:100})`。

- **[数据·通病 24] 重复查询 / 无效请求**
  `douyin-creators` 的 `overviewQuery` 与主列表 `metrics` **重复拉 500 条**；`douyin-accounts` 的 `refetchInterval: 5_000` 在无活跃任务时也轮询。

- **[数据·通病 25] `invalidate` 的 queryKey 不完整**
  `UnifiedWorksPanel.tsx:159-167` 漏 `douyin-works-tags`（**标签下拉计数不刷新**）；`douyin-browsers` 只有两个 key。

## 4.4 全局共性问题（架构级，7 条）

- **[BUG]** **错误态普遍缺失（最严重）** — 5 个列表中 **3 个**（互动/日志/标签）无 `isError` 分支，接口失败被吞为「空态文案」，形成**运维盲区**；只有账号页和浏览器页用了 `QueryErrorState`。
  **建议**：统一封装 `<ListState empty/error/loading>` 容器**强制**三态。
- **[架构]** **手写 Table 重复造轮子** — 35 处全部手写 `<Table>` + 内联列定义。**技术栈已声明 TanStack Table 却未使用**（`DataTable.tsx:41-46` 只注册了 `getCoreRowModel` + `getPaginationRowModel`）。
- **[BUG]** **分页两极化** — 日志/标签有服务端分页；互动/账号硬编码 `limit:100` 无翻页。**同一项目两套策略，100 条上限是隐性数据丢失。**
- **[BUG]** **筛选状态全未同步 URL** — 5 个页面**无一**使用 router `searchParams`。与 TanStack Router 的 URL-first 能力直接冲突。
- **[性能]** **派生计算未 memo** — 账号 3 次 `filter`（L46/L334）、日志 `taskMap` 重建、标签 2 次 `reduce`（L504）、互动 `rows.some`。**在轮询场景下被放大。**
- **[风格]** **`formatDate` 重复定义 4+ 处**（互动 L617 / 账号 L927 / 日志 L601 / 标签 L242）。
- **[BUG]** **轮询策略不一致** — 互动页有终态门控（**优秀**），账号页 `limit:500` 5s 不停止，`overview` 查询是双重查询（浪费），浏览器页 5s（合理但需服务端分页 + 概览统计接口）。

## 4.5 架构师结论（原文）

- **[结论]** **当前列表架构处于「每个列表各自为政」的作坊式状态**，缺乏统一的列表骨架抽象。
  **核心矛盾**：通用 `DataTable` 能力（仅客户端分页）**≪** 业务需求（服务端分页+排序+选择+多视图+筛选+URL 同步+三态+轮询）。
- **[结论]** 导致 **80% 列表被迫手写**，产生 **16 套重复的 Table 结构、3 份 Pager、4 份 `formatDate`、N 份空态/错误态**。
  **这不是单个 bug 问题，而是「缺少列表层抽象」的架构债务。**
- **[结论]** **根治方向**：步骤 1-2（扩展 `DataTable` + 提取 `ListShell`）是基建。做完后各业务列表从「手写 200 行」降为「**配置 30 行**」，重复代码自然消除，T2/T5/T9/T10 四项通病一并解决；步骤 4（`QueryClient defaultOptions`）**一行代码缓解最大面积轮询问题**。
- **[结论]** AI 主动提问（引出下一轮）：「需要我动手实现步骤 1-2（`ListShell` + 扩展 `DataTable`）的架构基建吗？这是提升最大、一劳永逸的方向。」

## 4.6 架构师改进路线图（11 步，含 ROI）

| 步 | 动作 | 类型 | 缓解通病 | ROI |
|---:|---|---|---|---|
| 1 | **扩展 `DataTable`** 为服务端列表骨架（`serverPagination`/`serverSorting`/`rowSelection`/`loading`/`error`） | 架构基建 | T5 | ⭐⭐⭐⭐⭐ |
| 2 | **提取 `ListShell`** 统一外壳（筛选栏 + ViewModeToggle + 三态 + Pager + 刷新），业务列表只传 `query`+`columns`+`filters` | 架构基建 | T2/T5/T9/T10 | ⭐⭐⭐⭐⭐ |
| 3 | **提取共享 `Pager`**（合并 3 份重复，支持页码跳转/首末页/每页大小） | 组件抽取 | T10 | ⭐⭐⭐⭐ |
| 4 | **`main.tsx` 加 `QueryClient defaultOptions`**（`staleTime: 30_000` + `refetchIntervalInBackground: false`） | **1 行改动** | T4 | ⭐⭐⭐⭐⭐ |
| 5 | **统一选择模型**：全部 `Array` → `Set<string>` + `.has()` | 批量小改 | T6 | ⭐⭐⭐⭐ |
| 6 | **统一 URL 同步**：筛选/分页/排序用 `validateSearch` + `useSearch` | 批量中改 | T3 | ⭐⭐⭐⭐ |
| 7 | **封装 `useSmartPolling`**（按业务终态自动停止 + visibility 暂停） | hook 抽取 | T4 | ⭐⭐⭐⭐ |
| 8 | **搜索防抖**：6 处加 `useDeferredValue` | 批量小改 | T7 | ⭐⭐⭐ |
| 9 | **派生计算 memo**：18 处加 `useMemo` | 批量小改 | T8 | ⭐⭐⭐ |
| 10 | **分页硬编码消除**：10 个 `limit:100` 列表接入服务端分页 | 批量中改 | T1 | ⭐⭐⭐⭐ |
| 11 | **`formatDate` 等工具函数抽取** `@/utils` | 小改 | T12 | ⭐⭐ |

## 4.7 ✅ 本卷的「正面样本」清单（应作为统一改造的参照实现）

> **AI 特意列出这些**，因为它们证明「正确做法在本项目里已经存在」，统一改造不是引入新范式，而是**把已有正确实现推广开**。

| 正面样本 | 位置 | 推广价值 |
|---|---|---|
| 终端停止 + 按状态门控轮询 | `MediaPipelinePanel.tsx:46-62`、`TaskInteractionsPanel.tsx:43-48`、`TaskResults.tsx:32-67`、`douyin-interactions.tsx:110-120` | **`useSmartPolling` 的参照实现** |
| `Set` 选择模型 | `douyin.tsx:114-116`、`douyin-comments.tsx:132`、`douyin-tracks_.$trackId.tsx:100-118`、`douyin-accounts.tsx`（`loginPendingIds`/`verifyPendingIds`） | **统一选择模型的参照** |
| 全选语义正确（基于可见集） | `douyin-tracks_.$trackId.tsx:254-260` | 通病 7 的正解 |
| 视图持久化标准实现 | `usePersistentViewMode`（`douyin.tsx:113`、`douyin-accounts.tsx:89` 等） | 通病 8 的正解 |
| 服务端分页正确实现 | `douyin-library.tsx:232-265`、`douyin-tags.tsx:49-61`、`douyin-request-logs.tsx:94-97,464-473` | **`DataTable` 服务端分页的参照** |
| 三态齐备 + 可重试 | `QueryErrorState.tsx`（46 行，`role="alert"`）、`douyin-browsers.tsx:59-65`、`douyin-tracks_.$trackId.tsx:195-237` | 通病 9 的正解 |
| 加载态用 Suspense | `admin.tsx:52-58`（`PendingUsers`） | 替代手写 `isLoading` |
| 路由级鉴权 | `admin.tsx:21-38`（`readUserMe` 判 `is_superuser` 否则 redirect） | 通病：`developer-tools` 缺此保护 |
| A11Y 最佳实践 | `douyin-tracks_.$trackId.tsx`（7 处 aria）、`MediaPipelinePanel.tsx:320-349`、`MediaTaskManagement.tsx:168-190`（fieldset+legend）、`douyin-browsers.tsx:78-132`（`nav aria-label` + `aria-pressed`）、`douyin-browsers.tsx` 在线状态点 `role="img"` + `aria-label` | 通病 20 的正解 |
| 批量后 invalidate 多组 key | `douyin-interactions.tsx:121-134`（4 组）、`douyin-tags.tsx:62-76`（4 组）、`douyin-tracks_.$trackId.tsx:138-151`（6 组） | 通病 25 的正解 |
| 筛选前置校验 | `douyin-comments.tsx`（`minLikes > maxLikes`、日期区间非法弹 toast） | 应推广 |
| 分页状态隔离 | `TaskResults.tsx:27-30`（三个 Tab 各自记忆页码）、`:265-300`（单页不显示分页条） | 应推广 |
| `placeholderData: previous` | `douyin-tags.tsx:60`、`douyin-library.tsx:232-265` | 避免翻页闪烁 |
| 日志类列表不轮询 | `douyin-request-logs.tsx:120-137`（仅手动刷新） | 通病 10 的正解 |
| 行 key 稳定 | 全卷 ✅ | — |

---

# 第五卷 · 架构方案

> 本卷是**建议**，不是发现。执行前需要决策。

## 5.1 前端拆分方案（A / B / C 三选一）

### 方案 A｜逻辑模块化（**AI 推荐先做**）

- 保持单 Vite 应用，把 `components/Douyin/` 按子域重组：
  - `tasks/` — `CreateTask` / `Resume` / `ExecutionProgress` / `Results` / `StatusBadge`
  - `media/` — `MediaPipeline` / `Migration` / `Subtitle` / `VideoPreview` / `ProcessMedia`
  - `interactions/` — `Composer` / `LiveMonitor` / `BatchComment` / `StatusBadge` / `ContentSummary`
  - `creators/` — `CreatorAvatar` / `UnifiedWorksPanel` / `AwemeActions`
  - `shared/` — `TrackSelect` / `SourceSelect` / `TaskIdentity` / `presentation` / `taskParameters`
- 配 `eslint-plugin-boundary` 禁止 `tasks/` 直接 import `media/` 等跨域依赖。
- 保留 TanStack 文件路由 + `autoCodeSplitting`。
- **成本最低，立刻可做、零构建改动。**

### 方案 B｜Monorepo 抽包（进阶）

用 bun/pnpm workspace 拆为：
- `packages/ui`（shadcn + Common，**0 业务依赖**）
- `packages/client`（OpenAPI SDK；后端改 schema → bump 版本 → 各应用升级）
- `packages/shared`（hooks + lib + Navigation + Sidebar）
- `apps/workbench`（路由 + 业务组件）

**收益**：`client` 独立版本化，与后端 OpenAPI 哈希契约形成「后端 → client 包 → 应用」明确链路；`ui` 可被未来第二个应用复用。
**成本**：构建配置、包间引用、生成脚本改造。

### 方案 C｜多应用/微前端（**不推荐**）

按域拆 `apps/auth`、`apps/douyin`、`apps/admin` 独立部署。**理由**：
- TanStack 文件路由强绑定单应用（`routeTree.gen.ts` 编译一棵树），拆应用要按域切路由目录；
- 跨应用跳转退化为 URL 跳转、**丢失 SPA 体验**；
- `autoCodeSplitting: true` 已解决按页懒加载，**「性能」不是动机**；
- 26 个路由、域间零耦合但共享同一布局/导航/认证，独立部署收益不抵成本；
- 仅当出现**多团队、独立发版**需求时才值得。

### AI 的推荐路径

① 先做**方案 A**（零风险、立刻改善可维护性）；
② 出现「需要第二个前端应用复用 UI/client」「希望 OpenAPI SDK 独立版本化」「团队 ≥2 组并行开发不同域」**任一信号**时升级到**方案 B**；
③ **方案 C 暂不考虑**。

**依据**：`components/ui/` 25 个 shadcn 组件为纯展示、无业务，天然可抽包边界；`client/`（OpenAPI SDK）是天然包边界。

## 5.2 多风格 / 多布局架构（Skin × Layout 正交）

### 设计目标

- **风格（Skin）**为一个自包含包 = 完整 token（明/暗两套）+ 装饰层（背景光晕、hero、卡片质感）+ 可选字体。可注册、可热切换。
- **布局（Layout）**为一个自包含壳组件 = 导航 + 头部 + 内容区。可注册、可热切换。
- **任意 Skin × 任意 Layout 正交组合。**
- 加新风格/新布局 = **加一个文件 + 注册一行**，不改核心（开闭原则）。
- 统一偏好中心 `AppearanceProvider` 管所有维度，一个设置面板切换，全部持久化 localStorage。

### 风格系统目录设计

- `src/styles/skins/{violet,ocean,graphite,neon,minimal}.css` — 每 skin 一个自包含文件，用 `[data-skin="xxx"]` 作用域，**既覆盖 token 也覆盖装饰**
- `src/styles/base.css` — 只留 `@theme inline` 映射 + base reset + 兜底 token
- `src/styles/registry.ts` — skin 元数据 `{ id, name, description, swatch }[]`

### 关键升级

**把 `data-preset` 统一改为 `data-skin`**，skin 语义从「配色」升级为「**完整视觉语言**」；`body` 装饰光晕（`index.css:199-216`）从 base 移除，由各 skin 自己定义，base 只给纯白兜底。

### 布局注册表接口契约

```ts
interface LayoutDef {
  id: string
  name: string
  icon: LucideIcon
  component: React.ComponentType<{ children: React.ReactNode }>
  minRole?: "superuser"
}
```
预置 4 项：`sidebar` / `horizontal` / `compact` / `focus`。

**`_layout.tsx` 从 148 行硬编码瘦身为约 15 行派发器**：
```tsx
const def = layouts.find(l => l.id === layout) ?? layouts[0]
const Shell = def.component
return <Shell><Outlet /></Shell>
```

### 偏好中心

把 `ThemeProvider` 升级为 `AppearanceProvider`：
```ts
interface Appearance { mode, resolvedMode, skin, density, layout, radius }
```
+ 各 setter，全部持久化 localStorage；`useEffect` 把 `data-mode`/`data-skin`/`data-density` 写到 `<html>`、`--radius` 写到 `:root`。

### 约束

**约束 1**：`data-skin` 取代 `data-preset` 需**一次性迁移**，`theme-provider.tsx` 的 `ThemePreset` 类型升级为 `SkinId`；迁移期可保留 `data-preset` 兼容读取旧 localStorage。

### 实施建议

**分两步**：
① 先做**布局注册表重构**（收益大、改动集中在 `_layout.tsx` + 新建 `layouts/`）；
② 再做 **skin 注册表**（迁移 `index.css` + 升级 provider）。

## 5.3 数据列表架构重构实施计划（阶段 0–4）

> 这是**可执行的工程计划**，与原第三卷「性能 10 步 ROI 路线图」内容重叠——**已在 §5.5 协调合并，不要分别执行**。

### 阶段 0：全局配置修复（1 处改动，立即见效）

- **[性能·0-1]** `src/main.tsx:39`：`QueryClient` 加 `defaultOptions`：
  ```ts
  { queries: { staleTime: 30_000, gcTime: 300_000, refetchIntervalInBackground: false, retry: 1 },
    mutations: { retry: 0 } }
  ```
  **1 行代码影响全部 query**，缓解 T4（12 处轮询不停止/后台轮询）+ 全局 refocus 风暴。
  ⚠️ **注意**：这正是 AI 曾实际动手改、随后被用户要求回退的那处改动（源 74403 行「你不用直接修改给出来计划就行了」）。

### 阶段 1：架构基建（新建 3 个文件 + 改写 1 个，不改现有列表）

**1-1 扩展通用 `DataTable`** — `src/components/Common/DataTable.tsx`（改写，**向后兼容**）

`DataTableProps<TData, TValue>` 新增（全可选）：
```ts
loading?: boolean                    // 显示骨架行
loadingRows?: number                 // 默认 5
error?: { title; description; onRetry?; retrying? } | null
emptyText?: string                   // 默认「暂无数据」
serverPagination?: {
  page: number          // 0-indexed
  pageSize: number
  total: number
  onPageChange: (page: number) => void
  onPageSizeChange?: (size: number) => void
  pageSizeOptions?: number[]
}
rowSelection?: {
  selected: Set<string>              // 统一用 Set
  getRowId: (row: TData) => string
  onToggle: (id: string) => void
  onToggleAll?: (ids: string[]) => void
}
scrollX?: boolean                    // 默认 true，包 overflow-x-auto
```
改动要点：
- 包裹 `<div className="overflow-x-auto rounded-xl border">`（修通病 21 响应式缺失）
- `loading` → 渲染 N 行 `<Skeleton>`；`error` → 复用 `QueryErrorState`（修通病 9，替代当前吞错误）
- `emptyText` 中文化，替代 `No results found.`；文案中文化（`Showing… entries` → 「第 X-Y 条，共 N 条」）（修通病 i18n）
- `serverPagination` → 渲染共享 `Pager`；`rowSelection` → 表头全选 Checkbox + 行 Checkbox，用 `Set.has()` **O(1)**
- **向后兼容**：不传新 props 时行为与当前一致（仅文案中文化 + 加 `overflow-x-auto`）→ **低风险**

**1-2 提取 `ListShell` 统一外壳** — `src/components/Common/ListShell.tsx`（新建）

```ts
interface ListShellProps {
  title: string
  description?: string
  actions?: React.ReactNode          // PageHero 右侧操作
  filters?: React.ReactNode          // 筛选栏内容
  viewMode?: { value: ListViewMode; onChange: (v: ListViewMode) => void }
  loading: boolean
  error: { title; description; onRetry; retrying } | null
  empty: boolean
  emptyText?: string
  onRefresh?: () => void
  refreshing?: boolean
  children: React.ReactNode          // 列表内容 table/cards/rows
  pager?: PagerProps                 // 透传给 Pager
}
```
职责：封装 `PageHero` + 筛选栏 + `ViewModeToggle` + 三态分发 + `Pager` + 刷新按钮；各业务列表**只传配置，不再各自拼装**。
渲染结构：`PageHero(title, description, actions=[Refresh, ...])` → `filters` → `Card` → `if error → QueryErrorState` / `else if loading → Skeleton` / `else if empty → EmptyState` / `else → children` → `pager`

**1-3 提取共享 `Pager`** — `src/components/Common/Pager.tsx`（新建）

```ts
interface PagerProps {
  page: number                       // 0-indexed
  pageSize: number
  total: number
  onPageChange: (page: number) => void
  pageSizeOptions?: number[]         // 默认 [20, 50, 100]
  onPageSizeChange?: (size: number) => void
  showJumper?: boolean               // 页码跳转输入
}
```
**替换 5 份重复 Pager**：`douyin-keywords.tsx:1197` / `TaskResults.tsx:265` / `UnifiedWorksPanel.tsx:1363` / `douyin-library.tsx:1556` / `douyin-comments.tsx:648`。

**1-4 提取智能轮询 hook** — `src/hooks/useSmartPolling.ts`（新建）

```ts
useSmartPolling<T>(
  queryKey: unknown[],
  queryFn: () => Promise<T>,
  options: {
    isActive: (data: T) => boolean
    activeInterval: number      // 默认 2000
    idleInterval?: number       // 默认 false（停止）
    enabled?: boolean
  }
): UseQueryResult<T>
```
**替换范围**：12 处手写 `refetchInterval` 终态判断，统一终态停止逻辑。

### 阶段 2：列表迁移（逐个接入新基建，可分批 PR）

> 按「**缺陷严重度 × 改动成本**」排序，分 4 批。

**批次 2a（高 ROI 小改）**
- **[架构]** `admin.tsx` — 已用 `DataTable` → 传 `serverPagination` + `loading`/`error`，移除 `limit:100` 硬编码
- **[架构]** `items.tsx` — 同 `admin`
- **[BUG]** `douyin-tags.tsx` — 搜索加 `useDeferredValue`；接 `ListShell` + `Pager`；**补错误态**；`overflow-hidden` → `overflow-x-auto`
- **[性能]** `douyin-request-logs.tsx` — `taskMap` 加 `useMemo`；**补错误态**；筛选状态上 URL

**批次 2b（核心业务列表）**
- **[架构]** `douyin.tsx` — 接 `ListShell`；筛选上 URL；补服务端排序；`limit:100` → 服务端分页
- **[BUG]** `douyin-library.tsx` — 筛选**双向** URL 同步（修「伪 `validateSearch`」）；`viewMode` 改 `usePersistentViewMode`；**补错误态**；`VideoTable` 操作列复用 `WorkActionButtons`；`selected` 改 `Set`
- **[BUG]** `douyin-comments.tsx` — **补错误态**；筛选上 URL；`CommentRow`/`CommentPreviewCard` 操作列合并
- **[BUG]** `douyin-creators.tsx` — `limit:200` → 服务端分页；`selected` 改 `Set` + **补全选**；`overviewQuery` 改后端聚合接口；补手动刷新

**批次 2c（运营管理列表）**
- **[架构]** `douyin-interactions.tsx` — `limit:100` → 分页；**补错误态**；筛选上 URL
- **[BUG]** `douyin-accounts.tsx` — `limit:100` → 分页；轮询加终态门控；`CreatePoolDialog` 的 `selected` 改 `Set`；**补 pools 空态** + 账号搜索
- **[BUG]** `douyin-tracks.tsx` — 全量拉取 → 服务端分页；`viewMode` 改 `usePersistentViewMode`；**补加载骨架**
- **[BUG]** `douyin-tracks_.$trackId.tsx` — 关键词/达人容器内全量滚动 → 服务端分页；轮询加终态停止

**批次 2d（组件内列表）**
- **[BUG]** `UnifiedWorksPanel.tsx` — `viewMode` 改 `usePersistentViewMode`；**补错误态**；`selected` 改 `Set`；**8 处派生计算加 `useMemo`**；行组件 `React.memo`
- **[BUG]** `TaskResults.tsx` — 补错误态；三 tab 提取泛型列表；`Pager` 换共享
- **[BUG]** `MediaTaskManagement.tsx` — `limit:100` → 分页；轮询加终态停止
- **[BUG]** `MediaPipelinePanel.tsx` — `limit:100` → 分页；**补错误态**；`failedAssets` 加 `useMemo`；**全局 `isPending` 改按行**
- **[BUG]** `TaskInteractionsPanel.tsx` — `limit:10` → 分页；补错误态；轮询终态改 `false`

### 阶段 3：性能批量修复（**不依赖架构基建，可与阶段 2 并行**）

- **[性能·3-1]** `UnifiedWorksPanel.tsx:646,794,829` + `douyin-creators.tsx:375` + `douyin-keywords.tsx:421` + `CreateTaskDialog.tsx:466`：`selected` 由 `Array` → `Set`（**5 处**）
- **[性能·3-2]** **18 处** render 体 `filter/map/reduce` 加 `useMemo`（依赖 `[rows, selected]`）
- **[性能·3-3]** `Sidebar/Main.tsx:33`：`useRouterState({ select: (s) => s.location.pathname })`
- **[性能·3-4]** **6 处**搜索 `onChange` 包 `useDeferredValue`
- **[性能·3-5]** `UnifiedWorksPanel.tsx:694-942` + `TaskResults.tsx:126`：行组件 `React.memo` + 稳定 props
- **[性能·3-6]** `douyin-request-logs.tsx:87`：`taskMap` 加 `useMemo`
- **[性能·3-7]** `vite.config.ts`：加 `build.rollupOptions.output.manualChunks` 拆 react/radix/tanstack（减小首屏 bundle）
- **[优化·3-8]** `package.json`：**移除 `react-icons` 死依赖**

### 阶段 4：P0 卡死修复（**需后端配合**）

- **[BUG·4-1]** `douyin-comments.tsx:987` — 导出改调后端流式接口下载 → 后端新增 `POST /comments/export` 返回 `StreamingResponse`
- **[BUG·4-2]** `douyin-library.tsx:420` — 字幕导出改后端流式 → 后端新增 `POST /works/export-subtitles`
- **[BUG·4-3]** `douyin-interactions.tsx:161` — 批量重试改后端批量接口 → 后端新增 `POST /interactions/batch-retry`
- **[性能·4-4]** `douyin-interactions.tsx:652` — 截图懒加载 `IntersectionObserver`（**无需后端**）

### 验证计划（质量门禁）

每批次 PR 前执行：
```
cd frontend && npx tsc --noEmit          # 类型检查
npx @biomejs/biome check src/            # lint
npm run build                            # 构建
npx playwright test                      # E2E（需后端）
```
阶段 1 额外验证：现有 `admin.tsx`/`items.tsx` **不传新 props 时行为不变**（回归测试）；`DataTable` 新 props 的三态/服务端分页/行选择单测。

### 工作量估算与依赖

| 阶段 | 改动文件 | 依赖 | 可并行 | 性质 |
|---|---|---|---|---|
| 0 | 1 | 无 | ✅ 立即 | 1 行 |
| 1 | 4（新建 3 + 改 1） | 无 | ✅ 与阶段 0 并行 | 基建 |
| 2a | 4 | 阶段 1 | ✅ | 迁移 |
| 2b | 4 | 阶段 1 | ✅ 与 2a 并行 | 迁移 |
| 2c | 4 | 阶段 1 | ✅ | 迁移 |
| 2d | 5 | 阶段 1 | ✅ | 迁移 |
| 3 | 约 15 | 无 | ✅ 与阶段 2 并行 | 性能 |
| 4 | 3 前端 + 3 后端 | 后端接口 | ❌ 需跨端协作 | P0 |

**建议执行顺序**：阶段 0 + 1（基建）→ 阶段 3（性能，独立）→ 阶段 2a-2d（迁移）→ 阶段 4（P0，最后）

## 5.4 统一品牌与命名（P3 的修法）

- **[i18n]** 抽一个 `@/lib/brand.ts` 暴露 `PRODUCT_NAME`，替换三处不一致：`login.tsx:44`/`settings.tsx:22`/`admin.tsx:34`/任务详情（「灵感采集台」）、`index.tsx:32`（「运营工作台」）、`AuthLayout.tsx:39`（「抖音内容运营工作室」）。

## 5.5 ⚠️ 两份路线图的协调（**本节是本次合并新增的**）

原文档里存在**两份彼此独立、内容重叠**的计划：

| | 第三卷 10 步 ROI 路线图（性能轮产出） | 第五卷 阶段 0–4（列表轮产出） |
|---|---|---|
| 来源 | 源 47660 区间（性能报告） | 源 74475 区间（列表收尾） |
| 粒度 | 10 步，按 ROI 排序 | 0/1/2/3/4 阶段 + 4 个批次，按依赖排序 |
| 覆盖 | 只覆盖性能 | 覆盖性能 + 架构 + 功能 |

**重叠项**：ROI 第 1 步 = 阶段 0；ROI 第 7 步 = 阶段 1-4；ROI 第 8 步 = 阶段 1-1/1-2/1-3；ROI 第 9 步 = 阶段 3-1/3-2/3-4；ROI 第 6 步 = 阶段 4。

**协调结论——按依赖关系合成一条执行路径：**

```
第 0 批（今天就能做完，5 行代码，零风险）
  ├─ main.tsx 加 QueryClient defaultOptions          [ROI-1 / 阶段 0]
  ├─ Sidebar/Main.tsx 加 select                       [ROI-2 / 阶段 3-3]
  ├─ PageShell.tsx PageHero 渲染 description          [§2.1 A1]
  ├─ settings.tsx slice(0,3) → slice(0,2)             [§2.1 A2]
  └─ douyin-library.tsx invalidate key 修正            [§2.1 A3]
        ↓
第 1 批（基建，可与第 0 批并行）
  └─ 阶段 1-1 扩 DataTable / 1-2 ListShell / 1-3 Pager / 1-4 useSmartPolling
        ↓
第 2 批（并行推进）
  ├─ 阶段 3 全部（性能，不依赖基建）
  ├─ 阶段 2a → 2b → 2c → 2d（迁移，依赖基建）
  └─ 布局注册表重构（§5.2，独立）
        ↓
第 3 批
  ├─ skin 注册表迁移（§5.2，依赖布局重构完成）
  └─ 阶段 4 P0（需后端配合，可提前与后端约定接口）
```

**关键判断（AI 在两份计划里都强调了）**：第 0 批虽然只有 5 行代码，但收益覆盖全站，**必须在任何重构之前完成**——否则后续所有验证都会被「原本就存在的问题」干扰。

## 5.6 ✅ AI 明确认定「无需改动」的架构项（防误改）

- ✅ **TanStack 文件路由 + `autoCodeSplitting: true`** — 已解决按页懒加载，**不需要为「性能」拆应用**（这是否决方案 C 的核心理由）。
- ✅ **`components/ui/` 25 个 shadcn 组件** — 纯展示、无业务，边界干净，**不需要改造**。
- ✅ **`client/`（OpenAPI SDK）+ 后端 OpenAPI 哈希契约测试** — 前后端契约已类型安全，**不需要手工同步类型**。
- ✅ **行 key 稳定性** — 12 维度中唯一全项通过的维度，**不需要改**。
- ✅ **`ViewModeToggle` / `usePersistentViewMode`** — 通用三视图组件已存在且实现正确，**问题在于「没有被全部页面采用」**，而非组件本身有问题。
- ✅ **`QueryErrorState.tsx`** — 错误态范本已存在（`role="alert"` + 重试），**问题在于「没被全部页面采用」**。
- ✅ **`data-density` 密度系统** — 已实现（`index.css` 4 条覆盖规则），A9「密度切换」只需把它接到列表上，**不需要新建**。

---
# 第六卷 · 产品与体验优化

> 本卷全部是**建议**，不是缺陷。原文明确标注：「跳出代码层面，从产品体验闭环、信息架构、用户旅程、数据洞察、效率能力等维度分析」。
> **排期方式与第二～四卷不同**：第二～四卷可以「照着修」，本卷需要先**决策做不做**。

## 6.1 列表功能增强：可添加的新功能（A1–A16，按价值排序）

| # | 功能 | 适用列表 | 实现要点 |
|---|---|---|---|
| **A1** | **列自定义/列可见性** — 隐藏/显示列、拖拽排序，窄屏自动隐藏低优先级列 | 所有表格视图 | `DataTable` 加 `columnVisibility` state + 表头设置 `DropdownMenu`；列定义加 `priority` 字段控制响应式隐藏顺序 |
| **A2** | **筛选条件 chips 可视化** — 已应用筛选显示为可点 × 移除的标签，一眼看清当前筛选 | 资源库/评论/关键词/互动/日志（多维筛选页） | `ListShell` 的 `filters` 区下方渲染 `FilterChip[]`，每个 chip 带 `onRemove` |
| **A3** | **批量操作工具栏** — 选中行后底部浮现操作栏（删除/导出/标签/状态变更），而非操作散在各处 | 任务/资源库/评论/达人/关键词/互动 | 选中数 >0 时渲染 `BulkActionBar`（fixed bottom），带「已选 N 项 · 清空」 |
| **A4** | **空状态引导** — 空态不只是「暂无数据」文案，要有插图 + 「创建第一个 XX」主按钮 | 全部列表 | `EmptyState` 组件（插图 + 标题 + 描述 + CTA，如「创建赛道」「同步标签」） |
| **A5** | **行展开内联详情** — 点击行展开内联子区域看详情，减少弹窗跳转 | 任务/互动/日志/评论 | TanStack Table `getExpandedRowModel` + 行 `RowExpander` |
| **A6** | **实时数据 diff 高亮** — 轮询返回新数据时，状态变化的行闪一下高亮（如 queued→running） | 任务/媒体任务/互动/账号 | query `onSuccess` 对比前后 data，变更行 id 写入 state，行组件加 `animate-highlight` class，2s 后清除 |
| **A7** | **行右键菜单** — 右键行弹出操作菜单（复制 ID/打开链接/重试/删除），操作列不再挤 | 任务/资源库/评论/互动 | `onContextMenu` + Radix `ContextMenu`；操作列只留 1-2 个高频操作 |
| **A8** | **操作列冻结** — 操作列固定右侧，横向滚动时不消失 | 资源库/评论/日志/互动（列多） | CSS `position: sticky; right: 0` + 背景色遮盖 |
| **A9** | **密度切换** — 紧凑/宽松行高切换，小屏用紧凑看更多行 | 所有表格 | **复用项目已有 `data-density` token**，列表 Card 间距/行 padding 随 density 变化，偏好持久化 |
| **A10** | **筛选预设/快捷视图** — 常用筛选组合保存为预设（如「仅失败」「仅今日」「待处理」），一键切换 | 任务/互动/评论/日志 | 预设存 localStorage，筛选栏左侧渲染预设标签按钮组 |
| **A11** | **列聚合/合计行** — 表格底部显示数值列合计/均值 | 日志（耗时均值）/任务（成功率）/标签（关联数合计） | `TableFooter` + 列定义加 `aggregate: 'sum'\|'avg'\|'count'` |
| **A12** | **内置导出（当前页/全部/选中）** — 列表右上角导出按钮，支持导出为 CSV | 任务/达人/评论/互动 | 前端拼当前页 CSV（小量）；全部走后端流式（**接阶段 4**） |
| **A13** | **键盘导航** — ↑↓ 选行、Enter 打开详情、Space 勾选、Esc 取消 | 所有列表 | `useRowNavigation` hook 管理 `activeRowIndex`，行 `aria-rowindex` + `onKeyDown` |
| **A14** | **刷新指示器** — 「上次更新 3 秒前」+ 自动刷新开关 + 手动刷新 | 所有轮询列表 | `ListShell` actions 区加 `<LastUpdated time={dataUpdatedAt} />` + 轮询 toggle |
| **A15** | **ID 列复制** — UUID 截断显示 + 悬停全文 + 点击复制 | 任务/作品/评论/互动 | `TaskIdentity` 已有短引用，扩展为可复制 `CopyableId` 组件 |
| **A16** | **相对时间** — 「3 分钟前」+ 悬停显示绝对时间 | 所有时间列 | 统一 `TimeAgo` 组件替代 4 份重复 `formatDate`，`title` 属性放绝对时间 |

## 6.2 列表功能优化：可优化的现有功能（O1–O20，按影响排序）

| # | 现状 → 优化 | 适用 |
|---|---|---|
| **O1** | 分页器仅上/下一页 → 加**页码跳转输入、首末页按钮、每页大小切换、总条数显示** | 5 份重复 Pager → 统一共享 Pager（阶段 1-3） |
| **O2** | 搜索无防抖，每次按键发请求 → `useDeferredValue` 300ms 防抖 + **搜索历史**（localStorage 最近 5 条） | 8 处搜索框 |
| **O3** | 筛选即时/draft 模式混用 → 统一为「**即时+防抖**」模式（赛道/状态等 Select 即时，文本搜索防抖），消除「改了 Select 还要再点查询」困惑 | 日志/评论/资源库 |
| **O4** | 筛选不持久化，刷新丢失 → 全部上 TanStack Router `validateSearch` + `useSearch`，**支持深链分享/书签/后退** | 12 个带筛选列表 |
| **O5** | 轮询固定间隔不停止 → `useSmartPolling`：终态返回 false 停止 + `refetchIntervalInBackground: false` + 页面不可见暂停 | 12 处轮询 |
| **O6** | `selected` 用 `Array`+`includes` O(n) → 统一 `Set<string>` + `.has()` O(1) | 5 处 |
| **O7** | 全选仅当前页 → 全选改为「**本页/全部**」两档：本页全选即时，全部全选弹确认（「将选中全部 N 条」） | 任务/资源库/评论/关键词 |
| **O8** | 加载态纯文案「正在加载…」 → **骨架屏**（`<Skeleton>` 行），保留表格结构，避免内容跳动 | 全部列表 |
| **O9** | 错误态缺失或文案误导 → 统一 `QueryErrorState`（已有范本），**明确区分「无数据」vs「加载失败」** | **13 个列表** |
| **O10** | 操作列按钮挤一行，窄屏溢出 → 高频操作（1-2 个）保留按钮，低频收纳 `DropdownMenu`；操作列 `sticky right` 冻结 | 资源库/评论/互动/账号 |
| **O11** | 视图切换持久化不统一 → 全部改 `usePersistentViewMode`，消除手写 localStorage / 无持久化 | 资源库/赛道/作品面板 |
| **O12** | 三视图功能不对等 → 统一操作列组件（如 `WorkActionButtons`），确保 cards/rows/table **三视图操作一致** | 资源库/赛道 |
| **O13** | 派生计算未 memo，轮询重算 → 18 处 `filter/map/reduce` 加 `useMemo`，`lastMediaActivity` 用**时间戳数字比较**替代 `new Date()` | 全部列表 |
| **O14** | 行组件每行挂载重 hook → `React.memo` + 稳定 props；或改为**单实例 + `activeRowId` 驱动** | UnifiedWorksPanel/TaskResults |
| **O15** | `window.confirm` 做危险确认 → 统一 `AlertDialog`，可自定义内容，可测试 | 10+ 处 |
| **O16** | 状态徽章颜色硬编码 → 走 CSS 变量 token，**响应 skin 切换** | MetricCard/StatusPill/CreatorAvatar/TaskExecutionProgress |
| **O17** | `formatDate` 4+ 份重复且格式不一 → 抽 `@/utils/time.ts` 统一 `formatDate/formatUnix/timeAgo` | 互动/账号/日志/标签/作品面板/任务结果 |
| **O18** | 全局 `isPending` 禁用所有行 → 追踪 `activeRowId`，**仅禁用当前操作行** | MediaPipelinePanel/账号 |
| **O19** | 大列表全量渲染 DOM → `@tanstack/react-virtual` 虚拟滚动，仅渲染可视区 + 上下缓冲 | 达人(200 行)/互动事件/监控步骤 |
| **O20** | 排序部分缺失 → 无排序的列表（任务/互动/账号/日志）补服务端排序，表头可点击 + 排序指示器 | 6 个列表 |

## 6.3 功能 × 列表优先级矩阵

| 列表 | 最该**添加** | 最该**优化** |
|---|---|---|
| 任务管理 | A6 diff 高亮、A10 快捷视图、A14 刷新指示 | O4 筛选上 URL、O20 补排序、O5 轮询停止 |
| 视频资源库 | A1 列自定义、A2 筛选 chips、A3 批量工具栏、A8 操作列冻结 | O4 URL **双向**同步、O9 错误态、O12 三视图对等 |
| 评论 | A5 行展开、A12 内置导出、A2 筛选 chips | O9 错误态、O4 筛选上 URL、O10 操作列收纳 |
| 达人 | A3 批量工具栏、A19 虚拟滚动、A10 快捷视图 | O6 `selected` 改 Set、O5 轮询停止、O20 补排序 |
| 互动任务 | A6 diff 高亮、A5 行展开、A7 右键菜单 | O9 错误态、O4 筛选上 URL、O10 操作列收纳 |
| 请求日志 | A11 列聚合（耗时均值）、A16 相对时间、A1 列自定义 | O9 错误态、O20 补排序（按耗时）、O2 搜索防抖 |
| 账号池 | A9 密度切换、A14 刷新指示 | O5 轮询终态门控、O18 行级 loading、O6 `selected` 改 Set |
| 关键词 | A2 筛选 chips、A10 快捷视图 | O9 错误态、O6 `selected` 改 Set、O2 搜索防抖 |
| 作品面板 | A6 diff 高亮、A1 列自定义、A8 操作列冻结 | O14 行组件 memo、O6 `selected` 改 Set、O13 派生计算 memo |

> **注**：矩阵中「A19 虚拟滚动」是原文笔误，实为 **O19**（优化项）。本报告保留原样并标注。

### 实施建议（原文）

- **[架构] 第一优先（基建赋能）** — **A1/A2/A3/A4/A8** 五个功能都依赖阶段 1 的 `ListShell` + 扩展 `DataTable` 基建；基建完成后这些功能成为「**配置项**」而非「每列表重写」。
- **[优化] 独立可做（不依赖基建）** — **A15**(复制 ID) / **A16**(相对时间) / **O2**(防抖) / **O6**(Set) / **O13**(memo) / **O15**(AlertDialog) / **O17**(统一 `formatDate`) 可**立即逐项推进**。
- **[产品] 产品价值最高** — **A2**(筛选 chips) + **A3**(批量工具栏) + **A6**(diff 高亮) + **A10**(快捷视图) 四项**对用户体感提升最大**：一眼看清筛选状态、批量操作触手可及、数据变化实时感知、常用视图一键切换。

## 6.4 产品架构师视角（IA/UJ/DV/EF/CF/FB/HP/MB）

### 一、信息架构与导航（产品骨架）

- **[产品·IA1]** 现状：20 个页面**平铺**在侧边栏/顶部导航，无分组。
  问题：新用户面对 20 个一级入口**认知过载**，不知道从哪开始。
  优化：按「**采集配置 → 数据采集 → 内容运营 → 资源管理 → 系统**」四组折叠分组，每组带图标 + 说明。
- **[产品·IA2]** 现状：**赛道（track）是核心组织维度但入口不突出**。
  问题：赛道详情是所有数据的归属枢纽，却在导航中与普通页平级。
  优化：赛道提升为**一级工作空间**概念，顶部赛道切换器（类似 Linear 的 team 切换），**全站数据按当前赛道过滤**。
- **[交互·IA3]** 现状：深层页面（任务详情/沉浸播放/赛道详情）**无面包屑**。
  问题：用户在 `$taskId/feed` 不知道属于哪个赛道/任务。
  优化：加 `Breadcrumb`：赛道 > 任务 > 沉浸播放，可点击逐级返回。
- **[交互·IA4]** 现状：无全局快速跳转。
  问题：20 个页面只能通过导航逐个点。
  优化：`Cmd+K` **Command Palette**：搜索页面/任务/赛道/达人，快捷跳转 + 最近访问。
- **[产品·IA5]** 现状：**品牌名三处不一致**（灵感采集台 / 运营工作台 / 抖音内容运营工作室）。
  问题：产品身份模糊。
  优化：统一产品名 + Logo，定义产品定位语。← 与 §2.8 P3 同源

### 二、用户旅程与任务流（体验闭环）

- **[产品·UJ1]** 现状：新用户登录后看到**空 dashboard，无引导**。
  问题：不知道「先建赛道→加关键词→建任务→登录账号→运行」的流程。
  优化：**首次引导 Checklist**：4 步引导卡（①创建赛道 ②添加关键词 ③登录抖音账号 ④创建首个任务），完成打勾，可跳过。→ **直接降低新用户流失**
- **[交互·UJ2]** 现状：创建任务是一个**大 Dialog（828 行）**，所有参数一屏堆。
  问题：新手面对「来源类型/关键词/达人/并发/间隔/评论数/子评论…」**20+ 参数迷失**。
  优化：改为**分步向导**：①选赛道 → ②选来源(搜索/详情/达人/点赞/收藏) → ③配置参数(分基础/高级折叠) → ④预览确认，每步有说明。
- **[交互·UJ3]** 现状：创建任务时**不知道有没有可用账号**。
  问题：没登录账号也能建任务，**运行时才报「无可用账号」**。
  优化：创建任务 Dialog 顶部显示**账号可用性状态**（「3 个账号可用」/ ⚠️「无可用账号，去登录」），**前置阻断**。
- **[交互·UJ4]** 现状：任务完成后**无下一步引导**。
  问题：任务 `succeeded` 后用户不知道去哪看数据。
  优化：任务完成态加**下一步快捷入口**：「查看作品数据 →」「进入沉浸播放 →」「批量互动 →」。→ **形成体验闭环**
- **[产品·UJ5]** 现状：媒体流水线（下载→字幕→翻译→迁移）**全手动逐步触发**。
  问题：用户要守着任务，每步完成手动点下一步。
  优化：**Pipeline 编排**——配置一次「下载完自动转字幕 → 自动翻译 → 自动迁移 MinIO」，任务跑完全自动。**当前 `MediaPipelinePanel` 只展示状态，不编排。**
  ｜原注：**（原文此处损坏，已尽力还原）** → 展开为 §6.5 的 PRD

### 三、数据洞察与可视化（产品价值呈现）

- **[产品·DV1]** 现状：首页 dashboard **仅 4 个数字卡片 + 任务列表**。
  问题：无趋势、无待办、无活动流，**首页价值低**。
  优化：重构 dashboard：①**采集趋势图**（近 7 天任务/作品/评论数折线）②**待办区**（失败任务/待登录账号/待确认互动）③**最近活动 timeline** ④**赛道健康度**概览卡片。→ **首页成为指挥中心**
- **[产品·DV2]** 现状：**无全局统计/报表**。
  问题：无法回答「总采集了多少、成功率多少、互动转化多少」。
  优化：新增**统计概览页**：总作品数/评论数/达人数、采集成功率、互动发送成功率、账号利用率，支持按赛道/时间筛选。
- **[产品·DV3]** 现状：任务进度是**文字 + 数字**。
  问题：缺少可视化阶段图，**不知道卡在哪一步**。
  优化：`TaskExecutionProgress` 改为**阶段流水图**（登录→采集→评论→媒体），每阶段带进度条 + 耗时 + 状态色，失败阶段高亮 + 错误详情。
- **[产品·DV4]** 现状：请求日志**只列表，无分析**。
  问题：排查问题靠翻日志，无错误率/慢请求统计。
  优化：日志页加**分析面板**：错误率趋势、Top 5 慢请求、Top 5 错误接口、状态码分布饼图。
- **[产品·DV5]** 现状：账号池**只显示状态枚举**。
  问题：不知道账号健康度、为何 cooldown、今日配额消耗。
  优化：账号卡片加**健康度评分**（成功率/响应时长/配额消耗环形图）+ cooldown 倒计时 + 最近错误。

### 四、效率能力（批量/模板/自动化）

- **[产品·EF1] 批量创建** — 现状：任务只能逐个创建。问题：对多个赛道/关键词要重复创建 N 次。
  优化：选多个赛道 + 共享配置 → 一次创建 N 个任务。
- **[产品·EF2] 任务模板** — 现状：任务配置不能复用。问题：每次都要重设并发/间隔/评论数等。
  优化：保存常用配置为模板（如「标准视频采集」「深度评论采集」），创建时一键套用。
- **[产品·EF3] 定时任务** — 现状：无定时/周期任务。问题：不能「每天凌晨自动采集新作品」。
  优化：cron 表达式配置周期采集，任务列表显示下次执行时间。
- **[产品·EF4] 任务链** — 现状：无任务依赖/编排。问题：不能「采集完自动转字幕再迁移」。
  优化：配置 A→B→C 依赖，A 成功自动触发 B（与 UJ5 pipeline 编排互补）。
  ｜原注：**（原文此处损坏，已尽力还原）**
- **[产品·EF5] 话术模板库** — 现状：互动内容（评论话术）每次手写。问题：不能管理话术库复用。
  优化：评论/私信话术存库 + **变量插槽（`{nickname}`）**，互动时选模板。
- **[产品·EF6] 导出中心** — 现状：无数据导出中心。问题：导出散在各列表（评论/字幕），**且前端全量加载卡死**。
  优化：统一**导出中心**：选择数据类型/范围/格式 → **后端异步生成** → 下载中心拉取。

### 五、配置与个性化

- **[产品·CF1] 偏好设置页** — 现状：主题/密度/布局切换**散在用户菜单**。问题：无统一偏好设置入口。
  优化：**偏好设置页**：外观（skin/密度/布局/圆角）+ 通知 + 默认筛选 + 语言，一处管理。
- **[产品·CF2] 通知配置** — 现状：无通知配置。问题：任务失败/完成**无主动通知**，靠用户刷新。
  优化：任务完成/失败时 → 浏览器通知 / 邮件 / webhook，按赛道/严重度配置。
- **[产品·CF3] 自定义 dashboard 布局** — 现状：首页卡片固定。问题：用户不能按关注点调整。
  优化：dashboard 卡片可拖拽排序 + 显隐，布局存 localStorage。
- **[产品·CF4] 筛选预设持久化** — 现状：筛选预设不能保存。问题：常用筛选每次重设（即列表卷 A10 的产品视角）。
  优化：**保存当前筛选为预设**，命名后入快捷栏，可设为默认。

### 六、反馈与通知

- **[产品·FB1] 通知中心** — 现状：toast 消息**稍纵即逝**。问题：错过 toast 就看不到操作结果。
  优化：**通知中心**：铃铛 icon + 未读角标，留存最近 50 条通知（任务完成/失败/账号掉线），可标记已读。→ **操作结果不丢失**
- **[产品·FB2] 浏览器通知** — 现状：长任务完成**无主动通知**。问题：用户要反复刷新看任务是否完成。
  优化：任务终态时触发**浏览器 Notification API**（已授权）+ 标签页标题角标 `(2)`。→ **离开也能感知**
- **[产品·FB3] 错误边界 + 上报** — 现状：前端错误**只进 console**。问题：用户遇到白屏/报错无法反馈。
  优化：捕获前端异常，展示友好错误页 + 「复制错误信息」+ 上报后端。

### 七、帮助与引导

- **[产品·HP1] 概念信息气泡** — 现状：复杂概念（赛道/来源类型/CDP/互动写回/脱敏）**无解释**。问题：新手不懂「赛道」是什么、CDP 浏览器 vs 本机浏览器区别。
  优化：关键概念加**信息提示气泡**（`?` icon hover 出解释），首次出现时自动弹出 tooltip。
- **[产品·HP2] 产品内嵌帮助面板** — 现状：**无用户使用文档**。问题：`developer-tools` 只有 API 文档，无操作指南。
  优化：按页面提供操作指引（「如何创建采集任务」步骤图文），`Cmd+?` 触发。
- **[产品·HP3] 空状态引导 CTA** — 现状：空状态纯文案「暂无数据」。问题：不告诉用户怎么创建。
  优化：空状态加**引导 CTA**（「创建第一个赛道」按钮 + 简要说明）。← 同 A4

### 八、多端与边界

- **[产品·MB1] 移动端适配** — 现状：整个应用为**桌面 1280px+ 设计**。问题：移动端表格溢出、Dialog 全屏、操作不可用。
  优化：列表改卡片堆叠、Dialog 改 bottom-sheet、导航改底部 tab bar。**优先适配「任务监控/账号状态」等需要移动查看的场景。**
- **[产品·MB2] PWA 离线壳** — 现状：无 PWA。问题：不能离线查看已采集数据。
  优化：缓存已加载列表数据，离线可浏览，在线自动同步。

### 九、产品架构师优先级（四象限）

| 象限 | 项目 | 理由 |
|---|---|---|
| **必做**（高影响 + 可行） | **UJ1** 首次引导 Checklist | 直接降低新用户流失，**无后端依赖** |
| **必做** | **IA1** 导航分组 | 20 入口一级平铺造成认知过载，**当前最突出的产品体验问题**（原注：原文此处损坏，已尽力还原） |
| **必做** | **DV1** dashboard 重构 | 首页是指挥中心，**当前价值极低** |
| **必做** | **UJ3** 创建任务前账号可用性阻断 | 减少失败任务，前置体验 |
| **必做** | **FB1** 通知中心 | 操作结果不丢失，基础体验 |
| **必做** | **HP1** 概念信息气泡 | 降低学习曲线，**低成本高回报** |
| **应做**（高影响 + 中等可行） | **UJ2** 创建任务分步向导 | **828 行 Dialog 是最大门槛** |
| **应做** | **UJ5 / EF4** Pipeline 编排 + 任务链 | **核心效率，从手动到自动化的质变** |
| **应做** | **EF2** 任务模板 | 减少重复配置 |
| **应做** | **DV2** 统计概览页 | 量化运营效果 |
| **应做** | **IA2** 赛道工作空间切换器 | 数据组织核心 |
| **应做** | **IA4** Cmd+K 快速跳转 | 专家效率 |
| **可做**（中影响 + 高可行） | **EF5** 话术模板库 | 互动效率 |
| **可做** | **CF1** 偏好设置页 | 个性化收口 |
| **可做** | **FB2** 浏览器通知 | 离开感知 |
| **可做** | **IA3** 面包屑 | 定位感 |
| **可做** | **DV5** 账号健康度 | 可观测性 |
| **远期**（高影响 + 低可行/需后端） | **EF3** 定时任务 | 需后端调度器 |
| **远期** | **EF6** 导出中心 | 需后端异步导出 |
| **远期** | **MB1** 移动端适配 | 工作量大 |
| **远期** | **DV4** 日志分析面板 | 需后端聚合接口 |

### 🔑 产品架构师核心结论（原文，最值得记住的一段）

> 当前产品**功能齐全但体验闭环不完整**：能采集、能运营，但用户**不知道从哪开始**（缺引导）、**不知道做得怎样**（缺洞察）、**不能自动化**（缺编排）、**离开就失联**（缺通知）。
>
> 产品架构师最该补的三件事：
> **① 首次引导闭环　② dashboard 洞察　③ Pipeline 自动化编排**
>
> 这三项让产品从「**能用**」升级为「**好用且离不开**」。

## 6.5 PRD：《媒体流水线自动编排（Pipeline Orchestration）》

> **导出损坏与截断说明**：该段原文混入 `-trackId）`、`5>`、`8>`、`Dy_V2`、`同时转9字幕`、`A处理A`、`�` 等垃圾 token，语义已尽力还原并就地标注「（原文此处损坏，已尽力还原）」；文件在 `interface PipelineTemplate { id; name; description?` 之后**中途截断**——**会话导出就此结束**。

### 1. 概述

- **[产品·1.1 背景]** 当前媒体处理流程是**纯手动逐步触发**：采集完成 → 用户手动创建下载任务 → 下载完手动点「处理媒体」（字幕提取/翻译）→ 处理完手动点「迁移到 MinIO」。用户必须守着任务，每步完成手动点下一步。`MediaPipelinePanel` **只展示状态，不编排**。（原文此处损坏，已尽力还原）
- **[产品·1.2 目标]** 让用户**配置一次 Pipeline 模板**，任务采集完成后**自动执行** 下载 → 字幕提取 → 翻译 → 迁移 全链路，无需人工逐步触发；支持按赛道绑定不同 Pipeline，支持条件跳过/失败重试/通知。
- **[产品·1.3 非目标]** 不改动采集任务本身的执行逻辑（**爬虫引擎不变**）。
- **[产品·1.3 非目标]** 不做跨任务依赖编排（A 任务触发 B 任务，属「任务链」另立 PRD）。
- **[产品·1.3 非目标]** 不做定时调度（属「定时任务」另立 PRD）。

### 2. 用户问题与场景

- **[产品·2.1 问题陈述（用户原声）]** 「我每天采集 5 个赛道的视频，每个赛道采集完都要手动点下载、等下载完手动再点转字幕、等字幕完再点迁移到云端。**一天要重复几十次，经常忘记某步导致数据没就绪。**」——运营人员（原文此处损坏，已尽力还原）
- **[产品·2.2 目标用户]** 运营人员：配置 Pipeline 后自动跑，**不再守任务**。
- **[产品·2.2 目标用户]** 管理员：为不同赛道配置不同 Pipeline 策略。
- **[产品·2.3 场景 S1 标准自动]** 采集完 → 自动下载 → 自动字幕 → 自动迁移 MinIO
- **[产品·2.3 场景 S2 按需字幕]** 采集完 → 自动下载 → **仅对 >30s 视频**提取字幕 → 迁移（原文此处损坏，已尽力还原）
- **[产品·2.3 场景 S3 多语翻译]** 采集完 → 下载 → 字幕 → **翻译英/日** → 迁移
- **[产品·2.3 场景 S4 仅下载]** 采集完 → 仅自动下载，不转字幕（快速预览场景）（原文此处损坏，已尽力还原）
- **[产品·2.3 场景 S5 手动介入]** 「Pipeline 自动下载，但字幕和迁移我手动」（半自动）（原文此处损坏，已尽力还原）

### 3. 功能需求（用户故事 + 验收标准）

**US1 创建 Pipeline 模板** — 作为运营人员，我希望保存一套媒体处理流程为模板，以便复用到多个赛道。
- 验收：可在「**设置 > Pipeline 模板**」页创建/编辑/删除模板
- 验收：模板含有序步骤列表，每步配置：**动作类型 / 条件 / 失败策略**
- 验收：模板可命名 + 描述，可设为某赛道默认（原文此处损坏，已尽力还原）

**US2 为赛道绑定 Pipeline** — 作为运营人员，我希望给赛道绑定一个 Pipeline 模板，该赛道下任务采集完自动执行。
- 验收：赛道详情页可选择绑定的 Pipeline 模板（或「无自动处理」）（原文此处损坏，已尽力还原）
- 验收：采集任务进入 `succeeded` 后，**自动创建对应的媒体任务链**，按模板步骤执行（原文此处损坏，已尽力还原）
- 验收：赛道列表显示「**已绑定 Pipeline: 标准自动**」标签

**US3 查看 Pipeline 执行进度** — 作为运营人员，我希望在任务详情看到 Pipeline 各步骤的实时进度和状态。
- 验收：任务详情「**媒体**」tab 展示 Pipeline 步骤流水图（下载→字幕→翻译→迁移）
- 验收：每步显示：状态(`pending`/`running`/`succeeded`/`failed`/`skipped`) + 进度 + 耗时 + 错误信息
- 验收：失败步骤可单独「**重试此步**」（原文此处损坏，已尽力还原）
- 验收：条件跳过的步骤标记 `skipped` **并显示跳过原因**

**US4 手动介入/暂停** — 作为运营人员，我希望能在某步暂停自动流程，手动处理后再继续。
- 验收：模板步骤可标记「**自动**」或「**手动确认**」
- 验收：标记「手动确认」的步骤执行到时暂停，等用户在 UI 点「确认执行」
- 验收：可整体「**暂停 Pipeline**」，当前步完成后不再自动触发下一步

**US5 失败处理策略** — 作为运营人员，我希望某步失败时按策略处理，而不是整个 Pipeline 静默卡住（原文此处损坏，已尽力还原）
- 验收：每步可配失败策略：`重试N次` / `跳过继续` / `终止Pipeline` / `通知用户`（原文此处损坏，已尽力还原）
- 验收：重试有**指数退避**，达上限后按策略降级
- 验收：失败通知进入**通知中心**（接 FB1）

### 4. 信息架构与交互设计

- **[交互·4.1 新增页面/入口]** Pipeline 模板管理：路由 `/settings/pipelines`，入口为设置页新 tab「Pipeline」
- **[交互·4.1]** 赛道绑定：位于赛道详情页，赛道详情头部「Pipeline: [选择器]」（原文此处损坏，已尽力还原）
- **[交互·4.1]** 执行进度：任务详情「媒体」tab，**现有 `MediaPipelinePanel` 升级**
- **[交互·4.2 模板编辑器线框]** 头部「Pipeline 模板：标准自动」+ 描述「采集完自动下载+字幕+迁移」；步骤卡片纵向排列并以 ↓ 连接；每张卡片含「动作 / `[自动▼|手动确认▼]` / 条件 / 失败策略」；底部 `[+ 添加步骤]  [保存模板]`
  - 步骤 1 — 动作：下载媒体 `[自动 ▼]`；条件：所有作品；失败策略：**重试 3 次 → 通知**
  - 步骤 2 — 动作：提取字幕 `[自动 ▼]`；条件：**时长 > 30 秒**；失败策略：跳过继续
  - 步骤 3 — 动作：翻译字幕 `[手动确认 ▼]`；目标语言：英语、日语
  - 步骤 4 — 动作：迁移存储 `[自动 ▼]`；目标：MinIO
- **[交互·4.3 任务详情执行视图线框]** 头部「Pipeline: 标准自动　状态: 运行中　⏸暂停」；步骤列表：

```
● 下载媒体   ✓ 成功 · 423 个        12s
● 提取字幕   ✓ 成功   387 个        45s   （36 个跳过：时长 < 30s）
● 翻译字幕   ⟳ 运行中  350/387     ──▓▓▓▓▓░░░ 72%
○ 迁移存储   ◌ 等待中
                                    底部：[重试失败] [跳过此步] [查看详情]
```

### 5. 数据模型 / 接口设计

```ts
type PipelineStepAction = "download" | "extract_subtitle" | "translate_subtitle" | "migrate_storage"
type StepExecution = "auto" | "manual_confirm"

interface PipelineStep {
  id: string
  action: PipelineStepAction
  execution: StepExecution
  condition?: {                       // ← 原文此处损坏，已尽力还原
    field: "duration_seconds" | "has_subtitle" | "storage_backend" | "all"
    operator: "gt" | "lt" | "eq" | "exists"
    value: number | string | boolean
  }
  params: {
    // download
    // extract_subtitle: { language?: string }
    // translate_subtitle: { target_languages: string[] }
    // migrate_storage: { target: "minio" | "default" }
    [key: string]: unknown
  }
  failure_strategy: {
    on_failure: "retry" | "skip" | "abort" | "notify"
    max_retries?: number
    retry_backoff_seconds?: number
  }
}

interface PipelineTemplate {
  id: string
  name: string
  description?: string
  // ⚠️ 原文到此截断 —— 会话导出结束
}
```

### ⚠️ 本 PRD 的完整性说明

- **原文在此截断**：文件在 `interface PipelineTemplate { id; name; description?` 之后**无任何后续内容**。因此该 PRD **缺少**：`PipelineTemplate` 的完整字段、`PipelineStep` 数组字段、执行实例模型（PipelineRun/PipelineStepRun）、API 端点定义、前端组件清单、样式走查、UI 走查（「六、样式与 UI 走查」章节缺失）、上线检查单、风险与依赖。
- **这是原文档的问题，不是提取遗漏**——9 段提取已覆盖到最后一行 75,392。

---

# 第七卷 · AI 明确判定「不是问题 / 无需改动」的清单

> **本卷存在的唯一目的：防止后续重构时把正确实现改坏。** 原会话中 AI 主动标注了大量「此处实现正确」「不需要改」的项，散落在各轮结论里。全部汇总于此。

## 7.1 前端技术栈与架构（不要动）

- ✅ **TanStack 文件路由 + `autoCodeSplitting: true`** — 已实现按路由自动代码分割。**「性能」不构成拆分应用的理由**（否决方案 C 的核心理由）。
- ✅ **`components/ui/` 25 个 shadcn 组件** — 纯展示、无业务依赖，边界干净，是天然的抽包边界，**不需要改造**。
- ✅ **`client/`（OpenAPI SDK）+ 后端 OpenAPI 哈希契约测试** — 前后端契约已类型安全，**不需要手工同步类型**。`schemas.gen.ts` / `types.gen.ts` / `sdk.gen.ts` 自动生成，改后端 schema 后跑 `generate-client` 即可。
- ✅ **`vite.config.ts` 的 `/api` 代理** — 配置正确。
- ✅ **`tsconfig.json` 的严格选项**（`strict`/`noUnusedLocals`/`noUnusedParameters`/`noFallthroughCasesInSwitch`）— 配置到位，**不要放松**。
- ✅ **`__root.tsx` 的 devtools 条件挂载**（`import.meta.env.DEV && VITE_ENABLE_DEVTOOLS === "true"`）— 实现正确。
- ✅ **`appearance` 的 4 维模型**（theme/preset/density/layout）— 设计合理，**问题只在「没有注册表」**，不是维度设计有问题。

## 7.2 性能（AI 明确说「实现正确」）

- ✅ **`createObjectURL` / `revokeObjectURL` 17 处配对全部正确** — revoke 时机无泄漏。命中：`douyin-library.tsx:448,455`、`VideoPreviewDialog.tsx`、`MediaPipelinePanel.tsx` 等。**不要「顺手」重构这部分。**
- ✅ **`douyin-accounts.tsx:83-88`** — `Set` state 用法**正确**（是「正确实现」的范例，其他文件应向它看齐，而不是改它）。
- ✅ **`douyin-request-logs.tsx:184-203`** — `inputMode="numeric"` + `isNaN` 检查**正确**（对比 H17 `BatchCommentDialog` 的 `Number("abc")` 未校验）。
- ✅ **`douyin-request-logs.tsx:120-137`** — 日志类列表**不轮询、仅手动刷新**，是**正确策略**（日志是只增不改的数据）。
- ✅ **`douyin_.$taskId.feed.tsx:85-92`** — wheel 滚动锁实现**合理**。
- ✅ **`douyin-tags.tsx:60`** — `placeholderData: previous` 使用**正确**（保留上一页避免闪烁）。
- ✅ **`douyin-browsers.tsx:5s 轮询`** — 槽位为实时监控场景，**5s 可接受**（对比 `douyin-accounts` 的 5s 在无活跃任务时也跑，那就是浪费）。
- ✅ **`MediaPipelinePanel.tsx:46-62`** — **按数据状态门控轮询、终态 `false`**，是 `useSmartPolling` 的**参照实现**。
- ✅ **`TaskInteractionsPanel.tsx:43-48`、`TaskResults.tsx:32-67`、`douyin-interactions.tsx:110-120`** — 终态停止/降频，**正确**。
- ✅ **`douyin.tsx` 刷新按钮与 `isFetching` 联动**（`animate-spin` + `disabled`）— 加载反馈**正确**。
- ✅ **`douyin.tsx:114-116`** — `Set<string>` 选择模型，**正确样本**。
- ✅ **`douyin-tracks_.$trackId.tsx:100-118`** — `Set` 选择 + **全选基于可见集**，**正确样本**。
- ✅ **`TaskResults.tsx:27-30, 265-300`** — 三个 Tab 各自记忆页码 + 单页不显示分页条，**正确样本**。
- ✅ **`douyin-tags.tsx:62-76`（4 组）、`douyin-interactions.tsx:121-134`（4 组）、`douyin-tracks_.$trackId.tsx:138-151`（6 组）** — 批量后 `invalidate` 多组 queryKey，**正确样本**。
- ✅ **`douyin-library.tsx:232-265`、`douyin-tags.tsx:49-61`、`douyin-request-logs.tsx:94-97,464-473`** — 服务端分页，**正确样本**。

## 7.3 可访问性（做得好，不要退步）

- ✅ **`douyin-tracks_.$trackId.tsx`** — 451、482、528、537、602、674、683 行共 **7 处 `aria-label`**，全站最佳。
- ✅ **`MediaPipelinePanel.tsx:320-349`**、**`MediaTaskManagement.tsx:168-190`**（`fieldset` + `legend`）、**`douyin-browsers.tsx:78-132`**（`nav aria-label` + `aria-pressed` + `role="img"` + `aria-label` 状态点）、**`douyin-browsers.tsx:214-220`**。
- ✅ **`douyin.tsx:367-382`** — 表头全选 `Checkbox` 带 `indeterminate` + `aria-label="全选可删除任务"`。
- ✅ **任务列表筛选整体**（源 3718-3805 行）— `<fieldset>` + `<legend className="sr-only">任务状态筛选</legend>` + `aria-pressed` + `<h2 id="task-list-heading" className="sr-only">` + `<section aria-labelledby>` + 4 处 `aria-label`。**全站最佳实践，应作为统一改造的模板。**

## 7.4 行 key 稳定性（12 维度中唯一全项通过）

- ✅ `task.id` / `item.id` / `row.id` / `creator.id` / `keyword.id` / `asset.id` / `aweme.id` / `slotKey(slot)` / `source_task_id` — **全部稳定，无索引 key。**
- ✅ `douyin-library.tsx:1407` 用 `aweme.id`、`douyin-tracks.tsx:271-317` 用 `track.id`、`Admin` 用 `key={item.id}`。

## 7.5 其它已正确实现的组件/模式

- ✅ **`QueryErrorState.tsx`**（46 行，`role="alert"` + `AlertTriangle` + 重试按钮 `retrying ? "正在重试…" : "重试"`）— **错误态范本已存在**，问题在于「**没被全部页面采用**」。
- ✅ **`ViewModeToggle.tsx` + `usePersistentViewMode`** — 通用三视图组件**已存在且正确**（`ListViewMode = "table" | "rows" | "cards"`、`fieldset` + `sr-only legend` + `aria-pressed`），问题在于「**没被全部页面采用**」。
- ✅ **`data-density` 密度系统** — 已在 `index.css` 实现（4 条 `[data-density="compact"]` 覆盖规则）。A9「密度切换」只需**接上**，不需要新建。
- ✅ **`admin.tsx:21-38`** — `beforeLoad` 中 `readUserMe` 判 `is_superuser` 否则 redirect，**路由级鉴权正确**（应推广到 `developer-tools.tsx`）。
- ✅ **`admin.tsx:52-58`** — 用 `Suspense` + `PendingUsers` 兜加载态，**优于手写 `isLoading`**。
- ✅ **`items.tsx:31-47`** — 空态显式处理，**正确**。
- ✅ **`douyin-accounts.tsx:125`** — `window.open(viewer_url)` 已正确带 `noopener,noreferrer`。
- ✅ **`douyin-browsers.tsx:204-211`** — iframe `key={selected.viewer_url}` 强制重建（切槽位时避免复用旧页面），**正确**。
- ✅ **`douyin-browsers.tsx:59-65`、`:121-130`** — 错误态用 `QueryErrorState` + 空态/加载态分支齐备，**正确**。
- ✅ **`douyin-request-logs.tsx:323-333`** — 空态/加载态 `colSpan` 与 7 列表头一致（对比 `douyin-interactions` 的 `colSpan={6}` ≠ 7 列**错位**）。
- ✅ **`douyin-comments.tsx` 筛选区** — `minLikes > maxLikes`、日期区间非法时弹 toast，**筛选前置校验正确**。
- ✅ **`douyin-library.tsx:113-150`** — `validateSearch` 完整解析 9 个参数，**URL 契约已具备，缺的只是「回写」**。
- ✅ **`douyin-library.tsx:92-111`** — `SortValue` 8 种排序 + 合法性校验，**正确**。
- ✅ **`TaskIdentity`** — 已有短引用实现，A15 是**扩展**而非重建。

## 7.6 关于「AI 没有提出的问题」

**原会话中 AI 自始至终没有修改过任何一行项目源码。** 唯一一次改动是给 `src/main.tsx` 加 `defaultOptions`，用户在源 74403 行说「**你不用直接修改给出来计划就行了**」，AI 随即**完整回退**（曾加的内容为 `defaultOptions: { queries: { staleTime: 30_000, gcTime: 5*60_000, refetchIntervalInBackground: false, retry: 1 }, mutations: { retry: 0 } }`），工作区恢复原状。

**结论**：本报告的全部内容都是**未落地的建议与判定**，项目当前代码与 AI 分析前完全一致。

---

# 第八卷 · 附录

## 8.1 用户与 AI 的完整对话轮次（16 轮）

| # | 文档行号 | 用户诉求 | AI 交付物 |
|---:|---:|---|---|
| 1 | 5 | 项目介绍请求 | 项目介绍、目录结构、架构决策（→ A 段） |
| 2 | 1090 | 「可以，先做拆分方案」 | 三种拆分方案 A/B/C 对比（→ §5.1） |
| 3 | 1924 | 「多风格多布局」 | Skin × Layout 正交架构设计（→ §5.2） |
| 4 | 2892 | 「按这个思路查缺陷」 | F1–F6 / I1–I8 / P1–P8（→ §2.8） |
| 5 | 6041 | 「继续 / 深入」 | 212 条缺陷审查，6 个 agent 并行（→ §2.4/2.5/2.6） |
| 6 | 15496 | 缺陷报告续 | C1–C11 横切 + H1–H22 + M1–M30 |
| 7 | 15913 | 「性能专项」 | 性能轮 1（→ C 段） |
| 8 | 47533 | 性能轮续 | 性能轮 2（→ D 段） |
| 9 | 47767 | 性能轮续 | 性能轮 3（→ E 段） |
| 10 | 47774 | 性能轮续 | 性能报告 P0/P1/P2/P3 + 根因 G1–G3 + 10 步 ROI（→ F 段） |
| 11 | 48059 | 性能轮续 | 性能交接 |
| 12 | 73659 | 「继续」（附 atomgit 链接 + **误写分支 main**） | 列表 12 维度审查后半（→ G/H 段） |
| 13 | 74403 | **「你不用直接修改给出来计划就行了」** | 回退 `main.tsx` 修改 + 实施计划阶段 0–4（→ §5.3） |
| 14 | 74723 | 列表收尾 | 架构师改进路线图 11 步 + 结论（→ §4.6） |
| 15 | 74881 | 「功能增强」 | A1–A16 / O1–O20 + 优先级矩阵（→ §6.1/6.2/6.3） |
| 16 | 75130 | 「产品优化」 | IA/UJ/DV/EF/CF/FB/HP/MB + 四象限 + PRD（→ §6.4/6.5） |

> **注**：第 1–11 轮中「继续」类无实质诉求的轮次，AI 都做了实际的增量分析，非空转。

## 8.2 文档损坏区清单（影响行号可靠性）

| 区间（源行） | 损坏形式 | 影响 |
|---|---|---|
| **62838–68000** | `> 💭 思考过程` 与正文**逐字交错**，句子被拆散（残片如 `douyin819-key行`、`-inter/actions`、`jiao`） | 列表专題中段部分行号**不可确证** |
| **68000–73350** | 同一物理行内**两段无关文本按列拼接**（如「请求日志列表」表格与「任务列表」表格交错） | 逐列表判定需交叉验证 |
| **75220 起（PRD 段）** | 混入 `-trackId）`、`5>`、`8>`、`Dy_V2`、`同时转9字幕`、`A处理A`、`�` 等垃圾 token | PRD 语义需还原 |
| **75220 末尾** | 在 `interface PipelineTemplate { id; name; description?` **中途截断** | PRD **不完整**，且会话导出就此结束 |

**受影响的「行号不确定项」**（提取文件已就地标注）：
- `douyin-creators` 的 pageSize / `refetchInterval` 行号
- `douyin-library` 的排序字段行号
- **`Pager` 组件的归属文件与行号**（`UnifiedWorksPanel.tsx:1363` / `douyin-library.tsx:1556` / `douyin-keywords.tsx:1197` 三组混排）
- `douyin-request-logs` 的部分维度行号
- `douyin-browsers` 的 `invalidate` queryKey

## 8.3 提取文件索引（9 段，2,401 行，1,740 条）

| 文件 | 行数 | 覆盖源行 | 条目 | 主题 |
|---|---:|---|---:|---|
| `A-项目介绍与架构设计.md` | 200 | 1–6041 | 127 | 仓库/分支事实、项目介绍、拆分方案、多主题设计、F/I/P 系列 |
| `B-缺陷审查212条.md` | 225 | 6041–15913 | 163 | 212 条审查、C1–C11、H1–H22、M1–M30、Top-12 快赢 |
| `C-性能专项-1.md` | 142 | 15913–25000 | 113 | 10 个反模式 + 12 个反模式清单 |
| `D-性能专项-2.md` | 211 | 25000–35000 | 162 | 逐文件渲染/网络/内存分析 + 正确实现反例 |
| `E-性能专项-3.md` | 160 | 35000–42000 | 118 | 34 个 `refetchInterval`、8 个 `staleTime`、17 个正确 revoke |
| `F-性能报告与交接.md` | 206 | 42000–47767 | 118 | P0/P1/P2/P3 + 根因 G1–G3 + 10 步 ROI + 8 个卡顿场景 |
| `G-列表缺陷-1.md` | 393 | 47767–58000 | 276 | 12 维度框架 + 逐列表判定（前 19 个） |
| `H-列表缺陷-2.md` | 412 | 58000–68000 | 315 | 逐页判定收尾 + 25 条通病 |
| `I-列表收尾与产品方案.md` | 537 | 68000–75392 | 367 | 实施计划阶段 0–4 + A1–A16/O1–O20 + 产品 8 视角 + PRD |

**合计：2,401 行 / 369,193 字节 / 1,740 条**

## 8.4 关键数字口径对照（AI 原文 vs 本报告核对）

| 指标 | AI 原文 | 本报告核对 | 差异说明 |
|---|---:|---:|---|
| `formatDate` 重复份数 | 4–6 处 | **24 处** | AI 按**文件**计数，实为按**函数定义**计数 |
| `refetchInterval` 总数 | 34 处（E 段实测） | 34 ✅ | E 段 grep 准确 |
| 需改的轮询点 | 12 处 | 34 处中约 **14 处高频 + 12 处需终态门控** | 口径不同：34 是全部，12 是「需改」 |
| `staleTime` 覆盖 | 8 处 | 8 ✅ | 与 34 之比 ≈ **24%** |
| `refetchIntervalInBackground` | 0 处 | **0** ✅ | — |
| 未 memo 派生计算 | 18 处 | 分文件统计 **40+ 处** | AI 按「值得改的」计数 |
| `React.memo` 使用 | 0 处 | **0** ✅ | 44,200 行中为 0 |
| `useMemo` / `useCallback` | 33 / 9 | 33 / 9 ✅ | — |
| 手写 `<Table>` | 「80% 列表」 | **35 处** | 20 个列表 + 组件内列表 |
| `DataTable` 消费者 | 2 个 | **2 个**（`admin` / `items`）✅ | — |
| 重复 `Pager` | 3 份 / 5 份 | **5 份** | 列表卷说 5 份，架构卷说 3 份，**取 5** |
| `createObjectURL` 配对 | 17 处正确 | **17 ✅** | — |
| 重建提行数（`_layout.tsx`） | 148 → 15 行 | 本地 **148 行** ✅ | — |
| `index.css` | 338 行 | **338 行** ✅ | — |

## 8.5 优先级总表（跨卷排序，**这是本报告的最终建议**）

### 🔴 P0 — 今天做（5 行代码，零风险，全站受益）

| # | 动作 | 文件:行 | 出处 |
|---:|---|---|---|
| 1 | `QueryClient` 加 `defaultOptions` | `src/main.tsx:39` | ROI-1 / 阶段 0 |
| 2 | `useRouterState` 加 `select` | `Sidebar/Main.tsx:33` | ROI-2 / 阶段 3-3 |
| 3 | `PageHero` 渲染 `description` | `Common/PageShell.tsx:53-95` | §2.1 A1 |
| 4 | `slice(0,3)` → `slice(0,2)` | `settings.tsx:31` | §2.1 A2 |
| 5 | 修正 `invalidate` 的 queryKey | `douyin-library.tsx:362` | §2.1 A3 |

### 🟠 P1 — 本周做（基建 + P0 卡死）

- 阶段 1：扩 `DataTable` / 抽 `ListShell` / 抽 `Pager` / 抽 `useSmartPolling`
- 阶段 4-4：`douyin-interactions.tsx:652` 截图懒加载（**无需后端**）
- 补错误态：`douyin-tags` / `douyin-comments` / `douyin-keywords` / `douyin-creators` / `douyin-interactions` / `UnifiedWorksPanel` / `MediaPipelinePanel` / `TaskInteractionsPanel`（**8 个列表，O9**）
- 安全：`useAuth.ts` 的 H4–H7 四条（token 存储 / invalidate / queryClient.clear / token 校验）
- `developer-tools.tsx` 补权限控制（H8）

### 🟡 P2 — 本月做（迁移 + 性能批量）

- 阶段 2a–2d 列表迁移（17 个文件）
- 阶段 3 性能批量（`Set` ×5、`useMemo` ×18、防抖 ×6、`React.memo` ×2、`manualChunks`、移除 `react-icons`）
- 阶段 4-1/4-2/4-3（**需后端新增 3 个流式接口**）
- `formatDate` 24 份 → 1 份（O17）

### 🟢 P3 — 需要决策后做（产品向）

- **必做 6 项**：UJ1 引导 Checklist、IA1 导航分组、DV1 dashboard 重构、UJ3 账号可用性阻断、FB1 通知中心、HP1 概念气泡
- **应做 6 项**：UJ2 分步向导、UJ5+EF4 Pipeline 编排、EF2 任务模板、DV2 统计概览、IA2 赛道切换器、IA4 Cmd+K
- **布局注册表重构**（§5.2 第一步）→ 然后 **skin 注册表迁移**（第二步）
- **前端拆分方案 A**（§5.1，零风险）

### ⚪ 不做（AI 明确否决）

- ❌ **方案 C 微前端拆分** — `autoCodeSplitting` 已解决懒加载，「性能」不是动机
- ❌ **改造 `components/ui/`** — 边界干净
- ❌ **重构 17 处 `createObjectURL` 配对** — 已正确
- ❌ **改造行 key** — 12 维度唯一全通过项
- ❌ **新建 `ViewModeToggle` / `QueryErrorState` / `data-density`** — 已存在，问题在采用率

---

## 结语

**本报告与 `docs/前端项目分析.md` 的关系**：
- 源文档是 **75,392 行会话记录**，其中约 60% 是 `💭 思考过程`、25% 是工具调用 JSON、仅约 800 行是真正的交付内容，且 **6 次重复注入**了 `# 项目开发约定` 上下文。
- 本报告是那 **800 行交付内容 + 分散在各轮的增量判定**的**完整归并**：9 段提取（1,740 条）→ 跨段去重 → 现状核对 → 发现与建议分离。
- **未做任何源码修改。** 所有「现状核对」结论均来自对本地仓库的直接读取。

**最值得记住的三句话（AI 自己的结论）**：

1. **架构层**：「当前列表架构处于『每个列表各自为政』的作坊式状态……**这不是单个 bug 问题，而是缺少列表层抽象的架构债务。**」
2. **性能层**：「三个根因（无缓存层契约 / 无轮询抽象 / 无列表抽象）解释了 **80%** 的性能问题。修具体点只能止血，修这三处才能**根治**。」
3. **产品层**：「用户**不知道从哪开始**（缺引导）、**不知道做得怎样**（缺洞察）、**不能自动化**（缺编排）、**离开就失联**（缺通知）……最该补的三件事：**① 首次引导闭环　② dashboard 洞察　③ Pipeline 自动化编排**——让产品从『能用』升级为『好用且离不开』。」
