# E-性能专项-3（源码行号区间 35000-42000）

> 来源：`docs/前端项目分析.md` 第 35000-42000 行。本区间为「性能与卡顿专项」中段：AI 逐文件读取 + grep 全局扫描，逐条编号性能问题（自编号 1-40），并在区间末尾（约 42314 行起）开始输出分级审查报告。
> 反模式类型命名（AI 自建分类）：未 memo 的派生计算 / selected 数组 includes O(n) / 每行挂载重组件 + useQuery 实例 / 内联函数作为 prop / 内联对象作为 prop / 每次 render 重建对象-数组-Map / useEffect 依赖不稳定 / 无 staleTime / 终态后仍轮询 / setTimeout 未清理 / 无上限并发。

## 一、轮询与网络（refetchInterval / 无 inBackground / 终态后仍轮询）

- **[性能]** `routes/_layout/douyin.tsx:127` — `refetchInterval: 3_000` 固定 3s 轮询，无 inBackground，终态后仍轮询；任务列表页即使用户没有任何进行中的任务也永远每 3s 请求一次，多标签页叠加放大 | 严重度高
- **[性能]** `MediaTaskManagement.tsx:122` — `refetchInterval: 3_000` 固定 3s 轮询，无 inBackground，终态后仍轮询 | 严重度高
- **[性能]** `routes/_layout/douyin-library.tsx:264` — worksQuery `refetchInterval: 5_000` 固定 5s 轮询，无 inBackground，终态后仍轮询 | 严重度高
- **[性能]** `routes/_layout/douyin-creators.tsx:122` — creatorsQuery `refetchInterval: 10_000` 固定 10s 轮询，无 inBackground
- **[性能]** `routes/_layout/douyin-creators.tsx:124-134` — overviewQuery 为计算指标额外拉取 500 条创作者（`limit: 500`），`refetchInterval: 15_000`，无 inBackground | 高开销
- **[性能]** `routes/_layout/douyin-accounts.tsx:94` — accountsQuery `refetchInterval: 5_000` 固定 5s 轮询，无 inBackground
- **[性能]** `routes/_layout/douyin-accounts.tsx:105` — slotsQuery `refetchInterval: 5_000` 固定 5s 轮询，无 inBackground
- **[性能]** `routes/_layout/douyin-browsers.tsx:25` — slotsQuery `refetchInterval: 5_000` 固定 5s 轮询，无 inBackground
- **[性能]** `routes/_layout/douyin-tracks.tsx:113` — `refetchInterval: 10_000`（无 inBackground）
- **[性能]** `routes/_layout/douyin-tracks_.$trackId.tsx:124 / :130 / :136` — 三个 query 各 `refetchInterval: 10_000`，无 inBackground
- **[性能]** `routes/_layout/douyin-tracks_.$trackId.tsx:924` — TrackTasksPanel `refetchInterval: 3_000`，固定 3s 轮询，无 inBackground
- **[性能]** `routes/_layout/douyin-keywords.tsx:144` — `refetchInterval: 5_000`（无 inBackground）
- **[性能]** `routes/_layout/douyin-keywords.tsx:154` — `refetchInterval: 10_000`（无 inBackground）
- **[性能]** `routes/_layout/index.tsx:41` — Dashboard tasks `refetchInterval: 5_000` 固定 5s 轮询，无 inBackground
- **[性能]** `components/Douyin/MediaPipelinePanel.tsx:67` — summaryQuery `refetchInterval: active ? 2_000 : 5_000`，终态后仍 5s 轮询，无 inBackground
- **[性能]** `components/Douyin/TaskExecutionProgress.tsx:68` — `refetchInterval: active ? 2_000 : 10_000`，终态后仍 10s 轮询
- **[性能]** `components/Douyin/UnifiedWorksPanel.tsx:141 / :157` — 两处 `refetchInterval: active ? 2_000 : 5_000`，终态后仍 5s 轮询
- **[性能]** `components/Douyin/InteractionLiveMonitor.tsx:68 / :74` — `open ? 1_000 : false` + `open ? 2_000 : false`，1s 与 2s 双高频轮询叠加，无 inBackground | 严重度高（打开监控时持续）
- **[性能]** `components/Douyin/InteractionComposerDialog.tsx:121` — quotas `refetchInterval: open ? 5_000 : false`
- **[性能]** 全局统计 `grep refetchInterval` — 前端共 **34 处** refetchInterval，绝大多数无 inBackground，绝大多数不判断终态 | 轮询风暴
- **[结论]** 全局 `refetchInterval` 分布清单（AI 逐一列出）：`index.tsx:41`(5s)、`douyin.tsx:127`(3s)、`douyin_.$taskId.tsx:84`(函数)/`:373`(2s)、`douyin-tracks_.$trackId.tsx:124/130/136`(10s)/`:924`(3s)、`douyin-tracks.tsx:113`(10s)、`douyin-library.tsx:264`(5s)、`douyin-keywords.tsx:144`(5s)/`:154`(10s)、`douyin-interactions.tsx:103/115`(函数)、`douyin-creators.tsx:122`(10s)/`:133`(15s)、`douyin-accounts.tsx:94/105`(5s)、`douyin-browsers.tsx:25`(5s)、`UnifiedWorksPanel.tsx:141/157`(2s/5s)、`:1214`(3s)、`TaskInteractionsPanel.tsx:43`(函数)、`TaskResults.tsx:42/54/66`(3s)、`TaskExecutionProgress.tsx:68`(2s/10s)、`MediaTaskManagement.tsx:122`(3s)、`MediaPipelinePanel.tsx:46`(函数)/`:67`(2s/5s)、`InteractionLiveMonitor.tsx:68`(1s)/`:74`(2s)、`InteractionComposerDialog.tsx:121`(5s)、`AwemeActions.tsx:98`(3s)
- **[性能]** `components/Douyin/UnifiedWorksPanel.tsx:154-158` 与 `TaskExecutionProgress.tsx:65-70` 与 `douyin-tracks_.$trackId.tsx` 存在重复 queryKey + 不同 refetchInterval（2s / 10s / 2s / 5s）| 重复请求与刷新冲突

## 二、`selected` 数组 `includes` O(n)（每行线性查找）

- **[性能]** `UnifiedWorksPanel.tsx:646 / :794 / :829` — 表格 / 横条 / 卡片三种视图每行 `selected.includes(aweme.aweme_id)`，`selected` 是数组，每行 O(n) 查找 | 严重度高（20 行 × 每 2s 轮询）
- **[性能]** `UnifiedWorksPanel.tsx:651` 附近（表格行渲染）— 同行重复出现 `selected.includes(aweme.aweme_id)` 模式 | 严重度高
- **[性能]** `douyin-creators.tsx:375` — `selected={selected.includes(creator.id)}`，每行 O(n) | 严重度高（200 行 × 每 10s 轮询；overviewQuery 15s）
- **[性能]** `CreateTaskDialog.tsx:219-220 / :265-266` — `(creatorsQuery.data?.data ?? []).filter(item => form.selectedCreatorIds.includes(item.id))` 两处，includes O(n)
- **[性能]** `CreateTaskDialog.tsx:456-479` — render 体里 `(creatorsQuery.data?.data ?? []).filter(...).filter(item => !item.is_placeholder).map(...)`，循环内 `form.selectedCreatorIds.includes(...)` 每行 O(n)
- **[性能]** `douyin-library.tsx:482-487` — `setSelectedAwemeIds` 内用 `current.includes(awemeId)` 数组查找 O(n)
- **[性能]** `douyin.tsx:339-341 / :351-353` — `onSelectedChange={(checked) => toggleTaskSelection(task.id, checked)}` 内联函数每行新建 | 低（TaskMobileCard / TaskCompactRow 未 memo 时影响有限）

## 三、未 memo 的派生计算（render 体内 filter/map/reduce/some）

- **[性能]** `UnifiedWorksPanel.tsx:224-262` — render 体内大量未 memo 派生计算：`pageIds = rows.map(...)`(224)、`allPageSelected = pageIds.every(id => selected.includes(id))`(225-226)、`selectedWorks = rows.filter(... selected.includes(...))`(227-229)、`selectedAssets = rows.filter(...).map(...)`(238-240)、`lastMediaActivity = rows.reduce(...)`(256-262)、`rows.some(...)` | 严重度高（每 2s 轮询触发，20 行 × selected 长度）
- **[性能]** `douyin-library.tsx:284-299` — 同一 render 体内 **8 次遍历**：`pageAwemeIds = rows.map`(284)、`selectedRows = rows.filter`(285-287)、`allPageSelected`(288-290)、`somePageSelected`(291)、`pageLocal = rows.filter(...).length`(292-294)、`pageMinio = rows.filter(...).length`(295-297)、`pageUndownloaded = rows.filter(...).length`(298)、`hasPlayableRows = rows.some`(299) | 严重度高（每 5s 轮询，32 行）
- **[性能]** `douyin-tracks_.$trackId.tsx:243-269` — render 体内：`filteredKeywords`(243-246)、`visibleKeywords`(247-253)、`visibleKeywordIds = visibleKeywords.map`(254)、`allVisibleKeywordsSelected`(255-257)、`someVisibleKeywordsSelected`(258-260)、`visibleIds = rows.map(...)`(268-269) | 严重度高（关键词列表可能很长，每 10s 轮询重算全部）
- **[性能]** `douyin-tracks_.$trackId.tsx:927-929` — `keywordNameById = new Map(keywords.map(...))` 在 render 体内每次渲染重建
- **[性能]** `douyin-tracks_.$trackId.tsx:931-946` — `filtered = tasks.filter(...)`（含 `.some(...toLocaleLowerCase...)`）在 render 体内
- **[性能]** `douyin-tracks_.$trackId.tsx:947-959` — `groupMap = new Map(); filtered.forEach(...)` 分组在 render 体内
- **[性能]** `douyin-tracks_.$trackId.tsx:402` — `keywords.filter(item => item.task_count === 0).length` 在 render 体内（按钮标签内），每次渲染重算 | 中（关键词列表可能很长，每 10s 轮询）
- **[性能]** `MediaTaskManagement.tsx:125-140` — `counts = tasks.reduce(...)`（active/ready/attention/downloaded/completed 五项）在 render 体内，query `refetchInterval: 3_000` | 中
- **[性能]** `MediaPipelinePanel.tsx:103-108` — `failedAssets = assets.filter(...).map(...)` 在 render 体内（未 memo），mediaQuery 2s/5s | 中
- **[性能]** `MediaPipelinePanel.tsx` 渲染侧 — summary 六个 SummaryItem 由 summaryQuery（2s/5s）驱动重算
- **[性能]** `douyin-accounts.tsx:188-191` — `ready` / `busy` / `availableSlots` 三个 filter 在 render 体内，accountsQuery 5s + slotsQuery 5s | 中
- **[性能]** `douyin-browsers.tsx:35-36` — `selected = slots.find(...)` 在 render 体内 | 低
- **[性能]** `douyin-request-logs.tsx:87-92` — `taskMap = new Map((tasks.data?.data ?? []).map(...))` 在 render 体里，每次渲染重建 Map | 中（200 个任务时每次渲染 new Map）
- **[性能]** `index.tsx:50-65` — `activeCount` / `attentionCount` / `total`(reduce) / `readyAccounts` 四个 filter/reduce 在 render 体内，tasks `refetchInterval: 5_000` | 中（每 5s 重算 4 次遍历）
- **[性能]** `index.tsx:66-67` — `dataScope = (tasks.data?.count ?? 0) > rows.length ? ... : ...` 在 render 体内
- **[性能]** `douyin-tags.tsx:102 / :106` — `rows.reduce(...)` 两次在 render 体内 | 低
- **[性能]** `TaskExecutionProgress.tsx:72-77` — `current` / `completed` / `skipped` / `errors` / `pending` 五个 find/filter 在 render 体内，summaryQuery 2s/10s | 低（stages 只有 4 个元素）
- **[性能]** `douyin-tracks.tsx:166-169` — `active` / `keywordCount` / `works` / `comments` 四个 reduce/filter 在 render 体内，tracksQuery 10s | 中（每 10s 重算 4 次遍历）
- **[性能]** `douyin-creators.tsx:94-96` — `selectedTrack = tracksQuery.data?.data.find(...)` 在 render 体内 | 低
- **[性能]** `douyin-keywords.tsx:169-171` — `pageIds = rows.map(...)`、`allPageSelected` 在 render 体内 | 中
- **[性能]** `douyin-comments.tsx:194-197` — `visibleIds = rows.map(...)`、`allVisibleSelected` 在 render 体内 | 中
- **[性能]** `douyin.tsx:130-134` — `attentionCount = tasks.filter(...).length` 在 render 体内，tasks 3s 轮询 | 中
- **[性能]** `douyin.tsx:156-159` — `selectableTasks = filteredTasks.filter(isDeletableTask)`、`allSelectableTasksSelected` 在 render 体内 | 中
- **[性能]** `douyin.tsx:735-743` — BulkResumeButton 的 `candidates = tasks.filter(...)` / `selectedResumable = candidates.filter(...)` 在 render 体内 | 中
- **[性能]** `CreateTaskDialog.tsx:539-552` — `accounts.filter(...["ready","busy"].includes(status)).map(...)`、`pools.filter(pool => pool.enabled && pool.accounts.length > 0).map(...)` 在 render 体内
- **[性能]** `CreateTaskDialog.tsx:108-109` — `.map(item => item.trim()).filter(Boolean)` 在 render 体内
- **[性能]** `InteractionComposerDialog.tsx:128` — `accounts.data?.data.filter(account => account.enabled) ?? []` 在 render 体内（未 useMemo）
- **[性能]** `BatchCommentDialog.tsx:90-97` — 两组 `options` 的 `.filter(item => item.enabled).map(...)` 在 render 体内
- **[性能]** `douyin-library.tsx:366-373` — `migrationFilters` 对象字面量在 render 体内每次渲染新引用（仅作为 mutationFn 闭包，不是 prop）| 影响不大

## 四、每行挂载重组件 / useQuery 实例（列表性能核心问题）

- **[性能]** `UnifiedWorksPanel.tsx:694 / :912` — 表格 / 横条 / 卡片三种视图每一行都挂载 `<WorkQuickActions>`（内含 `AwemeActions`：1 个 useQuery + 2 个 useMutation + useCustomToast + useNavigate + 2 个 `InteractionComposerDialog`，每个 Dialog 含 4 个 useQuery）、`<CommentsDialog>`、`<VideoPreviewDialog>`（含 useEffect + useState）| 严重度高（一页 20 行 × 约 13 个 hook 实例 ≈ **260 个 observer/状态实例**，每 2s 轮询重渲染）
- **[性能]** `TaskResults.tsx:126-130` — 每行挂载 `<AwemeActions>`，内含 useQuery（`enabled: commentsOpen`）+ useMutation + useCustomToast + useNavigate + 2 个 InteractionComposerDialog | 严重度高（20 行 × 每行大量 hook 实例）
- **[性能]** `UnifiedWorksPanel.tsx:725 / :937` — 每行挂载 `<CommentsDialog count={row.persisted_comment_count} />`，其内部 useQuery 在 `:1202`（`enabled: open`）| 中（enabled 限制 fetch，但 20 个 useQuery observer 仍创建/注册/注销）
- **[性能]** `AwemeActions.tsx:260-266` — 评论列表每行挂载 `<InteractionComposerDialog>`（内含 4 个 useQuery）| 中（10 条评论 × 4 useQuery = **40 个 observer**）
- **[性能]** `MediaPipelinePanel.tsx:204-215` — `assets.map` 每行 `MediaRow`，行内含 `PipelineStatus` / `MigrationStatusView` / `VideoPreviewDialog` / `MediaMigrationDialog`（mediaQuery 2s/5s 轮询驱动全部重渲染）
- **[性能]** `TrackSelect.tsx:59-74` — 每个 `TrackSelect` 实例都创建 `useTrackCatalog` useQuery observer（有 `staleTime: 30_000`，但无 staleTime 覆盖的重复挂载仍注册 observer）
- **[性能]** `TaskResults.tsx:32-67` — 三个 query 全部用 `enabled: tab === ...` + `placeholderData`，未激活 tab 不 fetch（正面，但 observer 仍存在）

## 五、串行 await / 全量加载 / 无上限并发（网络风暴 + 主线程阻塞）

- **[性能]** `douyin-library.tsx:329-365` — `recrawlComments` 使用 `for...of` + `await` **串行循环**，且循环内可能调用 `DouyinService.getTask`(line 337)（有 taskCache，首次仍会请求）| N 个视频 = N 次顺序请求（+ 可能的 getTask 请求）
- **[性能]** `UnifiedWorksPanel.tsx:196-213` — `recrawlComments` 同类串行 `for...of` + `await` 循环 | 与 douyin-library.tsx:333-355 重复问题
- **[性能]** `douyin-library.tsx:390-462`（`exportSubtitles`）— `while (works.length < total)`(420) 分页拉取**全部作品到前端**，再用 `blocks.join`(433-447) 拼接巨大字符串 | 命中几万条带字幕作品时 = 网络风暴 + 主线程阻塞；虽有 `total > 1000` 的 `window.confirm` 提示（411-418），确认后仍全量拉取
- **[性能]** `douyin-comments.tsx:987-994` — `while (items.length < total)` 全量分页加载后前端拼装导出（exportTxt）| 严重（全量加载）
- **[性能]** `douyin-interactions.tsx:161-171` — `do { ... } while (skip < total)` 全量分页拉取
- **[性能]** `douyin-interactions.tsx:178-185` — `Promise.allSettled(...)` **无上限并发**（retryAll）| 严重
- **[优化]** 全局统计 `grep "Promise.allSettled|for(...of) await|do{|while("` — 共 **6 处**匹配：`douyin-library.tsx:420`(while)、`douyin-interactions.tsx:161/171/178`(do-while + Promise.allSettled)、`douyin-comments.tsx:987`(while)、`VideoPreviewDialog.tsx:316`(while，仅单位换算，无害)
- **[优化]** 正面样本：`douyin.tsx:748` `bulkResumeTasks` 走后端批量 API（非前端串行循环）；`douyin.tsx:735-743` 已是后端串行执行 —— 属"好的设计"
- **[性能]** `douyin-library.tsx:337` — 串行循环内 `DouyinService.getTask({ taskId: sourceTaskId })` 首次必发请求

## 六、搜索无防抖 / queryKey 与请求放大

- **[性能]** `douyin-creators.tsx:110` + `:288` — `creatorsQuery` 的 `queryKey: ["douyin-creators", trackId, search, status, enabled, sort]` **包含 `search`**，而 `search` 由 `onChange` 直接 `setSearch` | 每次按键都触发一次网络请求，**无防抖**
- **[优化]** `douyin.tsx:278` — `searchTerm` 由 `onChange` 直接 set，但**不进 queryKey**（`["douyin-tasks", trackId, sourceValue]`），搜索是前端 `filter`（`135-155` useMemo）| 无网络请求问题（OK）
- **[优化]** `MediaTaskManagement.tsx:208` — `search` 直接 set，但**不进 queryKey**（`["douyin-media-tasks", trackId]`），前端 filter | OK，无网络请求问题
- **[性能]** `douyin-request-logs.tsx:85` — tasks 查询 `limit: 200` 且**无 staleTime**，每次挂载重新请求 200 条任务
- **[性能]** `douyin-request-logs.tsx:94-97` — logs 查询无 staleTime（分页由前端 `skip` 控制）
- **[性能]** `douyin_.$taskId.feed.tsx:37-46` — works 查询 `limit: 100`，无 refetchInterval、**无 staleTime**（一次性拉取 100 条）
- **[性能]** `TaskSelect.tsx:25`(`useTrackCatalog`) — `limit: 200` 赛道；有 `staleTime: 30_000`
- **[性能]** `CreateTaskDialog.tsx:135 / :140 / :145` — 弹窗内 3 个 useQuery（`accountsQuery` / `poolsQuery` / `creatorsQuery`）
- **[性能]** `BatchCommentDialog.tsx:78 / :83` — 弹窗内 2 个 useQuery（accounts / pools）且无 staleTime
- **[性能]** `InteractionComposerDialog.tsx:100 / :105 / :111 / :117` — 弹窗内 **4 个 useQuery**（accounts / task / track / quotas）
- **[性能]** `douyin_.$taskId.tsx:465` — `const refreshTimer = window.setInterval(load, 15_000)` 独立 15s 轮询定时器（与 react-query 轮询叠加）
- **[性能]** `douyin_.$taskId.tsx:84` — `refetchInterval: (query) => ...` 函数式（AI 判定可接受）
- **[优化]** `douyin-interactions.tsx:103-106` — `refetchInterval` 回调内 `query.state.data?.data.some(item => ["queued","running"].includes(item.status))` 属 TanStack Query 标准用法，100 行 some 可接受
- **[优化]** `MediaPipelinePanel.tsx:46-62` — `refetchInterval` 用函数判断是否存在处理中资产（queued/downloading/subtitle pending-running/migration queued-uploading-verifying-switching-cleanup_pending），有则 2_000，无则 `false` | **好的设计**（终态停止），但无 inBackground

## 七、staleTime 缺失（全局）

- **[性能]** `main.tsx:39` — QueryClient **全局无 `defaultOptions`**（无全局 staleTime）| 导致大量 useQuery 无 staleTime，每次挂载都重新请求
- **[结论]** 全局统计 `grep staleTime` — 仅 **8 处**设置 `staleTime: 30_000`：`douyin-library.tsx:209 / :218 / :230`、`douyin-comments.tsx:154`、`developer-tools.tsx:70`、`UnifiedWorksPanel.tsx:152`、`TrackSelect.tsx:28`、`SourceSelect.tsx:27` | 其余绝大多数查询无 staleTime
- **[性能]** `douyin-request-logs.tsx:85 / :94` — 均无 staleTime（重复条目，见第六节）

## 八、useEffect 依赖 / useRouterState 订阅

- **[性能]** `douyin-browsers.tsx:28-34` — `useEffect(..., [selectedName, slots])`，`slots` 每次轮询都是新引用（slotsQuery 5s）| useEffect 每 5s 执行一次（effect 本身轻量）| 中
- **[性能]** `Sidebar/Main.tsx:33` — `useRouterState()` **无 `select`**，全量订阅整个 router state | 严重度高（侧边栏常驻，任何路由变化包括 search param 变化都会触发整个侧边栏重渲染）
- **[优化]** `douyin_.$taskId.tsx:75-77` — `useRouterState({ select: ... })` 带 select | 正确用法，不是问题
- **[优化]** `UnifiedWorksPanel.tsx:154-156` — `useRouterState({ select: ... })` 带 select | 正确用法，不是问题
- **[优化]** `douyin-library.tsx:154-156` — `useRouterState({ select: state => state.location.pathname.endsWith("/feed") })` 带 select | 正确用法
- **[优化]** `InteractionComposerDialog.tsx:124` — `new Map((quotas.data ?? []).map(...))` 放在 `useMemo` 里 | 正确用法
- **[优化]** `MediaTaskManagement.tsx:141-161` — `filtered` 用 `useMemo`（依赖 `[filter, search, tasks]`）| 正面
- **[优化]** `douyin-creators.tsx:137-147` — `metrics` 用 `useMemo`（依赖 `[allRows, overviewQuery.data?.count]`）| 正面
- **[优化]** `douyin-library.tsx:266-269 / :270-278 / :280-283` — `taskMap`、`rows`（去重）、`selectedAwemeSet` 均 useMemo | 正面
- **[优化]** `douyin_.$taskId.feed.tsx:47-53` — `rows` 用 useMemo 过滤 `media?.download_available` | 正面
- **[优化]** `TrackSelect.tsx:65-74` — `useEffect` 自动选择默认赛道，依赖含 `onValueChange`（调用方内联函数时可能反复触发，AI 未列为问题）
- **[性能]** `CreateTaskDialog.tsx:156` — 存在 useEffect（AI 标注待确认，未展开）

## 九、主线程 / 内存 / ObjectURL / setTimeout 未清理

- **[性能]** `useCopyToClipboard.ts:21` — `setTimeout(() => setCopiedText(null), 2000)` **未清理** | 内存泄漏风险（组件卸载后仍 setState）
- **[性能]** `developer-tools.tsx:511` — `window.setTimeout(() => setCopied(false), 1500)` 未清理 | 影响很小（只是清空 copied 状态）
- **[性能]** `douyin-library.feed.tsx:136` — `window.setTimeout(...)` 未清理（上下文待确认）
- **[性能]** `douyin_.$taskId.feed.tsx:89-91` — `window.setTimeout(() => { wheelLocked.current = false }, 450)` 未清理，但只是 ref（非 state），组件卸载后无副作用 | 影响很小
- **[结论]** 全局统计 `grep "setInterval|setTimeout"` — 共 **5 处**：`douyin_.$taskId.feed.tsx:89`、`douyin_.$taskId.tsx:465`(setInterval)、`douyin-library.feed.tsx:136`、`developer-tools.tsx:511`、`useCopyToClipboard.ts:21`
- **[优化]** 全局统计 `grep "createObjectURL|revokeObjectURL"` — 共 **17 处**，AI 逐一确认 revoke 时机**均正确**（`anchor.click()` 后立即 revoke）：`douyin_.$taskId.tsx:455-456 / :470`、`douyin-library.tsx:448 / :455`、`douyin-interactions.tsx:678 / :689`、`douyin-comments.tsx:1011 / :1020 / :1187 / :1198`、`UnifiedWorksPanel.tsx:1422 / :1431 / :1446 / :1451`、`MediaPipelinePanel.tsx:478 / :483` | 结论：无 ObjectURL 泄漏
- **[性能]** `douyin-library.tsx:448-455` — 导出字幕时 `URL.createObjectURL(new Blob([﻿${content}]))`，content 为全量拼接的巨大字符串（见第五节）
- **[性能]** `iframe` 重挂载：`douyin-browsers.tsx:205-211` — `<iframe key={selected.viewer_url} src={selected.viewer_url}>`，`key` 变化即卸载重建 | 若 viewer_url 每 5s 轮询变化（如带 token/时间戳）会导致 iframe 每 5s 重载；通常 viewer_url 稳定，风险中等
- **[性能]** `VideoPreviewDialog.tsx:316` — `while (size >= 1024 && unit < units.length - 1)` 文件大小格式化循环 | 无害，不算问题

## 十、内联对象 / 内联函数作为 prop

- **[性能]** `index.tsx:219-223` — `<Link ... params={{ taskId: task.id }}>` 内联对象作为 prop，传给 Outlet/Link，可能导致 Link 重渲染 | 低
- **[性能]** `douyin.tsx:629` 等 — `<Link to="/douyin/$taskId" params={{ taskId: task.id }}>` 同类内联对象
- **[性能]** `douyin.tsx:339-341 / :351-353` 与 `UnifiedWorksPanel.tsx` 各视图 — `onSelectedChange` / `onCheckedChange` / `onDownload` / `onRetry` / `onRetranslate` 全部为内联箭头函数，每行新建 | 低（子组件未 memo 时影响有限）
- **[性能]** `MediaPipelinePanel.tsx:211-213` — `onRetry={() => retryMutation.mutate([asset.id])}` / `onRetranslate={() => retranslateMutation.mutate(asset.id)}` 每行内联
- **[性能]** `TaskResults.tsx:126-130` — 每行 `<AwemeActions>` 传 `active={active}` 等，无 memo 包装

## 十一、跨区间分级与结论

- **[结论]** AI 明确"最严重（高频轮询场景下必现卡顿）"清单 7 项：① `UnifiedWorksPanel` 的 `selected` 数组 includes + 未 memo 计算（每 2s 轮询）② `UnifiedWorksPanel` 每行挂载重组件（每 2s 轮询重渲染 20 行 × 大量 hooks）③ `douyin-creators` 的 `selected` 数组 includes（每 10s 轮询，200 行）④ `douyin-library` 的未 memo 多次遍历（每 5s 轮询，8 次遍历）⑤ `douyin-tracks_.$trackId` 的未 memo 计算（每 10s 轮询，长列表）⑥ `InteractionLiveMonitor` 双高频轮询（1s + 2s）⑦ `Sidebar/Main` 无 select 全量订阅（每次导航）
- **[结论]** AI 自编号性能问题累计至 **40 条**（区间内逐条编号 1-40，含少量"非问题"的正面对照项）
- **[结论]** 区间末尾（约 42314 行）开始输出正式分级报告，已出现标题：`## 🔴 严重（高频轮询场景下必现卡顿）`，条目骨架为：`### 1. UnifiedWorksPanel 每行挂载重组件 + useQuery 实例（最严重）`、`### 2. 无上限并发：Promise.allSettled`（`douyin-interactions.tsx:161-171` retryAll、`:178-185`）、`### 3. 全量加载`（`douyin-comments.tsx:987-994` exportTxt、`douyin-library.tsx:420-424` exportSubtitles）、`### 4. 无 staleTime`（`main.tsx:39` 全局无 defaultOptions、大量 useQuery 无 staleTime、终态后仍轮询）、`### 5. setTimeout 未清理`（`developer-tools.tsx:511`、`feed.tsx:89/136`）、`### 6. 内存泄漏：useCopyToClipboard.ts:21`、`### 6. recrawlComments 无上限并发`
- **[结论]** 量化统计汇总（本区间 grep 实测）：`refetchInterval` 34 处、`staleTime` 仅 8 处、`createObjectURL/revokeObjectURL` 17 处（全部正确）、`setInterval/setTimeout` 5 处、串行/全量循环类 6 处
- **[结论]** 单页 hook 实例量化：`UnifiedWorksPanel` 一页 20 行 ≈ **260 个 observer/状态实例**；`TaskResults` 一页 20 行 × AwemeActions 实例 ≈ 每行 1 useQuery + 2 useMutation + 2 InteractionComposerDialog（各 4 useQuery）；`AwemeActions` 评论列表 10 条 × 4 useQuery = **40 个 observer**
- **[结论]** AI 判定为"好设计"、不需修改的点：`douyin.tsx:748` 后端批量 resume、`douyin.tsx:278` / `MediaTaskManagement.tsx:208` 前端过滤不入 queryKey、`MediaPipelinePanel.tsx:46-62` 函数式 refetchInterval 终态停止、`TaskResults.tsx:42/54/66` 与 `AwemeActions.tsx:98` 与 `UnifiedWorksPanel.tsx:1214` 与 `douyin_.$taskId.tsx:373` 的 `active ? ... : false` 终态停止、`TrackSelect.tsx:28` / `SourceSelect.tsx:27` 的 `staleTime: 30_000`、全部 ObjectURL revoke 时机
