# G-列表缺陷-1（覆盖行 47767–58000）

> 主题：「数据列表架构专题」前半部分。区间内容 = 上一轮交接总结（H1–H16 / 性能 P0–P3 / 横切关注 / 文件清单）+ 本轮用户需求 + 带数据列表的页面清单 + 12 条架构审查维度 + 3 个子代理分工与提示词 + 子代理读取各列表页面源码所得的结构性证据（分页/limit/轮询/选择模型/视图持久化/三态）。

---

## 一、本轮任务与审查框架

- **[结论]** 位置：本轮用户请求 — 要求「站在前端架构师的角度分析每个页面的数据列表都有哪些缺陷」 | 影响：确定本轮为"数据列表架构专题"，产出为逐页缺陷清单 + 改进方向
- **[产品]** 位置：AI 列举的"带数据列表的页面"共 17 个：`douyin.tsx`、`douyin_.$taskId.tsx`、`douyin_.$taskId.feed.tsx`、`douyin-tracks.tsx`、`douyin-tracks_.$trackId.tsx`、`douyin-keywords.tsx`、`douyin-library.tsx`、`douyin-comments.tsx`、`douyin-creators.tsx`、`douyin-interactions.tsx`、`douyin-accounts.tsx`、`douyin-browsers.tsx`、`douyin-request-logs.tsx`、`douyin-tags.tsx`、`admin.tsx`、`items.tsx`、`index.tsx` | 价值：审查覆盖面基线
- **[产品]** 位置：AI 列举的"组件内的列表"共 6 个：`UnifiedWorksPanel`、`TaskResults`、`MediaTaskManagement`、`MediaPipelinePanel`、`TaskInteractionsPanel`、`InteractionLiveMonitor` | 价值：补充容易被漏掉的嵌入式列表
- **[架构]** 位置：12 条数据列表审查维度 — ①分页（服务端/客户端/无、pageSize 常量、页码跳转、硬编码 limit）；②筛选架构（URL 同步 vs 本地 state、即时 vs 草稿、防抖、条件组合）；③排序（服务端/客户端/无、字段）；④选择与批量（Set vs Array+includes、全选范围、批量二次确认、批量后是否清空）；⑤视图切换（卡片/横条/表格、持久化是否统一、功能对齐）；⑥三态完备（空/加载/错误、骨架屏 vs 文案、重试、各视图一致性）；⑦数据刷新（轮询策略、终态停止、`refetchIntervalInBackground:false`、手动刷新、`invalidate` queryKey 正确性）；⑧组件复用（通用 DataTable vs 手写 Table、列定义复用、操作列重复）；⑨性能（虚拟滚动、memo、每行重 hook、选中查找复杂度）；⑩无障碍（表格语义、aria-label、复选框标签、键盘）；⑪响应式（列隐藏、横向滚动、操作列溢出）；⑫行 key 稳定性（id vs 索引） | 价值：本轮所有缺陷判定的统一评分标准
- **[架构]** 位置：三个子代理分工 — Agent A「核心业务列表」、Agent B「运营类列表」、Agent C「配置类 + 组件内列表」 | 价值：审查并行化，本区间记录的是 A/B/C 读取源码阶段
- **[数据]** 位置：子代理提示词中给出的源码规模 — `douyin-tracks.tsx` 1936 行、`douyin-tracks_.$trackId.tsx` 2205 行、`douyin-library.tsx` 1627 行、`douyin-keywords.tsx` 1245 行、`douyin-comments.tsx` 1206 行、`douyin-creators.tsx` 1101 行、`douyin-accounts.tsx` 934 行、`douyin.tsx` 874 行、`douyin-interactions.tsx` 753 行、`douyin-request-logs.tsx` 606 行、`MediaTaskManagement` 571 行、`MediaPipelinePanel` 487 行、`UnifiedWorksPanel` 1479 行、`CreateTaskDialog` 828 行、`TaskResults` 313 行、`douyin-tags.tsx` 249 行、`TaskInteractionsPanel` 236 行、`douyin-browsers.tsx` 233 行、`admin.tsx` 73 行、`items.tsx` 70 行 | 影响：单文件 2000 行级页面是"列表逻辑内联"的根因之一
- **[架构]** 位置：子代理提示词要求产出「每个页面：列表实现现状 + 按 12 维度逐条缺陷 + 严重度分级 + 改进建议」 | 价值：本区间后续内容为该产出的素材层

---

## 二、会话交接（47767-47940）

> 上一轮会话的压缩总结（交接 prompt），位于 **47848–47978**，含 `## Goal`（47848-47856）、`## Instructions`（47858-47866）、`## Discoveries`（47868-47920）、`## Accomplished`（47922-47935）、`## Relevant files / directories`（47937-47978）五个小节。

### Goal（47848–47856）

- **[结论]** 位置：交接 prompt `## Goal` — 用户正在对抖音数据采集工作台 MediaCrawler_Dy_V2 的**前端项目**做全面审查与优化规划，共 6 项目标：①了解前端项目结构；②评估前端拆分成多个模块的可行性；③设计多风格（Skin）× 多布局（Layout）的灵活架构；④全面审查风格/交互/页面缺陷；⑤专项分析性能与卡顿问题；⑥最终产出可执行的修复路线图 | 影响：本轮"数据列表架构专题"是目标④⑤的进一步下钻

### Instructions（47858–47866）

- **[结论]** 位置：`## Instructions` — 交流语言：始终用简体中文回复，**包括内部思考**
- **[结论]** 位置：`## Instructions` — 仓库 `https://atomgit.com/qq_41251172/MediaCrawler_Dy_V2`，已克隆到 `/workspace/MediaCrawler_Dy_V2`
- **[结论]** 位置：`## Instructions` — 仓库**没有 main 分支**；默认分支 `refactor/uv-workspace-modules`，另有 `dev`；前端在 dev 与默认分支之间仅差 5 行（`login.tsx` 4 行 + `tsconfig.json` 1 行），故分析结论通用；当前工作区检出的是 `dev` 分支
- **[产品]** 位置：`## Instructions` — 用户多次指定 main 分支但实际不存在，需告知用户并基于现有分支分析 | 影响：分支事实需主动澄清
- **[结论]** 位置：`## Instructions` — 用户要求启动多智能体并行分析，使用 task 工具（`subagent_type: general`） | 价值：本轮同样沿用该并行审查模式
- **[安全]** 位置：`## Instructions` — 分析为**只读**，未修改任何代码；用户尚未授权修改代码，多次提议动手修复但用户未明确同意 | 影响：本轮数据列表分析延续只读约束

### Discoveries（47868–47920）

- **[结论]** 位置：`## Discoveries` → 项目技术栈 — React 19 + TypeScript 5.9 + Vite 7 + Tailwind CSS v4 + shadcn/ui (Radix) + TanStack Router/Query/Table + react-hook-form + zod + axios + 自动生成 OpenAPI client（@hey-api/openapi-ts）；包管理 Bun，规范 Biome，E2E Playwright | 价值：列表方案选型与既有依赖基线
- **[数据]** 位置：`## Discoveries` → 前端结构 — `frontend/src/routes/` 为 TanStack 文件路由，`_layout/` 下 **20 个业务页面**
- **[数据]** 位置：`## Discoveries` → 前端结构 — `frontend/src/components/Douyin/` 共 **25 个**抖音业务组件（核心）
- **[数据]** 位置：`## Discoveries` → 前端结构 — `frontend/src/components/{Common,Navigation,Sidebar,ui,Admin,Items,UserSettings}` 为通用与模板组件
- **[数据]** 位置：`## Discoveries` → 前端结构 — `frontend/src/client/` 为 OpenAPI 自动生成 SDK；`frontend/src/hooks/` 仅 **4 个** hook（`useAuth`、`useCustomToast`、`useMobile`、`useCopyToClipboard`） | 影响：无 `usePersistentViewMode` 等列表通用 hook 之外的抽象层
- **[数据]** 位置：`## Discoveries` → 前端结构 — `frontend/src/index.css` **338 行**，含 design token + skin + density 定义
- **[架构]** 位置：`## Discoveries` → 拆分分析结论 — 域间几乎**零耦合**；共享层（ui/Common/Navigation/Sidebar）**零业务依赖**
- **[架构]** 位置：`## Discoveries` → 拆分分析结论 — 推荐**方案 A（逻辑模块化，零构建改动）**优先，**方案 B（monorepo 抽 ui/client/shared 包）**作为进阶；**微前端不推荐**（TanStack 文件路由强绑定单应用）
- **[架构]** 位置：`## Discoveries` → 多风格多布局架构设计 — 现状：`theme(3) × preset(3) × density(2) × layout(2) = 36` 组合雏形，但**耦合在硬编码里**
- **[架构]** 位置：`## Discoveries` → 多风格多布局架构设计 — 设计：Skin 注册表（每 skin 一个 CSS 文件 + `data-skin` 属性）+ Layout 注册表（壳组件）+ `AppearanceProvider` 统一四维 + 外观设置面板从注册表**动态生成**
- **[架构]** 位置：`## Discoveries` → 多风格多布局架构设计 — 关键改动：`data-skin` 取代 `data-preset`；body 装饰光晕移出 `base.css`；`_layout.tsx` 从 **148 行硬编码瘦身为约 15 行**派发器
- **[数据]** 位置：`## Discoveries` → 缺陷审查 — **6 个智能体**产出 **212 条**原始记录，去重后 **高 22 / 中 48 / 低 60+**
- **[BUG]** 位置：H1 `PageShell.tsx:47,53-95` — `PageHero` 声明了 `description` prop 但 body **从未渲染** | 影响：全站 hero 描述丢失；**1 行即可修复**
- **[BUG]** 位置：H2 `settings.tsx:30-32` — `slice(0,3)` 与原数组等长导致 **superuser 分支无效**，普通用户可见"危险操作" tab | 影响：应改为 `slice(0,2)`；**1 行修复**
- **[安全]** 位置：H3 `login.tsx:57-58` — **默认凭据硬编码** `admin@example.com` / `changethis`
- **[安全]** 位置：H4–H7 `useAuth.ts`（共 4 条） — ①token **明文存 localStorage**；②登录后**不刷新 `currentUser`**；③`logout` **不清缓存**；④`isLoggedIn` 仅检查 token 是否存在
- **[BUG]** 位置：H8 `developer-tools.tsx` — **无权限控制** | 影响：开发者工具页任意用户可访问（与 `admin.tsx` 的 `beforeLoad` 双重鉴权形成对比）
- **[BUG]** 位置：H9 `douyin-library.tsx:362` — `invalidateQueries` 的 queryKey 写错（`["douyin-tasks"]` vs 正确 `["douyin-library-tasks"]`） | 影响：重采评论后列表不刷新；**1 行修复**
- **[BUG]** 位置：H14 `douyin_.$taskId.feed.tsx:244-250` — 字幕用 **data URI 塞进单个 99:59:59 的 cue** | 影响：时间轴全丢
- **[A11Y]** 位置：H16 `Appearance.tsx:44-128` — 用 `div` 包裹 `DropdownMenuItem` | 影响：破坏 Radix 键盘导航
- **[BUG]** 位置：H21/H22 `src/routes/_layout.tsx` — 布局壳 **148 行硬编码** | 影响：多风格多布局无法落地
- **[风格]** 位置：F2 `src/components/Common/AuthLayout.tsx` — **渐变硬编码** | 影响：与 CSS 变量体系不一致
- **[架构]** 位置：`## Discoveries` → 11 项横切关注 — ①表单模式分裂（react-hook-form 已安装但核心表单手写 `useState`）；②轮询策略不一；③确认对话框分裂（`window.confirm` vs shadcn Dialog）；④错误处理分裂；⑤重复代码（`formatDate` 6 处等）；⑥CSS 变量 vs 原生色混用；⑦localStorage 散落；⑧手动 fetch 绕过 client；⑨a11y 系统性缺失；⑩i18n 缺失；⑪分页不一 | 影响：②⑤⑦⑧⑨⑪ 六项直接命中数据列表
- **[数据]** 位置：`## Discoveries` → 性能专项审查 — **2 个智能体**产出 **47 条**，去重后 **40 条**
- **[性能]** 位置：P0-1 `douyin-comments.tsx:987` — 评论导出：`while` **串行 100 次请求** + 拼接 **5–10MB** 字符串 | 影响：卡死
- **[性能]** 位置：P0-2 `douyin-library.tsx:420` — 字幕导出：拼接 **25–50MB** 字符串 | 影响：低端设备 **OOM**
- **[性能]** 位置：P0-3 `douyin-interactions.tsx:161` — 批量重试发出 **500 个并发请求** | 影响：卡死/打爆后端
- **[性能]** 位置：P0-4 `douyin-interactions.tsx:652` — 互动截图 **50 个并发 fetch** + `createObjectURL` | 影响：卡顿与内存泄漏
- **[性能]** 位置：P1-1 — `UnifiedWorksPanel` 每行挂载约 **260 个 hook 实例** + **2 秒**轮询 | 影响：高频轮询必现卡顿
- **[性能]** 位置：P1-2 — `selected` 用数组 `includes`，复杂度 **O(n)** × 每行 × 每轮轮询 | 影响：卡顿放大（点名 `douyin-creators.tsx`）
- **[性能]** 位置：P1-3 — `Sidebar` 的 `useRouterState` **无 `select`**，全量订阅 | 影响：任何路由变化全量重渲染
- **[性能]** 位置：P1-4 — `InteractionLiveMonitor` **1s + 2s 双高频轮询** | 影响：重复请求
- **[性能]** 位置：P1-5 — **18 处未 memo** 的派生计算 × 每轮轮询 | 影响：重复遍历
- **[性能]** 位置：P1-6/P1-7 `main.tsx:39` — `QueryClient` **无 `defaultOptions`** | 影响：**一行修复可缓解 10+ 个问题**
- **[架构]** 位置：`## Discoveries` → 横切根因 — **G1** `QueryClient` 无 `defaultOptions`；**G2** 轮询无统一封装；**G3** `selected` 普遍用数组 | 价值：三条根因覆盖绝大多数列表性能缺陷
- **[架构]** 位置：`## Discoveries` → 修复路线图 — 共 **10 步**；**步骤 1-2** 各一行代码即可缓解最大面积卡顿；**步骤 9（P0 后端化）**是唯一需要**跨端协作**的大改

### Accomplished（47922–47935）

- **[结论]** 已完成：项目结构分析完成
- **[结论]** 已完成：拆分可行性分析完成（三方案 + 推荐）
- **[结论]** 已完成：多风格多布局架构设计完成（**未落地**）
- **[结论]** 已完成：缺陷审查完成（6 智能体，212 条，统一报告）
- **[结论]** 已完成：性能专项审查完成（2 智能体，47 条，最终报告）
- **[结论]** 已完成：修复路线图产出（**10 步，按 ROI 排序**）
- **[结论]** 未完成：未动手修任何代码（用户未授权）
- **[结论]** 未完成：多风格多布局架构未落地；缺陷未修复；性能问题未修复
- **[产品]** 位置：`## Accomplished` → 下一步 — 等待用户决定是否动手修复 | 提议的快赢项：**H1/H2/H9 各一行修复**；性能**步骤 1-2 各一行代码**

### Relevant files / directories（47937–47978，逐条）

- **[数据]** `frontend/` — 前端根目录
- **[数据]** `frontend/package.json` — 依赖（**react-icons 死依赖**）
- **[数据]** `frontend/vite.config.ts` — **无 `manualChunks`** | 影响：产物不做分包，首屏体积与缓存粒度差
- **[数据]** `frontend/tsconfig.json` — 路径别名 `@/*`
- **[数据]** `frontend/components.json` — shadcn 配置
- **[数据]** `frontend/src/main.tsx` — 入口，`QueryClient` 配置（**P1-7 根因**，`main.tsx:39`）
- **[数据]** `frontend/src/index.css` — 338 行，design token + skin + density
- **[数据]** `frontend/src/utils.ts` — `handleError`（**3 种调用风格分裂**）
- **[数据]** `frontend/src/client/` — OpenAPI 自动生成 SDK
- **[数据]** `frontend/src/hooks/` — `useAuth.ts`（**H4-H7 安全问题**）、`useCustomToast`、`useMobile`、`useCopyToClipboard`
- **[数据]** `frontend/src/routes/__root.tsx` — 根路由
- **[数据]** `frontend/src/routes/_layout.tsx` — 布局壳（**148 行硬编码，H21/H22**）
- **[数据]** `frontend/src/routes/login.tsx` — **H3 默认凭据**
- **[数据]** `frontend/src/routes/_layout/index.tsx` — 首页（带数据列表页面之一）
- **[数据]** `frontend/src/routes/_layout/settings.tsx` — **H2 superuser 逻辑**
- **[数据]** `frontend/src/routes/_layout/admin.tsx` — `beforeLoad` **双重鉴权**（正面样本）
- **[数据]** `frontend/src/routes/_layout/developer-tools.tsx` — **H8 无权限控制**
- **[数据]** `frontend/src/routes/_layout/douyin.tsx` — 任务管理（列表：硬编码 `limit:100`）
- **[数据]** `frontend/src/routes/_layout/douyin_.$taskId.tsx` — 任务详情
- **[数据]** `frontend/src/routes/_layout/douyin_.$taskId.feed.tsx` — **H14 字幕时间轴丢失**
- **[数据]** `frontend/src/routes/_layout/douyin-tracks.tsx` — 赛道管理（**1936 行**）
- **[数据]** `frontend/src/routes/_layout/douyin-tracks_.$trackId.tsx` — 赛道详情（**2205 行**，全仓最大）
- **[数据]** `frontend/src/routes/_layout/douyin-keywords.tsx` — 关键词管理
- **[数据]** `frontend/src/routes/_layout/douyin-library.tsx` — 资源库（**P0-2 字幕导出、H9 queryKey 错**）
- **[数据]** `frontend/src/routes/_layout/douyin-comments.tsx` — 评论管理（**P0-1 评论导出**）
- **[数据]** `frontend/src/routes/_layout/douyin-creators.tsx` — 达人列表（**P1-2 selected O(n)**）
- **[数据]** `frontend/src/routes/_layout/douyin-interactions.tsx` — 互动任务（**P0-3 / P0-4**）
- **[数据]** `frontend/src/routes/_layout/douyin-accounts.tsx` — 账号池
- **[数据]** `frontend/src/routes/_layout/douyin-browsers.tsx` — 浏览器监控
- **[数据]** `frontend/src/routes/_layout/douyin-request-logs.tsx` — 请求日志
- **[数据]** `frontend/src/routes/_layout/douyin-tags.tsx` — 标签管理
- **[数据]** `frontend/src/components/Douyin/` — **25 个**业务组件，其中 `UnifiedWorksPanel` **1479 行**（含 **P1-1/P1-5**）、`CreateTaskDialog` **828 行**、`MediaPipelinePanel`、`TaskResults`、`InteractionLiveMonitor` 等
- **[数据]** `frontend/src/components/Common/PageShell.tsx` — **H1 PageHero description 未渲染**
- **[数据]** `frontend/src/components/Common/Appearance.tsx` — **H16 键盘导航破坏**
- **[数据]** `frontend/src/components/Common/AuthLayout.tsx` — **F2 渐变硬编码**
- **[数据]** `frontend/src/components/Common/DataTable.tsx` — **英文硬编码**（i18n 缺失）
- **[数据]** `frontend/src/components/Navigation/navigation.ts` — 导航定义
- **[数据]** `frontend/src/components/Sidebar/Main.tsx` — **P1-3 `useRouterState` 无 `select`**
- **[数据]** `frontend/src/components/theme-provider.tsx` — 主题 provider


> 上述 `## Discoveries` 各小节已逐条展开；H1/H2/H3/H4-H7/H8/H9/H14/H16/H21/H22/F2、11 项横切关注、P0-1~P0-4、P1-1~P1-7、G1-G3、10 步修复路线图 均已在上方归档，此处不重复。

---

## 三、任务列表 `routes/_layout/douyin.tsx`（874 行）

- **[数据]** 位置：`douyin.tsx:117-128` — 列表查询硬编码 `{ skip: 0, limit: 100 }`，无分页参数 | 影响：超过 100 个任务后不可见，且无任何分页 UI
- **[数据]** 位置：`douyin.tsx:127` — `refetchInterval: 3_000` 固定 3 秒轮询，无"全部终态即停止"判断 | 影响：列表空闲时仍持续请求
- **[性能]** 位置：`douyin.tsx:130-134` — `attentionCount` 每次渲染 `filter` 计算 | 影响：未 memo 的重复遍历
- **[性能]** 位置：`douyin.tsx:156,157-159` — `selectableTasks` 每次渲染生成新数组；`allSelectableTasksSelected` 全量 O(n) 判选 | 影响：大列表渲染退化
- **[优化]** 位置：`douyin.tsx:114-116,161-179` — `selectedTaskIds` 使用 `Set<string>`，`toggleTaskSelection`/`toggleAllSelectableTasks` 均基于 Set 增删 | 价值：**正面样本**，与其它页面的数组方案形成对比
- **[数据]** 位置：`douyin.tsx:135-155` — 搜索/状态/赛道/来源筛选全部走 `useMemo` **客户端过滤**，不请求服务端 | 影响：数据量受 `limit:100` 限制，筛选结果不完整
- **[架构]** 位置：`douyin.tsx:224-309` — 筛选条件（状态/赛道/来源/搜索）全部本地 `useState`，**不同步到 URL** | 影响：刷新/分享/后退丢失筛选状态
- **[交互]** 位置：`douyin.tsx:250-254` — 切换赛道时重置来源并清空已选 | 影响：用户已选任务被静默丢弃
- **[交互]** 位置：`douyin.tsx:270-282` — 搜索框即时过滤，**无防抖** | 影响：大列表每次按键全量 filter
- **[数据]** 位置：`douyin.tsx:107-113` — `viewMode = usePersistentViewMode("douyin-tasks-view")`，支持卡片/横条/表格三视图 | 价值：视图持久化标准实现（后续页面多处未采用）
- **[交互]** 位置：`douyin.tsx:311-331` — 三态：`isLoading` 仅显示"正在加载任务…"文案（**非骨架屏**）；错误态 `QueryErrorState` 有重试；空态分"无任务"/"无匹配"两种 | 影响：加载体验与其它页不统一
- **[A11Y]** 位置：`douyin.tsx:367-382` — 表头全选 `Checkbox` 带 `indeterminate` 与 `aria-label="全选可删除任务"` | 价值：**正面样本**
- **[性能]** 位置：`douyin.tsx:583-668` — `TaskActions` 每行一个 DropdownMenu + 3 个 `useMutation` + `useQueryClient` + `useCustomToast` | 影响：每行挂载重 hook，是"每行重 hook"通病的实例
- **[交互]** 位置：`douyin.tsx:646-648` — 从头重启使用 `window.confirm` | 影响：与全站 shadcn Dialog 风格分裂（横切关注③）
- **[数据]** 位置：`douyin.tsx:697-702,723-724` — `deletableTaskStatuses = ["failed","cancelled","interrupted"]`；`restartableTaskStatuses = ["failed","interrupted"]` | 价值：状态白名单常量，可抽出复用
- **[数据]** 位置：`douyin.tsx:726-818` — `BulkResumeButton` 默认 `taskInterval = "10"`，上限 `3600` | 价值：量化参数
- **[交互]** 位置：`douyin.tsx:820-865` — `BulkDeleteButton` 使用 `window.confirm`（`window.confirm` 在 douyin.tsx 内重复出现，**（重复 2 次）**）
- **[架构]** 位置：`douyin.tsx:193-205` — Tab 计数用 `data?.count ?? tasks.length`，与筛选后的行数不一致 | 影响：徽标数字与实际渲染条数对不上
- **[风格]** 位置：`douyin.tsx:867-874` — 本地定义 `formatDate` | 影响：全站 6 处重复实现之一

---

## 四、视频资源库 `routes/_layout/douyin-library.tsx`（1627 行）

- **[数据]** 位置：`douyin-library.tsx:84` — `const pageSize = 32` | 价值：量化常量（唯一非 50 的页大小）
- **[数据]** 位置：`douyin-library.tsx:232-265` — `worksQuery`：`skip: page*pageSize, limit: pageSize`，queryKey 含 10 个筛选字段，`placeholderData: (previous) => previous` | 价值：**正面样本**，全站少数具备服务端分页的列表
- **[性能]** 位置：`douyin-library.tsx:264` — `refetchInterval: 5_000` **无条件轮询**，无终态判断 | 影响：下载状态稳定的列表仍每 5 秒重拉
- **[数据]** 位置：`douyin-library.tsx:159` — `const [page, setPage] = useState(0)`，页码为本地 state，**不同步 URL** | 影响：刷新后回到第一页
- **[架构]** 位置：`douyin-library.tsx:160-173` — search/trackId/sourceValue/taskId/creatorHash/tagId/storageBackend/downloadStatus 均以 `routeSearch` 为初始值、之后**只读不回写** | 影响：URL 与界面状态单向绑定，属"筛选不同步 URL"的典型变体
- **[架构]** 位置：`douyin-library.tsx:113-150` — `Route.validateSearch` 完整解析 track/source/q/task/creator/tag/storage/subtitle/sort 共 9 个参数 | 价值：URL 契约已具备，缺的是回写
- **[架构]** 位置：`douyin-library.tsx:174-181` — `viewMode` **手写 `localStorage.getItem("douyin-library-view")`**，未用 `usePersistentViewMode`，且无 try/catch | 影响：视图持久化第三套实现（与 hooks 版本、tracks 版本并存）
- **[数据]** 位置：`douyin-library.tsx:201-210` — `tasksQuery`（`limit: 100`, `staleTime: 30_000`）；`douyin-library.tsx:211-219` — `creatorsQuery`（`staleTime: 30_000`）；`douyin-library.tsx:220-231` — `tagsQuery`（`limit: 500, sortBy: aweme_count`） | 价值：量化 limit 值
- **[性能]** 位置：`douyin-library.tsx:292-299` — `pageLocal`/`pageMinio`/`pageUndownloaded` 每次渲染对 `rows` 做 3 次 `filter` | 影响：未 memo 的重复遍历
- **[性能]** 位置：`douyin-library.tsx:279-291` — `selectedAwemeIds` 为 `string[]`，另用 `useMemo` 建 `selectedAwemeSet`，`selectedRows = rows.filter(...)` | 影响：数组 + 集合双份状态，赋值处必须同时维护
- **[性能]** 位置：`douyin-library.tsx:481-489` — `toggleSelection` 用数组 `includes` + 展开重建 | 影响：O(n) 判选，与 douyin.tsx 的 Set 方案不统一
- **[BUG]** 位置：`douyin-library.tsx:490-492` — `togglePageSelection(checked)` 直接 `setSelectedAwemeIds(checked ? pageAwemeIds : [])` | 影响：全选仅覆盖当前页，且会**清空之前跨页选择**，跨页批量语义错误
- **[性能]** 位置：`douyin-library.tsx:329-365` — `recrawlComments` 用 `for` 循环**串行**逐个创建任务（N 次请求） | 影响：批量重采评论耗时线性增长
- **[BUG]** 位置：`douyin-library.tsx:362` — `invalidateQueries(["douyin-tasks"])`，与列表实际 queryKey 不符 | 影响：重采后视频库列表不刷新（H9）
- **[性能]** 位置：`douyin-library.tsx:389-462` — `exportSubtitles` 用 `while` **全量翻页**（每页 `limit: 100`）累积，`total > 1000` 才 `window.confirm`，最后拼成大字符串导出 txt（**（重复 2 次）**：与 P0-2 同源） | 影响：25–50MB 字符串 OOM
- **[交互]** 位置：`douyin-library.tsx:389-462` — 导出无并发控制、无进度反馈、无取消 | 影响：导出期间界面假死
- **[交互]** 位置：`douyin-library.tsx:574-711` — 第一行筛选区堆叠 8+ 个控件（搜索、赛道、来源、任务、标签、创作者、存储、字幕状态） | 影响：可用性与响应式压力
- **[交互]** 位置：`douyin-library.tsx:712-789` — 第二行：视图切换 fieldset（表格/横条/卡片）、下载状态、排序 | 影响：筛选与视图混排，缺分组语义
- **[数据]** 位置：`douyin-library.tsx:92-111` — `SortValue` 共 **8 种**排序，`sortValues` 做合法性校验 | 价值：量化（8）
- **[交互]** 位置：`douyin-library.tsx:790-840` — 批量条：全选 Checkbox `id="select-library-page"`；批量创建评论任务**超过 20 条需确认**；配 `BatchCommentDialog` | 价值：量化阈值 20
- **[交互]** 位置：`douyin-library.tsx:925-932` — `Pager` 翻页时**清空已选** | 影响：跨页选择不可用
- **[数据]** 位置：`douyin-library.tsx:937-972` — `mediaStateLabel` / `MediaStateBadge` 状态映射 | 价值：可复用状态字典
- **[性能]** 位置：`douyin-library.tsx:992-1086` — `WorkActionButtons` 每行 6–8 个按钮 | 影响：表格操作列溢出（响应式维度）
- **[性能]** 位置：`douyin-library.tsx:1088-1231 / 1233-1342 / 1344-1554` — `VideoCard` / `VideoRow` / `VideoTable` 三套视图各自实现，操作按钮逻辑重复 | 影响：三视图功能对齐需三处同步维护
- **[数据]** 位置：`douyin-library.tsx:1407` — 行 key 用 `aweme.id` | 价值：**正面样本**（行 key 稳定）
- **[A11Y]** 位置：`douyin-library.tsx:1344-1554` — 表格列宽紧张时依赖横向滚动 | 影响：响应式维度待改进
- **[风格]** 位置：`douyin-library.tsx:1598-1626` — 本地 `formatUnix` / `formatDateTimeText` / `subtitleStatusLabel` / `compact` | 影响：格式化函数重复实现

---

## 五、评论管理 `routes/_layout/douyin-comments.tsx`（1206 行）

- **[数据]** 位置：`douyin-comments.tsx:80` — `const pageSize = 50` | 价值：量化常量
- **[数据]** 位置：`douyin-comments.tsx` 评论列表查询 — 本地 page + 上一页/下一页 | 影响：无页码跳转、无每页大小切换
- **[架构]** 位置：`douyin-comments.tsx` 筛选区 — **草稿态 / 已应用态双份筛选项**，点查询才生效，**筛选不同步 URL** | 影响：URL 无法还原视图
- **[交互]** 位置：`douyin-comments.tsx` 筛选校验 — `minLikes > maxLikes`、日期区间非法时弹 toast | 价值：**正面样本**（筛选前置校验）
- **[性能]** 位置：`douyin-comments.tsx` 选择 — `selected` 为 `Set<string>` | 价值：**正面样本**
- **[性能]** 位置：`douyin-comments.tsx:987`（H/P0-1） — `CommentExportDialog` 全量结果导出 TXT：`while` 循环**串行 100 次请求**、每次 `limit: 100`、`total > 1000` 时 `window.confirm`、最终拼 5–10MB 字符串 | 影响：主线程阻塞、导出慢、内存压力
- **[安全]** 位置：`douyin-comments.tsx` `downloadSelectedComments` — **手动 fetch** `/api/v1/douyin/comments/export`，自行 `localStorage.getItem("access_token")` 取 token | 影响：绕过 OpenAPI 客户端，token 明文外带（横切关注⑧）
- **[数据]** 位置：`douyin-comments.tsx` 表格 — `min-w-[900px]` 固定最小宽度 | 影响：小屏依赖横向滚动
- **[交互]** 位置：`douyin-comments.tsx` 列表/导出 — 导出与下载两套导出路径并存 | 影响：行为与提示口径不一致

---

## 六、达人列表 `routes/_layout/douyin-creators.tsx`（1101 行）

- **[BUG]** 位置：`douyin-creators.tsx:75` — `const pageLimit = 200`，**完全无分页**，界面文案"仅显示前 200 位达人，共 N 位" | 影响：第 200 名之后的达人永久不可达，且这是**显式承认的截断**
- **[数据]** 位置：`douyin-creators.tsx:122` — 列表 `refetchInterval: 10_000` 固定轮询，无终态判断 | 影响：空闲期持续请求
- **[性能]** 位置：`douyin-creators.tsx:130,133` — 另有 `overviewQuery`（`limit: 500`，`refetchInterval: 15_000`）**为统计数据重复拉取全量数据** | 影响：同一份数据两份请求、两套轮询
- **[BUG]** 位置：`douyin-creators.tsx` 错误态 — 仅渲染一个虚线框 div，**无重试按钮** | 影响：出错后用户只能刷新整页
- **[性能]** 位置：`douyin-creators.tsx` 选择 — `selected` 为 `string[]` + `includes` | 影响：O(n) 判选（与 douyin.tsx 的 Set 不统一）
- **[数据]** 位置：`douyin-creators.tsx` 批量创建 — 单次上限 **20** 条 | 价值：量化阈值
- **[交互]** 位置：`douyin-creators.tsx` 搜索框 — **无防抖**，直接触发查询 | 影响：输入过程产生多余请求

---

## 七、互动任务 `routes/_layout/douyin-interactions.tsx`（753 行）

- **[数据]** 位置：`douyin-interactions.tsx:87-109` — `listInteractions` 硬编码 `limit: 100`，**无分页** | 影响：超过 100 条不可见
- **[数据]** 位置：`douyin-interactions.tsx:103-108` — `refetchInterval` 为函数：存在 queued/running 时 `2_000`，否则 `5_000` | 价值：**正面样本**（轮询降频），但仍未彻底停止
- **[数据]** 位置：`douyin-interactions.tsx:110-120` — 详情查询 `refetchInterval`：存在 queued/running 时 `2_000`，否则 `false` | 价值：**正面样本**（终态停止）
- **[性能]** 位置：`douyin-interactions.tsx:156-202`（P0-3） — `retryAll`：`do/while` 全量翻页拉取（`limit: 100`/页）后一次性 `Promise.allSettled` 全部重试，实测可发 **500 并发** | 影响：打爆后端与浏览器连接池；`window.confirm` 后才执行
- **[交互]** 位置：`douyin-interactions.tsx:602-615` — `retryAfterReview` 使用 `window.confirm` | 影响：确认弹窗风格分裂（**（重复 2 次）** 含 retryAll）
- **[BUG]** 位置：`douyin-interactions.tsx` 表格空态单元格 `colSpan={6}`，而表头声明 **7 列** | 影响：空态错位
- **[产品]** 位置：`douyin-interactions.tsx:313-459` — 表格**无选择列** | 影响：无任何批量操作入口（与其它列表能力不对齐）
- **[数据]** 位置：`douyin-interactions.tsx:313-459` — 表格**无排序、无分页** | 影响：无法按时间/状态排序定位
- **[性能]** 位置：`douyin-interactions.tsx:539-582` — 详情 Dialog 内事件列表**无虚拟滚动、无分页** | 影响：长任务事件量大时渲染卡顿
- **[安全]** 位置：`douyin-interactions.tsx:652-747`（P0-4） — `InteractionEvidence` **手动 fetch**（`interactionApiBase`，`douyin-interactions.tsx:749-753`）+ `localStorage.getItem("access_token")`；每事件 `URL.createObjectURL` | 影响：50 并发 fetch、blob 内存泄漏、绕过客户端鉴权层
- **[BUG]** 位置：`douyin-interactions.tsx:675-677` — 截图加载失败直接抛出 | 影响：单张失败影响整体展示
- **[优化]** 位置：`douyin-interactions.tsx:121-134` — 重试后统一 `invalidateQueries` 四组 queryKey | 价值：**正面样本**
- **[数据]** 位置：`douyin-interactions.tsx:79-84` — statusFilter/typeFilter/trackId/sourceValue/detailId/monitorId 全为本地 state | 影响：筛选不同步 URL

---

## 八、账号池 `routes/_layout/douyin-accounts.tsx`（934 行）

- **[数据]** 位置：`douyin-accounts.tsx:90-95` — `accountsQuery` 硬编码 `limit: 100`，无分页；`retry: false`；`refetchInterval: 5_000` 固定 | 影响：账号超过 100 个不可见 + 空闲轮询
- **[数据]** 位置：`douyin-accounts.tsx:101-106` — `slotsQuery` `retry: false` + `refetchInterval: 5_000` | 影响：与 accounts 轮询叠加
- **[结论]** 位置：`douyin-accounts.tsx:241-459` — **三态实现分裂**：table 视图把 loading/error/empty 渲染在表格行内（`colSpan 7`），cards/rows 视图另写一套三态（`douyin-accounts.tsx:411-457`） | 影响：同一页面两套三态，易漂移
- **[性能]** 位置：`douyin-accounts.tsx:114-159` — login/verify 用 mutation；`loginPendingIds`/`verifyPendingIds` 用 `Set` | 价值：**正面样本**（pending 集合用 Set）
- **[性能]** 位置：`douyin-accounts.tsx:187-191` — 顶部统计每次渲染计算，未 memo | 影响：重复遍历
- **[交互]** 位置：`douyin-accounts.tsx:379-385`、`douyin-accounts.tsx:498-504` — 删除账号 / 删除账号池均用 `window.confirm` | 影响：确认弹窗分裂（**（重复 2 次）**）
- **[数据]** 位置：`douyin-accounts.tsx:89` — `viewMode = usePersistentViewMode("douyin-accounts-view")` | 价值：**正面样本**（视图持久化标准实现）
- **[交互]** 位置：`douyin-accounts.tsx:125` — 预览用 `window.open(viewer_url)` | 影响：与内嵌 iframe 方案（browsers 页）不统一
- **[数据]** 位置：`douyin-accounts.tsx:462-528` — 账号池列表 `grid lg:2`，`pool.accounts` 用 Badge 展示 | 价值：布局量化
- **[风格]** 位置：`douyin-accounts.tsx:877-934` — 本地 `formatDate` | 影响：重复实现之一

---

## 九、请求日志 `routes/_layout/douyin-request-logs.tsx`（606 行）

- **[数据]** 位置：`douyin-request-logs.tsx:55-56` — `const PAGE_SIZE = 50`、`const ALL_TASKS = "all"` | 价值：量化常量
- **[数据]** 位置：`douyin-request-logs.tsx:94-97,464-473` — 基于 `applied + skip + limit` 的服务端分页，`buildQuery` 统一构造 | 价值：**正面样本**（本区间少数真正的服务端分页）
- **[架构]** 位置：`douyin-request-logs.tsx:103-117` — `applyFilters` 为草稿→已应用模型，点"查询"才生效并 `setSkip(0)`，**筛选不同步 URL** | 影响：URL 不可还原
- **[性能]** 位置：`douyin-request-logs.tsx:87-92` — `taskMap` 每次渲染用 `new Map(...)` 重建 | 影响：未 memo，任务选项多时开销明显
- **[数据]** 位置：`douyin-request-logs.tsx:83-86` — 任务下拉选项查询 `limit: 200` | 价值：量化 limit
- **[数据]** 位置：`douyin-request-logs.tsx:368-392` — 分页条仅有"共 N 条 · 第 X / Y 页"+ 上一页/下一页 | 影响：无页码跳转、无每页大小切换（分页实现不统一之一）
- **[优化]** 位置：`douyin-request-logs.tsx:120-137` — 仅手动刷新，**不轮询** | 价值：**正面样本**（日志类列表的正确刷新策略）
- **[数据]** 位置：`douyin-request-logs.tsx:323-333` — 空态/加载态 `colSpan` 与 7 列表头一致 | 价值：**正面样本**（对比 interactions 的 colSpan 错位）
- **[产品]** 位置：`douyin-request-logs.tsx:141-238` — 筛选条件丰富：任务、方法、路径包含、状态码、开始/结束时间（datetime-local） | 价值：本区间筛选维度最完整的列表
- **[风格]** 位置：`douyin-request-logs.tsx:601-606` — 本地 `formatDate` | 影响：重复实现之一

---

## 十、标签管理 `routes/_layout/douyin-tags.tsx`（249 行）

- **[数据]** 位置：`douyin-tags.tsx:34` — `const pageSize = 50` | 价值：量化常量
- **[数据]** 位置：`douyin-tags.tsx:49-61` — 服务端分页（`skip: page*pageSize, limit: pageSize`）+ `placeholderData` | 价值：**正面样本**
- **[BUG]** 位置：`douyin-tags.tsx:80-215` — **完全没有错误态**：仅有空态与加载态分支 | 影响：请求失败时界面停留在空白/骨架，用户无重试途径
- **[数据]** 位置：`douyin-tags.tsx:42-48` — 4 种排序（`aweme_count:desc` / `task_count:desc` / `last_seen_at:desc` / `name:asc`），以 `split(":")` 解析 | 价值：量化（4 种）
- **[交互]** 位置：`douyin-tags.tsx:109-120` — 搜索每次按键触发并把 `page` 重置为 0，**无防抖** | 影响：输入过程产生多余请求
- **[架构]** 位置：`douyin-tags.tsx:39-44` — page/trackId/search/sort 全为本地 state，**不同步 URL** | 影响：视图不可分享
- **[性能]** 位置：`douyin-tags.tsx:96-107` — "本页视频/任务合计" 用 `reduce` 每次渲染计算 | 影响：未 memo
- **[数据]** 位置：`douyin-tags.tsx:217-235` — 分页条仅上一页/下一页 + "共 N 个标签" | 影响：无页码跳转、无每页大小（分页不统一）
- **[产品]** 位置：`douyin-tags.tsx:155-215` — **仅表格视图**，无 `ViewModeToggle` | 影响：与其它列表视图能力不对齐
- **[数据]** 位置：`douyin-tags.tsx:177-198` — 行内"查看视频" `Link` 显式传递全量 9 个 search 参数 | 影响：参数新增时需两处同步，易漏
- **[数据]** 位置：`douyin-tags.tsx:62-76` — `sync` mutation 成功后 invalidate 4 个 queryKey | 价值：**正面样本**
- **[风格]** 位置：`douyin-tags.tsx:242-249` — 本地 `formatDate` | 影响：重复实现之一

---

## 十一、浏览器槽位 `routes/_layout/douyin-browsers.tsx`（233 行）

- **[数据]** 位置：`douyin-browsers.tsx:21-26` — `slotsQuery` `retry: false` + `refetchInterval: 5_000` | 影响：固定 5 秒轮询（此处可接受，槽位为实时监控）
- **[性能]** 位置：`douyin-browsers.tsx:28-34` — `useEffect` 依赖 `selectedName, slots` 同步选中项 | 影响：每 5 秒 slots 引用变化即触发 effect，可能造成选中态抖动
- **[A11Y]** 位置：`douyin-browsers.tsx:78-132` — 槽位列表 `nav aria-label`、槽位按钮带 `aria-pressed` | 价值：**正面样本**（本区间唯一的键盘/语义友好列表导航）
- **[A11Y]** 位置：`douyin-browsers.tsx` 在线状态点 — `role="img"` + `aria-label` | 价值：**正面样本**（状态色块可被读屏识别）
- **[数据]** 位置：`douyin-browsers.tsx:67-69` — 布局 `xl:h-[calc(100svh-12rem)]`、`min-h 620px` | 价值：量化尺寸
- **[数据]** 位置：`douyin-browsers.tsx:78-132` — 列表容器 `max-h-80 overflow-y-auto` | 影响：长列表靠滚动而非虚拟化/分页
- **[优化]** 位置：`douyin-browsers.tsx:204-211` — iframe `key={selected.viewer_url}` 强制重建 + `allow="clipboard-read; clipboard-write; fullscreen"` | 价值：**正面样本**（切槽位时避免复用旧页面）
- **[优化]** 位置：`douyin-browsers.tsx:59-65` — 错误态使用 `QueryErrorState`（含重试） | 价值：**正面样本**
- **[数据]** 位置：`douyin-browsers.tsx:121-130` — 空态/加载态文案分支齐备 | 价值：**正面样本**

---

## 十二、赛道列表 `routes/_layout/douyin-tracks.tsx`（1936 行）

- **[BUG]** 位置：`douyin-tracks.tsx:108-114` — `tracksQuery` **无分页参数**（全量拉取），无 limit 上限 | 影响：赛道数增长后列表整体变慢
- **[数据]** 位置：`douyin-tracks.tsx:108-114` — `refetchInterval: 10_000` 固定轮询，无终态判断 | 影响：空闲期持续请求
- **[架构]** 位置：`douyin-tracks.tsx:87-102` — `viewMode` **手写 `localStorage`（key `douyin-tracks-view`）并带 try/catch** | 影响：视图持久化第三套实现（vs `usePersistentViewMode`）
- **[BUG]** 位置：`douyin-tracks.tsx:254-321` vs `322-334` vs `336-426` — **视图功能不对齐**：table 视图的操作列只有"运行"（`douyin-tracks.tsx:308-314`），而 rows/cards 视图提供 编辑/启停/删除 | 影响：用户切换视图即丢失操作能力（视图切换维度最严重的缺陷）
- **[性能]** 位置：`douyin-tracks.tsx:164-169` — `active/keywordCount/works/comments` 统计用 `reduce` 每次渲染计算 | 影响：未 memo
- **[性能]** 位置：`douyin-tracks.tsx:121-138` — 处理 `run` 参数的 `useEffect` | 影响：与轮询叠加时需注意重复触发
- **[架构]** 位置：`douyin-tracks.tsx:76-79` — `validateSearch` 仅校验 `run` 参数 | 影响：搜索/视图等状态不进 URL
- **[交互]** 位置：`douyin-tracks.tsx:202-245` — 搜索 + 视图切换 fieldset（table/rows/cards） | 价值：三视图能力基线
- **[数据]** 位置：`douyin-tracks.tsx:271-317` — 行 key 用 `track.id` | 价值：**正面样本**（行 key 稳定）
- **[数据]** 位置：`douyin-tracks.tsx` 表格（8 列） — 含关键词/任务/作品/评论/最近采集 | 价值：列表列结构规模
- **[优化]** 位置：`douyin-tracks.tsx:139-140,141-163` — `remove`/`toggle` mutation 后 invalidate | 价值：**正面样本**
- **[交互]** 位置：`douyin-tracks.tsx:103-107` — `selectedTrack`/`editing`/`deleting` 三个弹窗状态 | 影响：弹窗风格与 `window.confirm` 页面并存

---

## 十三、赛道详情 `routes/_layout/douyin-tracks_.$trackId.tsx`（2205 行）

- **[BUG]** 位置：`douyin-tracks_.$trackId.tsx:126-131` — `keywordsQuery` 调 `listTrackKeywords` **无分页**，全量 | 影响：关键词上千时渲染卡顿
- **[BUG]** 位置：`douyin-tracks_.$trackId.tsx:132-137` — `creatorsQuery` **无分页** | 影响：同上
- **[数据]** 位置：`douyin-tracks_.$trackId.tsx:120-125/126-131/132-137` — 三个查询均 `refetchInterval: 10_000` | 影响：详情页 3 路 10 秒轮询叠加
- **[性能]** 位置：`douyin-tracks_.$trackId.tsx:240-269` — 关键词/达人筛选为**客户端 filter**，未 memo | 影响：每次渲染全量遍历
- **[性能]** 位置：`douyin-tracks_.$trackId.tsx:100-118` — `selectedKeywordIds` 用 `Set<string>` | 价值：**正面样本**（选择模型优于数组版）
- **[交互]** 位置：`douyin-tracks_.$trackId.tsx:254-260` — `allVisibleKeywordsSelected` / `someVisibleKeywordsSelected` 基于可见集计算 | 价值：**正面样本**（全选语义正确）
- **[交互]** 位置：`douyin-tracks_.$trackId.tsx` 批量删除关键词 — 使用 `window.confirm` | 影响：确认弹窗分裂
- **[产品]** 位置：`douyin-tracks_.$trackId.tsx:342-350` — Tabs 关键词/达人/任务/赛道设置；**达人列表无选择、无批量操作** | 影响：关键词与达人两个列表能力不对等
- **[数据]** 位置：`douyin-tracks_.$trackId.tsx:82-94` — `parseLibraryLines` 去重后 `slice(0, 100)` 上限 **100** | 影响：批量导入静默截断到 100 行（量化阈值）
- **[优化]** 位置：`douyin-tracks_.$trackId.tsx:195-237` — 三态：加载为普通 Card 文案（非骨架）、错误 Card 带重试 | 价值：**正面样本**（错误可重试）
- **[数据]** 位置：`douyin-tracks_.$trackId.tsx:138-151` — 刷新时 invalidate 六组 queryKey | 价值：**正面样本**
- **[交互]** 位置：`douyin-tracks_.$trackId.tsx:68-80` — `keywordStatusLabels` / `creatorStatusLabels` 状态字典 | 价值：可复用字典
- **[架构]** 位置：`douyin-tracks_.$trackId.tsx:100-118` — search/keywordFilter/selectedKeywordIds/searchCreators/addOpen 等全为本地 state | 影响：筛选不同步 URL

---

## 十四、关键词 `routes/_layout/douyin-keywords.tsx`（1245 行）

- **[数据]** 位置：`douyin-keywords.tsx:78` — `const pageSize = 50` | 价值：量化常量
- **[数据]** 位置：`douyin-keywords.tsx:119-145` — 主查询 `skip: page*pageSize, limit: pageSize`、`placeholderData`、`enabled: Boolean(trackId)`、`refetchInterval: 5_000` | 价值：分页正确 + 轮询固定
- **[性能]** 位置：`douyin-keywords.tsx:146-155` — `overviewQuery` `limit: 500` + `refetchInterval: 10_000`，与列表数据重复 | 影响：指标与列表双份请求（同 creators 页同病）
- **[性能]** 位置：`douyin-keywords.tsx:158-168` — `metrics` 基于 500 条上限全量 `useMemo` | 影响：超过 500 条后指标本身即不准
- **[数据]** 位置：`douyin-keywords.tsx:89-97` — `selected` 为 `string[]` 数组；`viewMode = usePersistentViewMode("douyin-keywords-view")` | 影响：选择模型不统一（数组）+ 视图持久化正确
- **[交互]** 位置：`douyin-keywords.tsx:169-171,393-404` — `pageIds` / `allPageSelected`，全选仅在当前页范围内 | 影响：跨页全选语义不明（与 library 同病）
- **[交互]** 位置：`douyin-keywords.tsx:260-272` — 批量删除用 `window.confirm` | 影响：确认弹窗分裂
- **[交互]** 位置：`douyin-keywords.tsx:219-384` — 顶部区堆叠统计 + 同步历史任务 + 创建 + 批量任务 + 批量删除 + 搜索/赛道/分类/状态/启用/排序 6 个筛选器 + "已选 N" + ViewModeToggle | 影响：单人维护的巨型工具栏，筛选全部本地 state 不同步 URL
- **[优化]** 位置：`douyin-keywords.tsx:172-177` — 两组 invalidate | 价值：**正面样本**
- **[数据]** 位置：`douyin-keywords.tsx:79-84` — `statusLabels` 状态字典 | 价值：可复用字典

---

## 十五、用户列表 `routes/_layout/admin.tsx`（73 行）

- **[数据]** 位置：`admin.tsx:14-19` — `readUsers({ skip: 0, limit: 100 })` + `useSuspenseQuery`，queryKey `["users"]` | 影响：**硬编码 `limit: 100` 且无分页 / 无搜索 / 无排序**
- **[数据]** 位置：`admin.tsx:40-50` — `UsersTableContent` 直接把全量数据交给通用 `DataTable`，靠其客户端分页兜底 | 影响：>100 用户不可见
- **[安全]** 位置：`admin.tsx:21-38` — `beforeLoad` 中 `readUserMe` 判断 `is_superuser`，否则 `redirect("/")` | 价值：**正面样本**（路由级鉴权）
- **[优化]** 位置：`admin.tsx:52-58` — Suspense fallback `PendingUsers` | 价值：**正面样本**（用 Suspense 兜加载态，替代手写 isLoading）

---

## 十六、items 列表 `routes/_layout/items.tsx`（70 行）

- **[数据]** 位置：`items.tsx:13-18` — `readItems({ skip: 0, limit: 100 })`，queryKey `["items"]` | 影响：**硬编码 `limit: 100` 且无分页 / 无搜索 / 无排序**
- **[优化]** 位置：`items.tsx:31-47` — `ItemsTableContent` 在数据为空时渲染"暂无条目"空态，否则用 `DataTable` | 价值：**正面样本**（空态显式处理）
- **[结论]** 位置：`admin.tsx` + `items.tsx` — 是通用 `DataTable` 的**全部两个消费者** | 影响：通用组件复用率极低，绝大部分列表为手写 Table

---

## 十七、通用表格 `components/Common/DataTable.tsx`（194 行）

- **[架构]** 位置：`DataTable.tsx:32-35` — `DataTableProps` 仅 `columns` + `data` 两个参数 | 影响：**无 loading / error / 分页受控 / 排序 / 选择 / 列可见性** 等任何接口
- **[架构]** 位置：`DataTable.tsx:41-46` — `useReactTable` 仅注册 `getCoreRowModel` + `getPaginationRowModel` | 影响：TanStack Table 的 sorting / rowSelection / columnVisibility / filtering / 服务端分页能力**全部未启用**
- **[i18n]** 位置：`DataTable.tsx:80-88` — 空态硬编码英文 `"No results found."` | 影响：与中文界面混排
- **[i18n]** 位置：`DataTable.tsx:93-191` — 分页条硬编码英文 `"Showing X to Y of Z entries"` / `"Rows per page"` / `"Page x of y"`，`sr-only` 文案 `"Go to first page"` 等 | 影响：i18n 缺失（横切关注⑩）在通用组件层面就已固化
- **[数据]** 位置：`DataTable.tsx:93-191` — 分页条仅在 `getPageCount() > 1` 时渲染；每页大小选项 `[5, 10, 25, 50]` | 价值：量化参数
- **[数据]** 位置：`DataTable.tsx:37-46` — **无 loading 态**（依赖外部 Suspense） | 影响：非 Suspense 场景下必然出现空表闪烁
- **[结论]** 位置：`DataTable.tsx` 整体 — 只有 `admin.tsx` / `items.tsx` 两个消费者 | 影响：本该统一的列表底座事实上被架空，导致 20 个页面各写分页/三态/选择

---

## 十八、组件内列表 `components/Douyin/TaskResults.tsx`（313 行）

- **[数据]** 位置：`TaskResults.tsx:18` — `const pageSize = 20` | 价值：量化常量
- **[优化]** 位置：`TaskResults.tsx:32-67` — 三个查询（作品/评论/点赞收藏）各自 `enabled: tab === "x"` + `placeholderData` + `refetchInterval: active ? 3_000 : false` | 价值：**正面样本**（终态停止 + 切 Tab 才请求）
- **[数据]** 位置：`TaskResults.tsx:27-30` — `awemePage` / `commentPage` / `actionPage` **三个独立分页状态**，各 Tab 记忆自己的页码 | 价值：**正面样本**（分页状态隔离）
- **[数据]** 位置：`TaskResults.tsx:265-300` — `Pager` 在 `count <= pageSize` 时返回 `null` | 价值：**正面样本**（单页不显示分页条）
- **[产品]** 位置：`TaskResults.tsx:92-149 / 174-196 / 219-238` — 三张表（作品 7 列 / 评论 6 列 / 点赞收藏 4 列）均**无选择、无排序、无搜索** | 影响：组件内列表普遍缺筛选能力
- **[数据]** 位置：`TaskResults.tsx:252-263` — `EmptyRow` | 价值：空态复用组件
- **[风格]** 位置：`TaskResults.tsx:302-313` — `formatDate` / `formatUnixDate` | 影响：重复实现之一

---

## 十九、组件内列表 `components/Douyin/UnifiedWorksPanel.tsx`（约 1479 行，本区间读取开头）

- **[数据]** 位置：`UnifiedWorksPanel.tsx:76` — `const pageSize = 20` | 价值：量化常量
- **[性能]** 位置：`UnifiedWorksPanel.tsx:141` — `refetchInterval: active ? 2_000 : 5_000` | 影响：**非激活时仍每 5 秒轮询**，是本区间最典型的"轮询不彻底"（P1-1 中"每行 ≈260 hook + 2s 轮询"的来源）
- **[数据]** 位置：`UnifiedWorksPanel.tsx:150` — 标签查询 `limit: 500` | 价值：量化 limit
- **[性能]** 位置：`UnifiedWorksPanel.tsx`（P1-1） — 每行约 **260 个 hook 实例** | 影响：单行成本极高，大列表不可用
- **[性能]** 位置：`UnifiedWorksPanel.tsx` 选择 — `selected` 为 `string[]` + `includes` | 影响：O(n) 判选（P1-2 实例）
- **[性能]** 位置：`UnifiedWorksPanel.tsx` 重采评论 — 串行循环创建任务 | 影响：与 `douyin-library.tsx:329-365` 同模式（**（重复 2 次）**）
