# H-列表缺陷-2（数据列表架构专题 · 中段）

> 覆盖原文第 58000–68000 行。
> **重要说明**：第 62838 行附近（`# 一` 标题）至第 68000 行区间，导出内容被严重损坏——`> 💭 **思考过程**` 引用块与正文交替出现、句子被逐字拆散（如 `douyin819-key行`、`-inter/actions`、`jiao`、`12 维度缺陷矩阵` 被打断）。本文件已尽力从碎片中还原本轮分析的全部实质信息，凡属碎片拼接处均标注"（原文此处损坏，已尽力还原）"。
> 分析统一采用同一套 **12 维度**判定框架：1 分页 / 2 筛选架构 / 3 排序 / 4 选择·批量 / 5 视图切换 / 6 三态完整性 / 7 数据刷新 / 8 组件复用 / 9 性能 / 10 可访问性 / 11 响应式 / 12 key 稳定性。

---

## 一、本区间读取（被分析）的文件清单与行数（数据）

- **[数据]** `src/routes/_layout/douyin.tsx`（任务列表）— 共 1627 行；其 Pager 组件位于第 1556–1588 行；尾部工具函数 `taskLabel` / `formatUnix` / `formatDateTimeText` / `subtitleStatusLabel` / `compact`。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-comments.tsx`（评论列表）— 共 1206 行；尾部包含导出对话框（973–1060 行）、`txtCell`、`countActiveFilters`、`activeFilterSummary`、`dateTimestamp`、`downloadSelectedComments`、`browserApiBase`。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-creators.tsx`（达人列表）— 共 1101 行；尾部含 `EditCreatorDialog` 剩余部分、`Field`、`Check`、`parseCreatorTargets`。 | 分析基数
- **[数据]** `src/components/Douyin/UnifiedWorksPanel.tsx`（任务详情统一作品/下载/字幕面板）— 共 1479 行。 | 分析基数
- **[数据]** `src/components/Douyin/MediaTaskManagement.tsx`（媒体任务管理）— 共 571 行。 | 分析基数
- **[数据]** `src/components/Douyin/MediaPipelinePanel.tsx`（任务详情媒体流水线）— 共 487 行。 | 分析基数
- **[数据]** `src/components/Douyin/TaskInteractionsPanel.tsx`（任务详情互动面板）— 共 236 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-tracks.tsx`（赛道列表）— 共 1936 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-tracks_.$trackId.tsx`（赛道详情：关键词/达人列表）— 共 2205 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-keywords.tsx`（关键词列表）— 共 1245 行；`Pager` 位于 1197–1231 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-interactions.tsx`（互动任务列表）— 共 753 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-accounts.tsx`（账号池列表）— 共 934 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-request-logs.tsx`（请求日志列表）— 共 606 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-tags.tsx`（标签列表）— 共 399 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-browsers.tsx`（浏览器槽位列表）— 共 851 行。 | 分析基数
- **[数据]** `src/routes/_layout/douyin-library.tsx`（视频资源库）— 服务端分页 pageSize=32（第 84 行）。 | 分析基数

---

## 二、任务列表 `douyin.tsx`（12 维度判定）

- **[BUG]** `douyin.tsx:123-124` — `DouyinService.listTasks({ ..., skip: 0, limit: 100 })` 硬编码 `skip: 0, limit: 100`，**无分页 UI、无 page state、无上一页/下一页**，一次性加载 100 条，超过 100 条的数据无法查看。 | 判定：**严重缺陷**
- **[性能]** `douyin.tsx:127` — `refetchInterval: 3_000`（3 秒轮询）无终态停止条件；判定为"基本合理但缺少 `refetchIntervalInBackground: false`"（原文同时出现"已设置 / 未设置"两种表述，损坏处无法确认）。 | 后台标签页仍在打请求
- **[性能]** `douyin.tsx:126` — `retry: false`，请求失败不自动重试。 | 失败态需手动重试
- **[交互]** `douyin.tsx:109-112` — 筛选状态全部用 `useState`（statusFilter / searchTerm / trackId / sourceValue），**未同步 URL**（无 `validateSearch`）。 | 刷新、分享、后退均丢失筛选
- **[交互]** `douyin.tsx` 搜索框 `searchTerm` — onChange 即时应用，**无防抖**。 | 每次按键触发请求
- **[优化]** `douyin.tsx` 排序 — **无排序功能**。 | 判定：缺陷
- **[结论]** `douyin.tsx:114` — 选择模型 `selectedTaskIds` 用 `Set<string>`。 | 判定：通过（选择模型正确）
- **[产品]** `douyin.tsx:170-179` — 全选 `toggleAllSelectableTasks` 基于 `selectableTasks`，即**仅当前筛选页全选，非服务端全量**。 | 全选语义需明确提示
- **[交互]** `douyin.tsx:851` — 批量删除使用 `window.confirm`，无组件级二次确认对话框；`onDeleted` 后清空选择（第 837 行）。 | 判定：缺陷（原生 confirm 不可定制、不可 A11Y 化）
- **[优化]** `douyin.tsx:113` — 视图切换用统一的 `usePersistentViewMode("douyin-tasks-view")`。 | 判定：通过（唯一正确示例）
- **[BUG]** `douyin.tsx` 三态 — 空态（446–455 行）、加载态（451–452 行）、错误态用 `isError` + `QueryErrorState` 重试。 | 判定：通过，但加载态**非骨架屏**
- **[性能]** `douyin.tsx:315-322` — `refetchInterval` 在后台标签页也会跑；空态 `EmptyState` 位于 322 行附近。（损坏处，结合上文归并） | 资源浪费
- **[架构]** `douyin.tsx:489-571` — **手写 `Table`**，未复用 shadcn 通用 `DataTable`。 | 判定：缺陷（无列可见性、列排序、行选择等基础能力）
- **[性能]** `douyin.tsx:135` — `filteredTasks` 用 `useMemo`（好）；但 `limit: 100` 全量渲染、**无虚拟滚动**。 | 100 行尚可，无保护上限
- **[A11Y]** `douyin.tsx:622` — 操作按钮有 `aria-label`；搜索框有 `label`（270 行附近）。 | 判定：通过
- **[风格]** `douyin.tsx` 响应式 — table 外层 `overflow-x-auto`。 | 判定：通过
- **[结论]** `douyin.tsx:336,347,396` — 行 key 用 `task.id`。 | 判定：通过（稳定）

---

## 三、互动任务列表 `douyin-interactions.tsx`（753 行）

- **[BUG]** `douyin-interactions.tsx:101` — `limit: 100` 硬编码，**无分页 UI（无上一页/下一页）、无 skip 状态**；仅 `retryAll` 内部做了分页拉取（159–171 行）。 | 判定：**严重缺陷**（数据超 100 条无法翻页）
- **[交互]** `douyin-interactions.tsx:79-82` — 筛选状态用 `useState`，**未同步 URL**；即时筛选（onChange 直接 setState），**无防抖**。 | 刷新丢失
- **[优化]** 排序 — **无排序功能（表头不可点击）**。 | 判定：缺陷
- **[产品]** 选择/批量 — **无行级单选/多选/全选**；"重试全部可重试项"（156–202 行）走**服务端全量遍历**而非基于本地选择（109–112 行）。 | 判定：缺陷（无法对已选子集操作）
- **[交互]** 搜索 `searchTerm` — onChange 即时应用，**无防抖**。 | 每次按键触发请求
- **[优化]** 视图切换 — cards / rows / table 三视图。 | 判定：缺陷（功能单一）
- **[风格]** 三态 — 空态（446–455 行有"暂无互动任务，可从视频或评论操作中创建"）、加载态（451–452 行"加载互动任务..."）、**错误态缺失（无 `isError` 处理、无重试按钮）**。 | 判定：**严重缺陷**
- **[BUG]** 批量操作 — `window.confirm` 二次确认（851 行附近）、`onDeleted` 后清选（837 行附近）。（该组行号在损坏区与其它文件混排，归属存疑，原文此处损坏，已尽力还原） | 判定：严重缺陷
- **[性能]** 数据刷新 — `refetchInterval`（103–108 行）活动任务 2 秒、否则 5 秒；**在后台也会跑，无 `refetchIntervalInBackground: false`**。 | 后台空转
- **[性能]** 列表**无虚拟滚动**，`limit: 100`。 | 判定：缺陷（无上限保护）
- **[架构]** 列表定义**内联在页面组件**，未复用统一表格组件。 | 判定：缺陷
- **[产品]** 三态 — 基本合理，但**加载态缺骨架屏**。 | 判定：缺陷
- **[A11Y]** 操作按钮有 `aria-label`（622 行附近）。 | 判定：通过

---

## 四、评论列表 `douyin-comments.tsx`（1206 行）

- **[交互]** `douyin-comments.tsx:80,131,179` — `pageSize = 50` 常量；page state 在 131 行；服务端分页 `skip: page * pageSize` 在 179 行。 | 分页能力完整
- **[产品]** 分页 UI — 只有上一页/下一页，**无页码跳转、无每页大小切换**。 | 判定：通过（基本 OK）
- **[架构]** `douyin-comments.tsx` — **无 URL 同步（无 `validateSearch`）**。 | 刷新/分享丢失筛选与页码
- **[产品]** `douyin-comments.tsx:129-130,199` — draft / filters **双状态**（draft 模式），`applyFilters`（199 行）**点击"查询"才应用**。 | 交互合理
- **[交互]** `douyin-comments.tsx:317` — 搜索 `commentContent` 走 draft，onKeyDown Enter 才应用。 | **无防抖**，但 draft 模式使其可接受
- **[优化]** `douyin-comments.tsx:177-178,499-509` — 服务端排序，5 个排序字段。 | 判定：通过
- **[结论]** `douyin-comments.tsx:132` — `selected` 用 `Set<string>`。 | 判定：通过
- **[产品]** `douyin-comments.tsx:195-196` — 全选 `allVisibleSelected` 基于 `visibleIds`（**本页全选**）。 | 全选语义受限
- **[产品]** 批量导出 `exportSelected`（229 行）— 导出**无二次确认**。 | 危险操作无确认
- **[交互]** `douyin-comments.tsx:135` — 视图切换用 `usePersistentViewMode("douyin-comments-view")`（统一 hook，table / cards / rows）。 | 判定：通过
- **[BUG]** 三态 — 视图分支用 `rows.length` 与 `viewMode !== "table"` 判断（79 行附近）；table 视图空态+加载态**合并**（323–333 行），cards/rows 视图同样合并（360–365 行）；**完全无 `isError` 处理、无重试按钮**（638–640 行）。 | 判定：**严重缺陷**（接口失败时静默显示"无数据"，误导用户）
- **[性能]** 数据刷新 — **无轮询（合理）**，有手动刷新（125–135 行）与 `invalidate`。 | 判定：通过
- **[性能]** `douyin-comments.tsx:184-192` — `rows` 用 `useMemo` 去重。 | 判定：通过
- **[性能]** `taskMap` — **每次 render 重建 Map**（未 memo），key 用 `item.comment.id`（原文此处损坏，行号为 279 行附近）。 | O(n) 重复计算
- **[性能]** 列表**无虚拟滚动**（pageSize 50，尚可接受）。 | 判定：缺陷
- **[A11Y]** `douyin-comments.tsx:595,697,817` — `Checkbox` 有 `aria-label`；**搜索框无 `aria-label`**。 | 判定：缺陷
- **[风格]** 响应式 — table 外层 `overflow-x-auto`（588–589 行）。 | 判定：通过
- **[结论]** key 用 `item.comment.id` / `item.id`。 | 判定：通过（稳定）
- **[架构]** **手写 `Table`**（589 行），未复用通用 `DataTable`。 | 判定：缺陷
- **[数据]** `douyin-comments.tsx:973-1021` — 导出筛选结果 TXT：先 `listCommentLibrary({...request, skip: 0})` 取 `total = first.count`，`total > 1000` 时 `window.confirm` 二次确认，随后 `while (items.length < total)` **循环全量拉取**，输出 `﻿` BOM + `\r\n`，字段取 `commentExportFields`。 | 超过 1000 条会长时间阻塞，且为串行全量拉取
- **[数据]** `douyin-comments.tsx:1168-1199` — `downloadSelectedComments` 直接 `fetch` `${browserApiBase()}/api/v1/douyin/comments/export`，手动读 `localStorage.access_token` 拼 `Authorization`，解析 `Content-Disposition`（`filename*=UTF-8''` 与 `filename=`）拿文件名。 | 与 `@/client` 生成的 OpenAPI client 双轨，鉴权与 baseURL 逻辑重复
- **[风格]** `douyin-comments.tsx:1154-1162` — 又一份本地 `formatDate`（`Intl.DateTimeFormat("zh-CN", {year,month,day,hour,minute})`）。 | 与其它页面各自重复实现

---

## 五、视频资源库 `douyin-library.tsx`

- **[数据]** `douyin-library.tsx:84` — `pageSize = 32` 常量。 | 分页基数
- **[交互]** `douyin-library.tsx:260` — 服务端分页 `skip: page*pageSize`。 | 判定：通过
- **[产品]** 分页 UI — `Pager` 只有上一页/下一页（130、136 行附近），**无页码跳转、无每页大小切换**。 | 判定：缺陷
- **[BUG]** `douyin-library.tsx:127,159-187` — `validateSearch` 读取 URL（127 行），但实际状态由 `useState` 从 routeSearch 初始化，**之后变更不写回 URL**——"初始化读 URL 但变更不同步 URL"。 | 判定：**严重缺陷（URL 单向同步）**
- **[交互]** `douyin-library.tsx:243-253` — search 即时应用，**无防抖**。 | 每次按键触发请求
- **[风格]** 筛选 — 多筛选组合（taskId / sourceValue / search / creatorHash / tagId / storageBackend / subtitleStatus / downloadStatus / sort，Key 见 233–246 行）与路由 URL 配合，覆盖较充足。 | 判定：缺陷（仅 URL 单向）
- **[优化]** `douyin-library.tsx:256-259` — 服务端排序 `sortBy` / `sortOrder`，字段保持（原文行号被损坏为 684/777，存疑）。 | 判定：通过
- **[结论]** `douyin-library.tsx:279-280` — `selectedAwemeIds: string[]`（279 行）+ `selectedAwemeQuerySet = useMemo(new Set(...))`（280 行）：**Array 存储但派生 Set，查找 O(1)，但 toggle 用 `includes` O(n)**。 | 判定：缺陷（半优化）
- **[产品]** `douyin-library.tsx:490-492` — 全选 `togglePageSelection` 为本页全选。 | 全选语义受限
- **[产品]** 批量创建评论 — `>20` 才 `window.confirm`（819 行附近）；`onCreated` 后清选（833 行附近）。 | 阈值式确认
- **[性能]** `douyin-library.tsx` 视图三套独立实现：`VideoTable` / `VideoCard` / `VideoRow`（1543 行附近）；操作列逻辑重复（`WorkActionButtons` vs `VideoTable` 内联，1487 行附近）。 | 判定：缺陷（三套派生计算未 memo）
- **[结论]** `VideoPendingIds` / `verifyPendingIds` 用 `Set`。 | 判定：通过（选择模型正确）
- **[性能]** 列表**无虚拟滚动**（容器内滚动 max-h ≈ 60px 常量区，原文行号损坏）。 | 判定：缺陷
- **[数据]** 刷新 — `refetchInterval: 10_000`，`invalidate` 无多 queryKey（69 行附近）。 | 通过
- **[A11Y]** `aria-label` 有（378、717 行附近）；选择项 `aria-label`（296 行附近）。 | 判定：通过
- **[风格]** 响应式 — `overflow-x-auto`，table 隐藏列用 `hidden xl:table-cell` 策略（原文行号损坏）。 | 判定：通过
- **[结论]** key 用 `row.id` / `item.id`。 | 判定：通过
- **[架构]** **手写 `Table`**，未复用 `DataTable`。 | 判定：缺陷

---

## 六、达人列表 `douyin-creators.tsx`（1101 行）

- **[数据]** `douyin-creators.tsx:75` — 客户端分页，pageSize 相关常量（原文数值被损坏为 200/10/25/50 混排，存疑）。 | 分页基数
- **[产品]** 分页 UI — 有上一页/下一页按钮（257–348 行附近），**无页码跳转**。 | 判定：通过
- **[BUG]** 空态 — 文案为英文 `"No results found."`（原文残留），**中文项目出现英文空态**。 | i18n 不一致
- **[交互]** `douyin-creators.tsx:27-28` — search 即时应用（onChange），**无防抖**；queryKey 含 `search`（61 行）。 | 每次按键触发请求
- **[优化]** 排序 — 服务端排序 `sortBy` / `sortOrder` 传入（原文行号损坏）。 | 判定：通过（有服务端能力）
- **[结论]** `selected` 用 `Set`（原文残留 `selected.includes` 判为通过）。 | 判定：通过
- **[产品]** 选择/批量 — 无全选；批量删除 + 清选（原文行号损坏）。 | 判定：缺陷
- **[交互]** 视图切换 — 有 `ViewModeToggle` + `usePersistentViewMode`（`creator-view`），table / cards 视图。 | 判定：功能基本对等
- **[性能]** 数据刷新 — `refetchInterval: 10_000`（122 行附近）与 `15_000`（133 行附近）两处，**无终态停止、无 `refetchIntervalInBackground: false`**。 | 后台空转
- **[性能]** `metrics` 用 `useMemo`（108–114 行附近）；`overview` 额外查询，**多拉 500 条**（202–213 行附近）。 | 重复查询浪费
- **[性能]** `rows.reduce` **两次**（102、106 行附近），每次渲染重算。 | 判定：缺陷
- **[风格]** 三态 — 有 empty + loading，**无 `isError` 处理、无重试**。 | 判定：缺陷
- **[结论]** key 用 `creator.id`（372 行附近）。 | 判定：通过
- **[A11Y]** `Checkbox aria-label`（446 行附近）；`TrackSelect` `ariaLabel`（131 行附近）；搜索框有 `aria-label`。 | 判定：通过
- **[风格]** 响应式 — 用 `overflow-x-auto`。 | 判定：通过
- **[架构]** **手写 `Table`**，未复用 `DataTable`。 | 判定：缺陷

---

## 七、关键词列表 `douyin-keywords.tsx`（1245 行）

- **[数据]** `douyin-keywords.tsx:478,89,139` — 服务端分页，pageSize 常量（478 行附近）、page state（89 行附近）、`skip: page*pageSize`（139 行附近）。 | 分页基数
- **[产品]** `douyin-keywords.tsx:1197-1231` — `Pager` 组件**只有上一页/下一页**，无页码跳转、无每页大小切换。 | 判定：通过（基本 OK）
- **[BUG]** 筛选 **未同步 URL**（原文明确判定"URL 未同步"）。 | 刷新/分享丢失筛选
- **[交互]** `douyin-keywords.tsx:243-253` — trackId / search / category / enabled 多维筛选**即时应用，无防抖**。 | 每次按键触发请求
- **[优化]** 排序 — 服务端排序，含排序字段下拉。 | 判定：通过
- **[结论]** `selected` 用 `string[]` + `Array.includes` **O(n)**；全选范围是 `visibleKeywords`（254–260 行附近）。 | 判定：缺陷（选择模型 O(n)）
- **[产品]** 批量操作 — `BatchTaskDialog`（765 行）按 `keywordIds` 批量创建任务，展示"已选关键词会在该赛道内创建任务，不允许跨赛道混合运行"。 | 判定：通过
- **[BUG]** 删除确认 — `DeleteKeywordDialog` 用自定义 Dialog（1131–1145 行），但列表另有 `window.confirm` 路径（原文残留）；批量后清选失败时不回滚（原文行号损坏）。 | 判定：缺陷
- **[交互]** 视图切换 — table / cards / rows 三视图；`KeywordPreview` 接收 `viewMode`（552–621 行）；**cards/rows 视图有独立空态（516–540 行），table 视图空态单独（494–503 行）**。 | 判定：通过
- **[BUG]** 三态 — table 视图空态与加载态**合并**（494–503 行"正在加载关键词… / 没有符合筛选条件的关键词"），**无独立错误态、无重试按钮**。 | 判定：**严重缺陷**
- **[性能]** `rows` 用 `useMemo`；**无虚拟滚动**。 | 判定：缺陷
- **[A11Y]** `Checkbox aria-label`（578–580 行）；**搜索框无 `aria-label`**（仅有 placeholder）。 | 判定：缺陷
- **[风格]** 响应式 — table 外层 `overflow-x-auto`（原文行号损坏）。 | 判定：通过
- **[结论]** key 用 `item.id`。 | 判定：通过
- **[架构]** **手写 `Table`（506 行附近）+ 页面级 `Pager` 函数**，未复用通用 `DataTable`。 | 判定：缺陷
- **[风格]** `douyin-keywords.tsx:1197-1231` — 与 `douyin.tsx:1556-1588` **两份几乎相同的 `Pager` 实现**。 | 重复代码
- **[风格]** `douyin-keywords.tsx:1238-1245` — 又一份本地 `formatDate`（含"从未"空值文案）。 | 重复实现
- **[优化]** `douyin-keywords.tsx:623-627` 与 `douyin-tracks_.$trackId.tsx:501-503` — `formatKeywordNotes` 逻辑（`赛道：` 前缀 → "历史备注（不代表当前归属）"）**在两处重复**。 | 重复实现

---

## 八、赛道列表 `douyin-tracks.tsx`（1936 行）

- **[BUG]** `douyin-tracks.tsx` — **无分页**（`DouyinTracksService.listTracks({ search })` 一次性拉全量）。 | 判定：**严重缺陷**
- **[交互]** `douyin-tracks.tsx:27-28` — `searchTerm` 即时应用，**无防抖**；queryKey 含 `search`（61 行）。 | 每次按键触发请求
- **[优化]** 排序 — **无排序**。 | 判定：缺陷
- **[产品]** 选择/批量 — **无选择、无批量操作**（维度缺失）。 | 严重缺陷
- **[交互]** 视图切换 — **无视图切换，只有 table 视图**（用 `Card` 模拟）。 | 判定：缺陷
- **[风格]** 三态 — 有 `isLoading` / `isError`（392 行附近）/ 空态（475–485 行"还没有运营赛道"）。 | 判定：通过
- **[性能]** `douyin-tracks.tsx:102,106` — `rows.reduce` **调用两次**，每次渲染重算，**未 memo**。 | 判定：缺陷
- **[性能]** `douyin-tracks.tsx:108-114` — `metrics` 用 `useMemo`（好）；但**无虚拟滚动**。 | 判定：缺陷
- **[性能]** `douyin-tracks.tsx:113` — `refetchInterval` 10s，**终态未停止**。 | 判定：严重缺陷
- **[性能]** `douyin-tracks.tsx:140` — `invalidate(["douyin-tracks"])` 正确。 | 判定：通过
- **[A11Y]** `TrackSelect` `ariaLabel`（131 行附近）；操作按钮有 `aria-label`。 | 判定：通过
- **[风格]** 响应式 — table 视图**非 `overflow-x-auto`**，赛道列多时可能截断。 | 判定：缺陷
- **[结论]** key 用 `track.id`（272 行）。 | 判定：通过
- **[架构]** **手写 `Table`（430 行附近）**，未复用 `DataTable`。 | 判定：缺陷
- **[交互]** `douyin-tracks.tsx:475-485` — 空态含 `Target` 图标 + 引导文案"从一个细分市场和一组用户搜索词开始。" | 产品引导做得好
- **[交互]** `douyin-tracks.tsx:509-533` — 删除赛道用自定义 `Dialog` 二次确认，文案明确列出连带删除范围（关键词/达人/任务/视频/评论/互动/请求日志），"此操作无法撤销"。 | 判定：通过
- **[交互]** `douyin-tracks.tsx:444-452` — 卡片"运营这个赛道"按钮在 `!track.enabled` 时禁用，并给出 `title="请先启用赛道再启动采集"`。 | 判定：通过
- **[交互]** `douyin-tracks.tsx:487-500` — `TrackWorkspaceDialog` 支持 `search.run` 深链打开（关闭时 `navigate({ search: { run: undefined }, replace: true })`）。 | URL 联动示例

---

## 九、赛道详情 `douyin-tracks_.$trackId.tsx`（2205 行）

- **[BUG]** 关键词列表 / 达人列表 — **无分页**，`listTrackKeywords` / `listTrackCreators` 一次性全量拉取（master-detail 固定布局）。 | 判定：缺陷（维度缺失）
- **[优化]** 排序 — **无排序**。 | 判定：缺陷
- **[结论]** `douyin-tracks_.$trackId.tsx:104-106` — 关键词选择用 `Set<string>`。 | 判定：通过
- **[产品]** `douyin-tracks_.$trackId.tsx:254-260` — 全选范围是 `visibleKeywords`（当前筛选结果），表头 `Checkbox` 带 `aria-label="选择当前筛选结果中的全部关键词"`。 | 全选语义有明确文案
- **[产品]** 达人列表 — **无批量选择**。 | 判定：缺陷
- **[交互]** 视图切换 — **无视图切换**（master-detail 固定）。 | 判定：缺陷
- **[风格]** 三态 — `QueryErrorState` 带重试（关键词 437–444 行、达人 606–613 行）、`isLoading`、空数据态（553–568 行 / 699–712 行，且空态文案随"搜索中 / 筛选条件 / 无数据"三态变化）。 | 判定：通过
- **[性能]** `douyin-tracks_.$trackId.tsx:437-444` — `retry: false`，失败不自动重试。 | 需手动重试
- **[性能]** 关键词/达人筛选 `filter` **每次 render 重算**（未 memo）；**无虚拟滚动**；容器内滚动 `max-h-[560px] overflow-auto`（446 行）与 `max-h-[560px] overflow-auto`（615 行）。 | 判定：缺陷
- **[性能]** 数据刷新 — 详情页 `refetchInterval` 未在原文残留片段中确认；`retry: false` 已确认。 | （原文此处损坏，已尽力还原）
- **[A11Y]** `Checkbox aria-label`（451 行"选择当前筛选结果中的全部关键词"、482 行 `选择关键词 ${keyword.keyword}`）；达人搜索框 `aria-label="筛选当前赛道达人"`（602 行）；关键词/达人编辑按钮有 `aria-label`（528、537、674、683 行）。 | 判定：通过（本项目 A11Y 最完整处）
- **[风格]** 响应式 — table 外层 `overflow-auto` + `TableHeader` `sticky top-0`（448 行 / 617 行）。 | 判定：通过
- **[结论]** key 用 `keyword.id` / `creator.id`。 | 判定：通过
- **[架构]** **手写 `Table`（447 行 / 616 行）**，未复用 `DataTable`。 | 判定：缺陷
- **[产品]** `douyin-tracks_.$trackId.tsx:723-731` — 设置 Tab 用 `forceMount` + `data-[state=inactive]:hidden` 保留状态。 | 判定：通过
- **[交互]** `douyin-tracks_.$trackId.tsx:734-764` — 重置赛道 Dialog 明确列出将被清理的数量（关键词数 / 达人数 / 任务数 / 作品数 / 评论数）。 | 判定：通过
- **[产品]** `douyin-tracks_.$trackId.tsx:684-694` — 默认赛道的达人"移除"按钮禁用，`title` 提示"默认赛道的达人不能移除，请将它移动到其他赛道"。 | 判定：通过
- **[数据]** `douyin-tracks_.$trackId.tsx:661-666,608-611,718-723` — 赛道详情一次性拉取 `listAccounts({limit:100})`、`listPools()`、`listTrackKeywords`、`listTrackCreators`。 | 多个 limit 硬编码

---

## 十、账号池列表 `douyin-accounts.tsx`（934 行）〔原文此处损坏，已尽力还原〕

- **[BUG]** `douyin-accounts.tsx:92` — `limit: 100` 硬编码，**无分页 UI（未见 Pager 组件渲染）**。 | 判定：**严重缺陷**（超过 100 条无法查看）
- **[数据]** `douyin-accounts.tsx:84,159,260,137` — `pageSize` 常量（84 行）、page state（159 行）、服务端分页 `skip: page*pageSize`（260 行）、`skip`（137 行）。 | 与分页缺陷并存的半成品分页
- **[BUG]** `douyin-accounts.tsx:127-147` — `validateSearch` 读取 URL；但状态由 `useState` 初始化自 `routeSearch`，**之后变更不写回 URL**（单向同步）。 | 判定：**严重缺陷（架构）**
- **[优化]** 筛选 — **无筛选**（账号列表无搜索/筛选）；`Tabs` 切换账号/池（231 行）不是筛选。 | 判定：缺陷
- **[优化]** 排序 — **无排序，表头不可点击**（原文残留 `L25x`）。 | 判定：缺陷
- **[结论]** `douyin-accounts.tsx:807` — `selected` 用 `useState<string[]>([])`，配合 `Array.includes` 查找 **O(n)**。 | 判定：缺陷
- **[产品]** 选择/批量 — `CreatePoolDialog` 中 `Checkbox` 多选（863–871 行）；**无全选**；账号无批量操作。 | 判定：缺陷
- **[交互]** 视图切换 — `ViewModeToggle`（245 行，table / cards / rows）+ `usePersistentViewMode` 持久化（89 行附近）。 | 判定：通过
- **[风格]** 三态 — `isError`（263 行）/ `isLoading`（275 行）/ 空（299 行）齐全 + `QueryErrorState` 重试（280 行）；**`pools` 分支：`isError`（463 行）、`isLoading`（470 行）有，但空态缺失**（474 行直接渲染，池为空时显示空白）。 | 判定：缺陷
- **[性能]** `douyin-accounts.tsx:264,94-181` — `refetchInterval: 5_000` **固定轮询，无终态停止，无 `refetchIntervalInBackground: false`**。 | 判定：缺陷（资源浪费）
- **[性能]** 轮询门控缺失：仅当存在 `login_required` / `verifying` / `busy` 账号时才应 2s 轮询，全 ready 时应停止或降频至 30s；当前**只要页面在就持续轮询**。 | 判定：严重缺陷（体验与资源双差）
- **[性能]** `selectedAssets` 每次 filter **未 memo**；列表**无虚拟滚动**（limt 100）。 | 判定：缺陷
- **[结论]** 账号规模上 `selected` 用 `Array` 而非 `Set`，无法定位历史数据。 | 判定：缺陷
- **[A11Y]** `Checkbox`（无 `aria-label`？原文残留"Checkbox 无 label"判缺陷）；`aria-label` 有（378、717 行附近）；选择项 `aria-label`（296 行附近）。 | 判定：缺陷 / 部分通过
- **[风格]** 响应式 — `min-w-[900px]` + `overflow-x-auto`。 | 判定：通过
- **[结论]** key 用 `row.id`（850、876 行附近）。 | 判定：通过
- **[架构]** **手写 `Table`**，未复用 `DataTable`；错误态复用 `QueryErrorState`。 | 判定：缺陷
- **[数据]** 批量创建评论 — `>20` 才 `window.confirm`（819 行）；`onCreated` 清选（833 行）。 | 阈值式确认
- **[产品]** 账号池三个列表（账号 / 池 / 槽位）**均无分页 UI**。 | 严重缺陷

---

## 十一、浏览器槽位列表 `douyin-browsers.tsx`（851 行）〔原文此处损坏，已尽力还原〕

- **[结论]** `douyin-browsers.tsx` — 结构特殊：**槽位列表 + 详情（master-detail）**（列表 837 行区域，详情 765 行区域），不是传统表格。 | 架构差异说明
- **[结论]** 分页 — **无分页**（槽位数量少，合理）。 | 判定：通过
- **[结论]** 筛选 — `fieldset` / `legend`（sr-only）+ 按钮 `aria-pressed`（214–220 行）。 | 判定：通过
- **[优化]** 排序 — **无列排序（列被隐藏）**。 | 判定：缺陷
- **[结论]** 选择 — 单个槽位 `selectedName`，**无批量选择**（槽位少，合理）。 | 判定：通过
- **[结论]** 视图切换 — **无**（master-detail）。 | 判定：通过
- **[风格]** 三态 — `isLoading`（321–331 行）、`isError` + `QueryErrorState` 重试（315–321 行）、`EmptyState`（322–331 行）。 | 判定：通过
- **[性能]** `douyin-browsers.tsx:127` — `refetchInterval: 3_000`，**无终态停止**、**无 `refetchIntervalInBackground: false`**。 | 判定：缺陷
- **[性能]** `douyin-browsers.tsx:36` — `slots.find(...)` **每次 render 重算**。 | 判定：缺陷
- **[性能]** `douyin-browsers.tsx:281` — `useEffect` 依赖 `slots`，**每次轮询触发整页加载**（loading 态整体闪烁）。 | 判定：缺陷（体验很差）
- **[性能]** `invalidate` 两个 `queryKey`（原文残留），列表**无虚拟滚动**（容器内滚动），量少可接受。 | 判定：通过
- **[A11Y]** 通过（原文判定）。 | 判定：通过
- **[风格]** 响应式 — `overflow-x-auto`（195–203 行）。 | 判定：通过
- **[结论]** key 用 `slotKey(slot)`（88 行），**稳定**。 | 判定：通过
- **[架构]** **手写 `Table`**，未复用 `DataTable`。 | 判定：缺陷

---

## 十二、请求日志列表 `douyin-request-logs.tsx`（606 行）〔原文此处损坏，已尽力还原〕

- **[数据]** `douyin-request-logs.tsx:55` — `PAGE_SIZE = 50` 常量。 | 分页基数
- **[数据]** `douyin-request-logs.tsx:77,471` — 服务端分页 `skip` / `limit`。 | 判定：通过
- **[产品]** `douyin-request-logs.tsx:373-390` — 上一页/下一页；**无页码跳转，只有上下页**。 | 判定：通过（基本 OK）
- **[BUG]** 筛选 — `trackId` / `search` / `status` / `category` / `enabled` 多维筛选**即时应用，无防抖**，且**未同步 URL**。 | 判定：缺陷
- **[优化]** 排序 — 服务端排序。 | 判定：通过
- **[结论]** 选择 — 有勾选，`selected` **用 `string[]`**（原文残留"selected 用 string[]，严重缺陷"）。 | 判定：严重缺陷（同 O(n) 通病）
- **[产品]** 全选 — **只有逐行勾选，无全选功能**。 | 判定：缺陷
- **[结论]** 视图切换 — 有 `usePersistentViewMode`（table / cards / rows）。 | 判定：通过
- **[风格]** 三态 — 有 `isLoading` / `isError` / 空态（原文判定"齐全"）。 | 判定：通过
- **[性能]** 数据刷新 — **无轮询（合理）**，有手动刷新。 | 判定：通过
- **[性能]** 列表**无虚拟滚动**；`rows` 用 memo 化处理。 | 判定：缺陷 / 部分通过
- **[A11Y]** 有 `aria-label`；`Checkbox` 有 `aria-label`。 | 判定：通过
- **[风格]** 响应式 — 列隐藏 `hidden lg:table-cell` + `overflow-x-auto`。 | 判定：通过
- **[结论]** key 用 `row.id`。 | 判定：通过
- **[架构]** **手写 `Table`**，但已抽离 `RequestLogPreview`（407 行、576 行）。 | 判定：缺陷（抽离较好）

---

## 十三、标签列表 `douyin-tags.tsx`（399 行）〔原文此处损坏，已尽力还原〕

- **[数据]** `douyin-tags.tsx:34` — **客户端分页**，pageSize = 50 常量。 | 分页基数
- **[结论]** 排序 — **客户端排序**（原文判定通过）。 | 判定：通过
- **[BUG]** `douyin-tags.tsx:28` — 搜索**即时应用，无防抖**。 | 每次按键触发过滤
- **[结论]** `douyin-tags.tsx:91` — `selected` 用 `string[]` + `includes` **O(n)**。 | 判定：严重缺陷（选择模型 O(n)）
- **[产品]** 全选 — **无全选功能**。 | 判定：缺陷
- **[交互]** `douyin-tags.tsx:270` — 批量删除用 `window.confirm`（原生确认）。 | 判定：缺陷
- **[结论]** 视图切换 — `usePersistentViewMode`（table / cards / rows）。 | 判定：通过
- **[风格]** 三态 — 有（原文判定通过）。 | 判定：通过
- **[性能]** 数据刷新 — 无轮询。 | 判定：通过
- **[性能]** 无虚拟滚动（数量小，可接受）。 | 判定：通过
- **[架构]** **复用 `DataTable`**（唯一明确判定"复用 ✓"的列表）。 | 判定：通过
- **[结论]** key 用 `row.id`（react-table 自动生成）。 | 判定：通过

---

## 十四、任务详情页组件（UnifiedWorksPanel / MediaPipelinePanel / MediaTaskManagement / TaskInteractionsPanel）

### UnifiedWorksPanel.tsx（1479 行）
- **[数据]** `UnifiedWorksPanel.tsx:76` — `pageSize = 20` 常量；`page` state（96 行）；服务端分页 `skip: page * pageSize, limit: pageSize`（137–138 行）。 | 判定：通过
- **[产品]** `UnifiedWorksPanel.tsx:577-602` — 视图切换用 `Tabs`（table / rows / cards）+ 三个 `TabsTrigger`；**未复用 `usePersistentViewMode`（用本地 `useState`，视图不持久化）**。 | 判定：缺陷（与其它列表不一致）
- **[结论]** `UnifiedWorksPanel.tsx:577-579` — 视图用 `useState`（103 行 `useState<WorkView>("table")`），**视图选择刷新即丢失**。 | 判定：缺陷
- **[性能]** `UnifiedWorksPanel.tsx:141,157` — `refetchInterval: active ? 2_000 : 5_000` 三处（worksQuery 141 行、summaryQuery 157 行），`active` 由父级传入。 | 未按数据终态停止
- **[结论]** `UnifiedWorksPanel.tsx:102,224-230` — `selected` 用 `string[]`；`pageIds`（224 行）、`allPageSelected`（225–226 行）、`selectedWorks = rows.filter(...)`（227–229 行）、`somePageSelected`（230 行）**每次 render 计算，未 memo**。 | 判定：缺陷（O(n) 派生未缓存）
- **[产品]** `UnifiedWorksPanel.tsx:231-237` — `togglePageSelection` 支持本页全选/取消（`Array.from(new Set([...current, ...pageIds]))`）。 | 全选语义为本页
- **[交互]** `UnifiedWorksPanel.tsx:294-326` — 头部操作区：`MediaMigrationDialog` / `ProcessMediaDialog` / "沉浸播放"链接（`to="/douyin/$taskId/feed"`）/ 刷新。 | 操作入口齐全
- **[交互]** `UnifiedWorksPanel.tsx:328-347` — `processing_media` 状态用 `<output>` 提示"每 2 秒自动刷新，无需重复提交"，并显示 `resume_count`（恢复次数）。 | 判定：产品体验好
- **[数据]** `UnifiedWorksPanel.tsx:350-369` — 6 个 `Summary` 指标：作品数 / 下载完成·仅字幕 / 下载中·排队 / 视频失败 / 字幕完成·失败 / 本地·云端。 | 量化看板
- **[BUG]** `UnifiedWorksPanel.tsx:376-385` — 搜索框 `onChange` 同时 `setSearch + setPage(0) + setSelected([])`，**无防抖**；`queryKey` 含 `search`（122 行），每次按键都打请求。 | 判定：缺陷
- **[交互]** `UnifiedWorksPanel.tsx:387-465` — 四个 `Select`（排序 `SortValue` 6 项 / 下载状态 / 字幕状态 / 标签 `tagId`，标签来自 `listTags({limit: 500})`，显示 `#name（aweme_count）`）；每次变更都 `setPage(0) + setSelected([])`。 | 状态重置正确
- **[BUG]** `UnifiedWorksPanel.tsx:494-528` — "批量补采评论"用 `window.confirm`（`selected.length > 20` 才确认："将为 N 个视频分别创建评论补采任务，确认继续？"）。 | 原生 confirm + 阈值确认
- **[性能]** `UnifiedWorksPanel.tsx:193-222` — `recrawlComments` **串行 for-await 逐个**调用 `recrawlAwemeComments`。 | 批量操作耗时随选择数线性增长
- **[性能]** `UnifiedWorksPanel.tsx:241-254` — `failedAssets` 用 `useMemo`（好）；`selectedAssets`（238–240 行）**未 memo**。 | 判定：缺陷（派生计算未 memo）
- **[性能]** `UnifiedWorksPanel.tsx:256-262` — `lastMediaActivity = rows.reduce(...)` 每次 render 重算。 | 判定：缺陷
- **[性能]** 表格 / 横条 / 卡片三视图各一套内联实现（605–829 行 + 后续），**`WorkQuickActions` / `WorkRowItem` / `WorkCardItem` 操作列重复**；**无虚拟滚动**。 | 判定：缺陷（三套实现重复）
- **[A11Y]** `UnifiedWorksPanel.tsx:611-623` — 表头 `Checkbox` 有 `aria-label="选择本页作品"`；行内 `Checkbox`（645–656 行）**无 `aria-label`**；搜索框**无 `aria-label`**；`TabsList` 有 `aria-label="选择作品展示方式"`（587 行）。 | 判定：缺陷
- **[风格]** `UnifiedWorksPanel.tsx:606` — `overflow-x-auto`；列用 `hidden lg:table-cell` / `hidden xl:table-cell` 折叠。 | 判定：通过
- **[结论]** key 用 `aweme.id` / `row.aweme.id`（643、790、825 行）。 | 判定：通过（稳定）
- **[数据]** `UnifiedWorksPanel.tsx:159-167` — `invalidate` 三处 `invalidateQueries`（`douyin-works` / `douyin-media` / `douyin-media-summary`），**漏 `douyin-works-tags`**。 | 标签下拉不会刷新
- **[产品]** `UnifiedWorksPanel.tsx:264-281` — `exportSelection("comments" | "subtitles")` 复用同一个 `downloadExport`，字幕可选 `subtitleFormat`（srt / vtt / txt）。 | 判定：通过
- **[数据]** `UnifiedWorksPanel.tsx:194-215` — 补采评论参数从父任务继承 `fetch_sub_comments` / `max_comments_per_aweme` / `request_delay_level`（`ultra_steady` 保留，其余降级为 `steady`）/ `account_id`。 | 参数继承合理

### MediaPipelinePanel.tsx（487 行）
- **[BUG]** `MediaPipelinePanel.tsx:45` — `listMedia({ taskId, limit: 100 })` **硬编码 limit，无分页**。 | 判定：缺陷
- **[优化]** `MediaPipelinePanel.tsx:46-62` — `refetchInterval` 用函数形式：仅当存在 queued/downloading/字幕 pending·running/迁移中时 2s，否则 `active ? 2_000 : false`。 | 判定：通过（终态门控范例）
- **[数据]** `MediaPipelinePanel.tsx:158-186` — 6 个 `SummaryItem`：媒体总数 / 下载完成·临时·失败 / 字幕完成·失败 / 处理中 / 本地·云端 / 迁移中·失败。 | 量化看板
- **[结论]** `MediaPipelinePanel.tsx:103-108` — `failedAssets` **未 memo**（每次 render filter+map）。 | 判定：缺陷
- **[交互]** `MediaPipelinePanel.tsx:144-154` — "重试失败项"按钮一次性提交 `failedAssets`。 | 判定：通过
- **[A11Y]** `MediaPipelinePanel.tsx:320-349` — 行内操作按钮均有 `aria-label`（下载视频 / 重试媒体任务 / 重新翻译字幕）。 | 判定：通过
- **[产品]** `MediaPipelinePanel.tsx:290-302` — 字幕全文用 `<details>` + `<summary>` 折叠（`line-clamp-2`）。 | 判定：通过
- **[风格]** `MediaPipelinePanel.tsx:357-367` — `MigrationStatusView` 用 `Record<string,string>` 内联 labels；`PipelineStatus`（418–427 行）同样内联 labels。 | 状态文案就地硬编码、不可复用
- **[结论]** key 用 `asset.id`（206 行）。 | 判定：通过
- **[数据]** `MediaPipelinePanel.tsx:466-487` — `downloadMedia` 手动 `fetch` `${OpenAPI.BASE}/api/v1/douyin/tasks/${taskId}/media/${asset.id}/file` + `localStorage.access_token`。 | 与 `UnifiedWorksPanel` 的同名函数重复实现且 baseURL 取值不同（`OpenAPI.BASE` vs `browserApiBase()`）

### MediaTaskManagement.tsx（571 行）
- **[BUG]** `MediaTaskManagement.tsx:118-119` — `listMediaTasks({ skip: 0, limit: 100 })` **硬编码，无分页 UI**。 | 判定：缺陷
- **[性能]** `MediaTaskManagement.tsx:122` — `refetchInterval: 3_000` **无终态停止**；`retry: false`（121 行）。 | 判定：缺陷
- **[结论]** `MediaTaskManagement.tsx:125-140` — `counts` 用 `tasks.reduce` **每次 render 重算，未 memo**。 | 判定：缺陷
- **[结论]** `MediaTaskManagement.tsx:141-161` — `filtered` 用 `useMemo`（依赖 filter/search/tasks），但**筛选为客户端全量过滤**（服务端只返回 100 条）。 | 判定：缺陷（100 条上限导致筛选结果不完整）
- **[产品]** `MediaTaskManagement.tsx:168-190` — 状态筛选用 `fieldset` + `legend sr-only` + `aria-pressed` 按钮组，按钮内带计数（`tasks.length` / `counts[x]`）。 | 判定：通过（A11Y 与信息量兼顾）
- **[BUG]** `MediaTaskManagement.tsx:205-211` — 搜索框 `onChange` 即时过滤，**无防抖**。 | 判定：缺陷
- **[产品]** `MediaTaskManagement.tsx:110-112` — 视图持久化用 `usePersistentViewMode("douyin-media-tasks-view")`。 | 判定：通过
- **[风格]** `MediaTaskManagement.tsx:65-99` — `mediaStatusCopy: Record<DouyinMediaTaskStatus, {label, className}>`，6 种状态各带一套 Tailwind 硬编码色值（amber/sky/violet/blue/red/emerald + dark 变体）。 | **状态色硬编码**，与全站其它 Badge 不统一
- **[产品]** `MediaTaskManagement.tsx:411-477` — `MediaProgress` 进度条带 `role="progressbar"` + `aria-valuemin/max/now` + `aria-label`；文案三态（未创建字幕任务 / N 条处理中 / N 条失败 / 当前无排队）。 | 判定：通过
- **[结论]** key 用 `task.source_task_id`（247、253、283 行）。 | 判定：通过
- **[风格]** `MediaTaskManagement.tsx:564-571` — 又一份本地 `formatDate`（月份数字 + 时分）。 | 重复实现
- **[架构]** `MediaTaskManagement.tsx:489-494` — `MediaTaskActions` 内**把 `DouyinMediaTaskPublic` 手工拼装成 `{id, track_id, aweme_count, request}` 假 `task` 对象**传给 `ProcessMediaDialog`。 | 类型耦合 hack
- **[交互]** `MediaTaskManagement.tsx:504-518` — 按 `dependency_ready` / `active` / `status` 派生按钮文案（等待采集 / 处理中 / 创建下载任务 · 继续处理 · 补充处理）。 | 判定：通过

### TaskInteractionsPanel.tsx（236 行）
- **[BUG]** `TaskInteractionsPanel.tsx:42` — `listInteractions({ taskId, limit: 10 })` **硬编码 limit: 10，无分页**，只有"查看全部"链接。 | 判定：缺陷（详情页内嵌列表截断）
- **[性能]** `TaskInteractionsPanel.tsx:43-48` — `refetchInterval` 按数据终态切换 2s / 5s（有 queued/running → 2s，否则 5s）。 | 判定：通过（终态门控范例）
- **[交互]** `TaskInteractionsPanel.tsx:179-186` — 重试按钮对 `needs_review` 状态用 `window.confirm("请先到抖音页面确认这条内容没有发送成功。确认未发送并重试吗？")`。 | 原生 confirm
- **[风格]** `TaskInteractionsPanel.tsx:231-236` — 又一份本地 `formatDate`（`dateStyle: "short", timeStyle: "short"`）。 | 重复实现
- **[结论]** key 用 `item.id`（122 行）。 | 判定：通过
- **[A11Y]** `TaskInteractionsPanel.tsx:161` — 实时监控按钮有 `aria-label="查看实时监控"`；`target_video_url` 外链 `rel="noreferrer"`。 | 判定：通过
- **[数据]** `TaskInteractionsPanel.tsx:50-59` — `refresh` 三处 invalidate（`douyin-task-interactions` / `douyin-interactions` / `douyin-interaction-quota`）。 | 判定：通过

---

## 十五、列表架构通病汇总（12 维度横向对比）〔原文此处严重损坏，已尽力还原〕

- **[结论]** 12 维度判定框架：1 分页 / 2 筛选架构 / 3 排序 / 4 选择·批量 / 5 视图切换 / 6 三态完整性 / 7 数据刷新 / 8 组件复用 / 9 性能 / 10 可访问性 / 11 响应式 / 12 key 稳定性；对约 20 个带数据列表的页面逐页打分并汇总。
- **[BUG]** **通病 1 · 分页缺失 / limit 硬编码 100**— 命中：`douyin.tsx`(123-124)、`douyin-interactions.tsx`(101)、`douyin-accounts.tsx`(92)、`douyin-tracks.tsx`(无分页)、`douyin-tracks_.$trackId.tsx`(无分页)、`MediaTaskManagement.tsx`(118-119)、`UnifiedWorksPanel`（pageSize 20 有分页 ✓）、`MediaPipelinePanel`(45)、`TaskInteractionsPanel`(42)、`douyin-browsers`（无分页，量少合理）。 | **严重缺陷（最高优先级）**：超过 100 条的数据永久不可见
- **[BUG]** **通病 2 · 筛选状态未同步 URL** — 命中：`douyin.tsx`(109-112)、`douyin-interactions`(79-82)、`douyin-comments`(无 `validateSearch`)、`douyin-library.tsx`(127 单向：读 URL 不回写)、`douyin-accounts.tsx`(127-147 单向)、`douyin-keywords`、`douyin-request-logs`。 | 刷新 / 分享 / 后退全部丢失筛选，无法深链
- **[BUG]** **通病 3 · 搜索无防抖** — 命中：`douyin.tsx`、`douyin-interactions`、`douyin-library`(243-253)、`douyin-tracks`(27-28)、`douyin-creators`(27-28)、`douyin-tags`(28)、`MediaTaskManagement`(205-211)、`UnifiedWorksPanel`(376-385)、`douyin-request-logs`；每次按键都打一次请求。 | 判定：严重缺陷
- **[优化]** **通病 4 · 无排序** — 命中：`douyin.tsx`、`douyin-interactions`、`douyin-tracks`、`douyin-tracks_.$trackId`、`douyin-accounts`、`douyin-browsers`；有服务端排序但前端无 UI 的：`douyin-creators`、`douyin-request-logs`。 | 判定：缺陷
- **[结论]** **通病 5 · 选择模型 O(n)** — `Array<string>` + `includes` 命中：`douyin-accounts`(807)、`douyin-tags`(91)、`UnifiedWorksPanel`(102)、`douyin-keywords`、`douyin-request-logs`；`Array` 存储 + `Set` 派生（半优化）：`douyin-library`(279-280)。 | 判定：严重缺陷（大列表 toggle 卡顿）
- **[结论]** **通病 6 · 选择模型正确范例** — `douyin.tsx`(114)、`douyin-interactions`、`douyin-comments`(132)、`douyin-tracks_.$trackId`(104-106)、`VideoPendingIds`/`verifyPendingIds`、`MediaPipelinePanel`(asset.id key)。 | 应推广为统一模式
- **[产品]** **通病 7 · 全选语义不统一** — "本页全选"：`douyin.tsx`(170-179)、`douyin-library`(490-492)、`UnifiedWorksPanel`(231-237)、`douyin-comments`(195-196)、`douyin-tracks_.$trackId`(254-260，有 `aria-label` 说明)；**完全无全选**：`douyin-interactions`、`douyin-tags`、`douyin-request-logs`、`douyin-accounts`。 | 用户可能误以为全选 = 全量
- **[产品]** **通病 8 · 视图不持久化 / 不统一** — 统一用 `usePersistentViewMode` 的：`douyin.tsx`(113)、`douyin-comments`(135)、`douyin-creators`、`douyin-accounts`、`douyin-tags`、`MediaTaskManagement`(110-112)；**未持久化（本地 useState）**：`UnifiedWorksPanel`(103,577-579，Tabs)；**完全无视图切换**：`douyin-tracks`、`douyin-tracks_.$trackId`、`douyin-browsers`。 | 判定：缺陷
- **[BUG]** **通病 9 · 三态（空 / 加载 / 错误）缺失或合并** — **无错误态、无重试**：`douyin-comments`(638-640，表头空态与加载态合并)、`douyin-interactions`(无 isError)、`douyin-keywords`(494-503 合并)、`douyin-creators`（无 isError）；**空态缺失**：`douyin-accounts` 的 pools 分支(474)；**加载态非骨架屏**：`douyin.tsx`、`douyin-interactions`。 | 判定：**严重缺陷**（接口失败静默显示"无数据"，误导"无数据"结论）
- **[性能]** **通病 10 · 轮询无终态停止 / 后台空转** — 命中：`douyin.tsx`(127，3s)、`douyin-interactions`(103-108，2s/5s)、`douyin-tracks`(113，10s)、`douyin-creators`(122,133，10s/15s)、`douyin-accounts`(264，5s)、`douyin-browsers`(127，3s)、`MediaTaskManagement`(122，3s)、`UnifiedWorksPanel`(141,157，2s/5s)；普遍缺 `refetchIntervalInBackground: false`。 | **正确范例**：`MediaPipelinePanel.tsx:46-62`（按数据状态门控，终态 `false`）、`TaskInteractionsPanel`(43-48)。 | 判定：严重缺陷（资源浪费）
- **[性能]** **通病 11 · 派生计算未 memo / 每次 render 重算** — 命中：`douyin-tracks`(102,106 `rows.reduce` 两次)、`MediaTaskManagement`(125-140 `counts` 未 memo)、`douyin-creators`(102,106)、`douyin-comments`(taskMap 每次重建 Map)、`UnifiedWorksPanel`(225-230,238-240,256-262)、`MediaPipelinePanel`(103-108)、`douyin-browsers`(36 `slots.find`)、`douyin-accounts`(selectedAssets)。 | 判定：缺陷
- **[性能]** **通病 12 · 行组件重 hook / 整页 loading 闪烁** — `douyin-browsers.tsx:281` 的 `useEffect` 依赖 `slots`，**每次 3s 轮询触发整页加载态**。 | 判定：严重缺陷（体验差）
- **[性能]** **通病 13 · 无虚拟滚动** — 全部列表均无（pageSize 50/100 尚可接受，但**无上限保护**）。 | 判定：中低优先级
- **[交互]** **通病 14 · `window.confirm` 代替组件级二次确认** — 命中：`douyin.tsx:851`（批量删除）、`douyin-interactions:851`、`douyin-tags:270`、`douyin-keywords`、`UnifiedWorksPanel:516-521`（批量补采）、`TaskInteractionsPanel:179-186`（重试）、`douyin-comments`（>1000 条导出）。 | 不可定制、不可 A11Y、不可测试
- **[风格]** **通病 15 · 状态色硬编码** — `MediaTaskManagement.tsx:65-99` 6 种状态各一份 Tailwind 色值（amber/sky/violet/blue/red/emerald + dark 变体）；各页 Badge 的 `variant` 选择（destructive / outline / secondary / default）也各自散落。 | 无统一状态色语义表
- **[风格]** **通病 16 · `formatDate` 重复实现** — 至少 6 份：`douyin.tsx:1598-1610`（`formatUnix` + `formatDateTimeText`）、`douyin-comments.tsx:1150-1162`、`douyin-keywords.tsx:1238-1245`（带"从未"）、`douyin-creators`、`MediaTaskManagement.tsx:564-571`、`TaskInteractionsPanel.tsx:231-236`、`douyin-tracks`。 | 判定：缺陷（应上提 `@/utils`）
- **[架构]** **通病 17 · 手写 `Table`，未复用通用 `DataTable`** — 命中：`douyin.tsx`(489-571)、`douyin-interactions`、`douyin-comments`(589)、`douyin-keywords`(506)、`douyin-tracks`(430)、`douyin-tracks_.$trackId`(447,616)、`douyin-accounts`、`douyin-browsers`、`UnifiedWorksPanel`、`MediaTaskManagement`、`MediaPipelinePanel`、`TaskInteractionsPanel`；**唯一复用 `DataTable` ✓**：`douyin-tags.tsx`（其能力已覆盖 客户端分页 / 排序 / 搜索 / 行选择）。 | 判定：**架构级缺陷**（每个列表都缺列排序、列可见性、行选择等基础能力）
- **[架构]** **通病 18 · 三视图（table / rows / cards）三套独立实现** — `UnifiedWorksPanel`（`WorkRowItem` / `WorkCardItem` / 内联 Table + `WorkQuickActions`）、`douyin-library`（`VideoTable` / `VideoCard` / `VideoRow`）、`douyin-keywords`(`KeywordPreview`)、`douyin-accounts`(`AccountPreview`)、`douyin-comments`(`CommentRow` / `CommentPreviewCard`)。 | 操作列逻辑重复，判定：缺陷
- **[风格]** **通病 19 · 操作列拥挤** — 操作列最多出现 5~6 个按钮（`UnifiedWorksPanel` 表格行：视频预览 / 字幕 / 下载 / 重试 / 抖音外链 / 进入任务；`douyin-keywords` 行：任务 / 移动 / 启停 / 删除；`MediaPipelinePanel` 行：预览 / 迁移 / 下载 / 重试 / 重译）；窄屏溢出依赖 `overflow-x-auto`。 | 判定：缺陷（`MediaTaskManagement` 的 `DropdownMenu` 收纳是正确做法）
- **[A11Y]** **通病 20 · `Checkbox` 无 `aria-label` / 搜索框无 `aria-label`** — 行内 `Checkbox` 缺 `aria-label`：`UnifiedWorksPanel`(645-656)；搜索框缺 `aria-label`：`douyin-comments`、`douyin-keywords`、`UnifiedWorksPanel`；**做得好**：`douyin-tracks_.$trackId`（451、482、528、537、602、674、683 行）、`MediaPipelinePanel`(320-349)、`MediaTaskManagement`(168-190 fieldset+legend)、`douyin-browsers`(214-220)。 | 判定：缺陷
- **[风格]** **通病 21 · 响应式不统一** — `overflow-x-auto` 正确：`douyin.tsx`、`douyin-comments`(588-589)、`douyin-creators`、`douyin-tags`、`douyin-accounts`(`min-w-[900px]`)、`douyin-browsers`(195-203)、`douyin-request-logs`、`UnifiedWorksPanel`(606)、`MediaTaskManagement`；**非 `overflow-x-auto`（可能截断）**：`douyin-tracks`、`douyin-keywords`。 | 判定：缺陷
- **[结论]** **通病 22 · key 稳定性普遍合格** — `task.id` / `item.id` / `row.id` / `creator.id` / `keyword.id` / `asset.id` / `aweme.id` / `slotKey(slot)` / `source_task_id`；`douyin-library` 用 `slotKey(slot)`、`Admin` 用 `key={item.id}`（原文残留 `29 key={item.id} 稳定`）。 | 判定：通过
- **[数据]** **通病 23 · `limit: 500` / `limit: 200` 等其它硬编码** — `UnifiedWorksPanel` 的 `listTags({limit: 500})`（150 行）；`douyin-creators` 的 overview 额外拉 500 条（202-213 行）；`douyin-library` `limit: 200`；`douyin-tracks_.$trackId` 的 `listAccounts({limit:100})`。 | 判定：缺陷
- **[数据]** **通病 24 · 重复查询 / 无效请求** — `douyin-creators` 的 `overviewQuery` 与主列表 `metrics` 重复拉 500 条；`douyin-accounts` 的 `refetchInterval: 5_000` 在无活跃任务时也轮询。 | 判定：缺陷
- **[数据]** **通病 25 · `invalidate` 的 queryKey 不完整** — `UnifiedWorksPanel.tsx:159-167` 漏 `douyin-works-tags`（标签下拉计数不刷新）；`douyin.browsers` 只有两个 key。 | 判定：缺陷（数据不一致）

---

## 十六、架构师改进建议与优先级〔原文此处严重损坏，已尽力还原〕

- **[架构]** **建议 1 · 引入通用 `DataTable`**（基于 `@tanstack/react-table`，`douyin-tags.tsx` 已示范：`getCoreRowModel` + `getPaginationRowModel` + `getSortedRowModel`），统一替换 12+ 处手写 `Table`。 | 收益：一次性补齐列排序、列可见性、列过滤、行选择、A11Y 表语义；**优先级：最高**
- **[架构]** **建议 2 · 分页与筛选状态提升至 URL**——用 TanStack Router 的 `useSearch` / `validateSearch` 管理 `page` / `pageSize` / 排序 / 筛选，支持深链分享与刷新保持；消除 `douyin-library` / `douyin-accounts` 的"单向读 URL"半成品。 | **优先级：最高**
- **[BUG]** **建议 3 · 分页化或明确上限提示**——所有 `limit: 100` 硬编码要么接入服务端 `skip/limit` 翻页，要么显式声明"仅展示最近 100 条"并在 `count > 100` 时提示。 | **优先级：最高**
- **[架构]** **建议 4 · 引入 `QueryErrorState` 统一替换手写错误态**——`douyin-comments`(638-640)、`douyin-interactions`、`douyin-keywords`(494-503)、`douyin-creators` 目前把接口失败渲染成"无数据"，必须补齐 `isError` 分支与重试按钮。 | **优先级：高**（误导性最强）
- **[性能]** **建议 5 · 轮询加终态门控**——仅当存在 `login_required` / `verifying` / `busy` / `queued` / `running` 状态时才 2s 轮询，全部终态时停止或降频至 30s，并统一加 `refetchIntervalInBackground: false`。参照 `MediaPipelinePanel.tsx:46-62` 的函数式 `refetchInterval`。 | **优先级：高**
- **[性能]** **建议 6 · 选择模型统一改 `Set<string>` + `has()`**——`selected: string[]` 改为 `Set`（`douyin-accounts:807` 起）；toggle 用 `has()` 而非 `includes`；全选基于 `Set` 批量合并。 | **优先级：高**
- **[性能]** **建议 7 · 派生值全部 `useMemo`**——`counts`（`MediaTaskManagement:125-140`）、`rows.reduce`（`douyin-tracks:102,106`）、`selectedAssets`（`UnifiedWorksPanel:238-240`）、`taskMap`（`douyin-comments`）、`slots.find`（`douyin-browsers:36`）。 | **优先级：中**
- **[交互]** **建议 8 · 用组件级 `AlertDialog` 替换 `window.confirm`**（7 处）。 | **优先级：中**
- **[优化]** **建议 9 · 状态色收敛为统一语义表**——把 `mediaStatusCopy` 之类的硬编码 Tailwind 色值提为 `statusColor(status)` 单点映射，兼容 dark 模式。 | **优先级：中**
- **[风格]** **建议 10 · `formatDate` / `formatUnix` 上提 `@/utils`**，消除 6+ 份重复实现与空值文案（"未知" / "从未"）不一致。 | **优先级：中**
- **[风格]** **建议 11 · 视图切换统一走 `usePersistentViewMode`**——`UnifiedWorksPanel` 的 Tabs 视图应改为持久化，保证全站一致。 | **优先级：中**
- **[性能]** **建议 12 · 列表量小时不需要虚拟滚动**——pageSize 50 / 100 条可接受；但需在 `count` 超过阈值时给出"仅显示前 N 条"的显式提示。 | **优先级：低**
- **[架构]** **建议 13 · 三视图（table / rows / cards）抽公共行渲染 + 操作列**——消除 `WorkQuickActions` / `WorkRowItem` / `WorkCardItem`、`VideoTable` / `VideoCard` / `VideoRow`、`CommentRow` / `CommentPreviewCard` 的重复。 | **优先级：中**
- **[交互]** **建议 14 · 操作列超过 3 个按钮时收入 `DropdownMenu`**（参照 `MediaTaskManagement.tsx:519-536` 的 `MoreHorizontal` 做法）。 | **优先级：低**
- **[A11Y]** **建议 15 · 补 `aria-label`**——行内 `Checkbox`、搜索框；搜索框统一 `label` + `sr-only` 关联。 | **优先级：低**
- **[数据]** **建议 16 · `invalidate` 补全 queryKey**（`douyin-works-tags` 等）。 | **优先级：低**
- **[结论]** **建议 17 · 合并文案/静态数据可接受无轮询；`useSuspenseQuery` 会把错误抛给路由 `ErrorBoundary`**，因此 `admin.tsx` 的用户列表用 `useSuspenseQuery` 无独立错误态可接受。 | 判定规则说明

---

## 十七、原文损坏区标记

- **[结论]** **第 62838 行附近（`# 一` 标题）至第 68000 行**：导出内容被严重损坏——`> 💭 **思考过程**` 与正文交替、句子被逐字拆散（残片如 `douyin819-key行`、`-inter/actions`、`jiao`、`12 维度缺陷矩阵`、`| 维度 | 判定 | 行号证据 |` 表格被切碎）。本文件第七–十六章内容系从这些碎片中尽力还原。（原文此处损坏，已尽力还原）
- **[结论]** 上述区间内可辨认的结构骨架为：`# 一`（12 维度缺陷矩阵，逐列表）→ `# 二、账号池列表 douyin-accounts.tsx（934 行）` → `# 三、请求日志列表 douyin-request-logs.tsx（606 行）` → 通病汇总表（`| 维度 | 严重缺陷 | 缺陷 | 通过 | 行号证据 |` 格式）→ `**架构师建议**`。
- **[结论]** 因损坏导致的**行号不确定项**（已在上文逐条标注"（原文此处损坏，已尽力还原）"）：`douyin-creators` 的 pageSize/refetchInterval、`douyin-library` 的排序字段行号、`Pager` 组件的归属文件与行号（1556-1588 / 1197-1231 / 130,136 三组混排）、`douyin-request-logs` 的部分维度行号、`douyin-browsers` 的 `invalidate` queryKey。
