# 灵感采集台 · 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 文档状态 | 已上线 · 逆向归档（与线上原型逐一对齐） |
| 编写日期 | 2026-08-19 |
| 产品基线 | 分支 `refactor/uv-workspace-modules`，提交 `7bbf0af` |
| 产品名称 | 灵感采集台（副标：内容运营工作台） |
| 文案基准附件 | 《前端逆向产品能力文档》（同目录，全量逐字 UI 文案以该文档为准） |
| 目标读者 | 产品、设计、前端/后端研发、测试、运营 |

## 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-08-19 | 基于已上线前端原型逆向归档，全模块首版 |

## 阅读说明

- 本文档为**逆向 PRD**：产品已上线，全部需求描述、界面文案、业务规则、状态机均与当前实现逐一核对，可作为后续迭代的对齐基准。
- 需求编号规则：`FR-{域}-{序号}`；优先级：**P0** 核心链路（缺则业务不闭环）/ **P1** 重要能力 / **P2** 增强体验。
- 界面文案在本文档中摘录关键主文案（标题、按钮、空态、确认框、Toast）；**全量逐字文案**以附件《前端逆向产品能力文档》为准，两者内容同源、口径一致。
- `{n}`、`{name}` 等花括号表示运行期变量；状态枚举左侧英文值为前后端约定值，右侧中文为界面实际展示。

---

## 1. 产品概述

### 1.1 背景与问题

抖音内容运营团队日常工作存在四类断点：

1. **采集散**：按关键词/作品/创作者抓内容靠脚本和手工，任务状态不可见、失败无感知；
2. **资产乱**：抓回的视频、评论散落在本地文件夹，无法按市场方向（赛道）归集、筛选与复用；
3. **互动险**：评论、回复、私信等互动直接在账号上手工操作，无确认环节、无配额控制、无操作留痕，风控风险高；
4. **环境杂**：多账号共用浏览器指纹与登录态，易串号；远程浏览器状态不可见。

### 1.2 产品定位

**灵感采集台**是一站式抖音内容运营工作台：以「赛道」组织市场方向，以「关键词/任务」驱动内容采集，沉淀视频、评论、标签三类内容资产，并通过托管账号池完成**人工确认式**互动，全程附带浏览器实时监控与风控配额管理。

品牌主张（登录页品牌栏逐字）：**把采集、管理与内容洞察，放进一个清爽的工作台。**

### 1.3 产品目标

| 目标 | 衡量方式（现有能力支撑） |
| --- | --- |
| 采集可追踪 | 任务 9 态状态机 + 四阶段进度 + 2~3 秒级实时刷新 + 断点续跑 |
| 资产可沉淀 | 视频资源库 / 评论管理 / 标签管理三视图，赛道自动归集 |
| 互动可管控 | 两段式人工确认 + 每日配额 + 最小间隔 + 24 小时防重 + 操作留痕 |
| 环境可隔离 | 每账号独立浏览器空间 + 远程槽位一对一绑定 + 账号池调度 |

### 1.4 产品范围总览

| 域 | 模块 | 优先级 |
| --- | --- | --- |
| 认证 | 登录 / 注册 / 找回密码 / 重置密码 | P0 |
| 概览 | 工作台 | P0 |
| 采集 | 赛道管理、抖音任务、任务详情、关键词管理 | P0 |
| 内容 | 视频资源库、评论管理、标签管理 | P1 |
| 运营与风控 | 互动任务、账号池、浏览器监控 | P0 / P0 / P1 |
| 系统 | 开发者中心、用户管理、个人设置 | P1 / P0 / P1 |
| 增强 | 沉浸播放（任务级/资源库级）、媒体迁移、批量导出 | P1 |

### 1.5 名词术语表

| 术语 | 定义 |
| --- | --- |
| 赛道（Track） | 市场方向的一级组织单元；关键词唯一归属一个赛道，任务与内容资产沿赛道归集 |
| 默认赛道 | 系统内置赛道，承接未指定归属的数据；不可删除、必须保持启用 |
| 关键词（Keyword） | 采集搜索词，唯一归属一个赛道；有启停位与爬取状态 |
| 任务（CrawlTask） | 一次采集作业（关键词搜索/指定作品/创作者作品/点赞/收藏），9 态状态机 |
| 分片（Shard） | 使用账号池执行时按目标拆分出的子任务，每分片独立浏览器空间 |
| 媒体资产 | 任务作品对应的视频文件（本地服务器或云端 MinIO 存储） |
| 字幕 | 由远程 Whisper API 生成的视频字幕，失败不回退本地模型 |
| 迁移（Migration） | 本地视频上传云端的三步流程：上传 → 回读校验 → 切换存储后删本地 |
| 互动任务（Interaction） | 视频评论/评论回复/作者私信，两段式人工确认后经托管浏览器执行 |
| 账号池（Account Pool） | 托管抖音账号集合，支持并行调度策略（最少负载/顺序轮询/加权轮询） |
| 槽位（Browser Slot） | 一个远程常驻浏览器 + 持久化登录空间，一对一绑定账号 |
| 待人工核对（needs_review） | 互动执行结果不确定，需人工到抖音确认未发送后再重试 |

---

## 2. 用户、角色与核心场景

### 2.1 角色定义

| 角色 | 判定 | 能力边界 |
| --- | --- | --- |
| 访客 | 无登录态 | 仅 4 个认证页 |
| 内容运营（登录用户） | 有效 `access_token` | 全部业务页面（抖音各页无页面级角色门槛） |
| 超级管理员 | `is_superuser = true` | 额外可见/访问「用户管理」 |

权限细则：未登录访问业务页一律重定向 `/login`；已登录访问认证页重定向 `/`；重置密码页必须带 `token`；资源级越权由后端 403/404 表达，前端提示「{资源}不存在，或当前账号无权访问」；管理员无法在后台编辑/删除自己（行内不渲染操作菜单）。

### 2.2 核心场景故事线

**场景 A：开辟一个新市场方向并采集内容（P0 主链路）**

1. 运营在「赛道管理」创建赛道「户外露营装备」，写入目标人群与初始关键词；
2. 在赛道运营工作区勾选本次采集关键词，选择「组合任务」与执行账号，启动采集；
3. 若任务进入「等待扫码」，在任务详情页用抖音 App 扫码，任务自动继续；
4. 在任务详情页观察四阶段进度（作品采集 → 评论采集 → 视频下载 → 字幕处理），异常时一键「继续任务」断点续跑；
5. 采集完成后，作品自动沉淀到「视频资源库」，评论进入「评论管理」，话题自动抽取为「标签」。

**场景 B：基于评论洞察发起互动（P0 风控链路）**

1. 运营在「评论管理」按赛道+关键词+点赞区间筛出高价值评论；
2. 点击行内「回复」，选择发送账号（可见今日剩余配额与最小间隔），撰写内容；
3. 系统先「发送前检查」生成草稿，运营「确认并发送」后进入浏览器执行队列；
4. 在「实时监控」抽屉观察浏览器画面与执行链路（每秒刷新），平台确认接收后任务「已成功」；
5. 若结果不确定进入「待人工核对」，运营先到抖音确认未发送，再执行重试，避免重复发送。

**场景 C：多账号并行与风险控制（P0）**

1. 管理员在「账号池」添加托管账号（别名即可，无需抖音号），远程槽位自动分配；
2. 打开账号专属浏览器完成扫码登录，回到页面「验证」后进入可用池；
3. 创建并行账号池（如 5 账号、最少负载策略、最大并行 3）；
4. 批量关键词任务按目标拆分分片并行执行，每账号受每日任务上限与配额约束；
5. 「浏览器监控中心」5 秒级探测每个槽位健康度与活动页面，可直接 iframe 内人工接管。

**场景 D：资产云端化与沉浸复盘（P1）**

1. 资源库筛选某赛道「本地服务器」视频，一键「本地视频转云端」（确认框明示三步安全流程）；
2. 迁移完成后用「沉浸播放」全屏竖滑复盘内容，支持 J/K 与滚轮切换；
3. 选中作品导出评论 TXT 或字幕 SRT/VTT/TXT，供内容分析。


---

## 3. 信息架构与导航

### 3.1 导航结构（侧边栏 5 组 12 页）

| 分组（副标） | 菜单项 → 路由 | 可见性 |
| --- | --- | --- |
| 概览 | 工作台 → `/` | 登录 |
| 采集（任务与策略） | 赛道管理 → `/douyin-tracks`；抖音任务 → `/douyin`；关键词管理 → `/douyin-keywords` | 登录 |
| 内容（资产与数据） | 视频资源库 → `/douyin-library`；评论管理 → `/douyin-comments`；标签管理 → `/douyin-tags` | 登录 |
| 运营与风控 | 互动任务 → `/douyin-interactions`；账号池 → `/douyin-accounts`；浏览器监控 → `/douyin-browsers` | 登录 |
| 系统 | 开发者中心 → `/developer-tools`；用户管理 → `/admin` | 后者仅超管 |

二级页面：任务详情 `/douyin/{taskId}`、任务沉浸播放 `/douyin/{taskId}/feed`、赛道详情 `/douyin-tracks/{trackId}`、资源库沉浸播放 `/douyin-library/feed`。无导航入口页：个人设置 `/settings`（用户菜单进入）、通用条目 `/items`（模板残留）。

### 3.2 布局规范

```
┌─────────────┬──────────────────────────────────────────────┐
│ Logo        │ ≡ │ 当前区块名              [● 服务运行正常]   │
│ 导航分组     │   │ 副标题：内容采集与运营管理                │
│  （5组）     ├──────────────────────────────────────────────┤
│             │        主内容区（最大宽 1600px，垂直滚动）      │
│ 用户菜单     │        页面骨架：PageHero → 指标卡 →           │
└─────────────┴        筛选区 → 数据区 → 分页                 ┘
```

- 侧边栏可折叠为图标态；移动端为抽屉，点选后自动收起；当前页高亮（精确或前缀匹配）。
- 顶栏左侧为区块名（14 条路径前缀映射规则）+ 固定副标题「内容采集与运营管理」；右侧为服务状态徽章（**当前版本为静态展示，见 10.1 已知问题 #1**）。
- 无障碍：全站提供「跳到主要内容」跳转链接；图标按钮配 aria-label；支持 motion-reduce 减少动效。

### 3.3 设计系统

| 维度 | 规格 |
| --- | --- |
| 明暗模式 | 浅色 / 深色 / 跟随系统（默认跟随系统，监听系统偏好变化） |
| 主题风格 | 清透蓝（ocean）/ 专业灰（graphite）/ 跃动紫（violet，默认） |
| 信息密度 | 舒适（默认）/ 紧凑 |
| 持久化 | 三个维度均存 localStorage（键前缀 `vite-ui-theme`） |
| 指标卡色调 | violet / blue / mint / coral / rose / slate，顶部渐变光晕 + 悬停上浮 |
| 容器语言 | 圆角卡片（rounded-2xl）+ 细边框 + 浅阴影 |
| 响应式断点 | 移动端 `md`（表格↔卡片）、网格 `sm/xl/2xl` |
| 图标 | lucide-react 全站统一 |

### 3.4 全局交互规范

1. **反馈**：写操作一律以 Toast 收尾，成功标题「操作成功」、失败标题「操作失败」；行级操作仅禁用当前行按钮（mutation.variables 级 pending）。
2. **确认**：破坏性操作（删除/批量迁移/批量重试等）必须二次确认（confirm 或确认 Dialog），确认文案须明示后果与不可逆性。
3. **实时性**：进行中数据按第 8.2 节轮询表自动刷新；页头普遍提供手动「刷新」。
4. **筛选**：筛选变更即重置页码；列表页空态/加载态/错误态三态齐全，错误态统一使用 QueryErrorState（标题 + 描述 +「重试/正在重试…」按钮）。
5. **异常兜底**：未匹配路由 → 404 页（「页面不存在」）；路由渲染异常 → 异常页（「暂时无法打开这个页面」）；两者均提供「返回工作台」。
6. **错误文案**：后端英文错误码经中文化映射表转换后弹出（含登录失效、账号停用、密码错误、邮箱已注册等 12 条映射 + 网络错误与兜底文案）；未命中映射的原文弹出。

---

## 4. 功能需求详述

### 4.1 认证模块

#### FR-AUTH-01 登录（P0）

- **页面**：`/login`，标题「欢迎回来」，副标「登录后继续管理你的采集任务与内容资产」。
- **交互**：邮箱 + 密码 +「登录」；「忘记密码？」→ `/recover-password`；底部「还没有账号？ 立即注册」→ `/signup`。
- **校验**：请输入有效的邮箱地址 / 请输入密码 / 密码至少需要 8 个字符（onBlur 触发）。
- **规则**：成功写 token 进首页；后端错误映射为中文 Toast（邮箱或密码错误 / 账号已停用等）。
- **验收**：正确凭据跳转 `/`；错误凭据出现对应中文 Toast；已登录访问本页重定向 `/`。

#### FR-AUTH-02 注册（P0）

- **页面**：`/signup`，标题「创建账号」，副标「加入灵感采集台，开始管理你的内容工作流」。
- **字段**：姓名 / 邮箱 / 密码（占位「至少 8 个字符」）/ 确认密码；按钮「注册」；底部「已有账号？ 返回登录」。
- **校验**：含「两次输入的密码不一致」（挂在确认密码字段）。
- **规则**：提交时剔除确认密码；邮箱重复 → Toast「该邮箱已注册，请直接登录」。
- **验收**：合法注册成功；重复邮箱提示明确；二次密码不一致被拦截。

#### FR-AUTH-03 找回密码（P1）

- **页面**：`/recover-password`，标题「找回密码」，副标「输入注册邮箱，我们会发送密码重置链接」。
- **交互**：邮箱 +「发送重置邮件」；成功 Toast「密码重置邮件已发送，请检查收件箱」并重置表单。

#### FR-AUTH-04 重置密码（P1）

- **页面**：`/reset-password`，标题「重置密码」，副标「设置一个至少 8 位的新密码」。
- **前置**：URL 无 `token` 重定向 `/login`。
- **字段**：新密码 / 确认新密码；按钮「更新密码」。
- **规则**：成功 Toast「密码已更新，请重新登录」并跳 `/login`；token 失效提示「链接已失效，请重新操作」。

### 4.2 概览域：工作台

#### FR-DASH-01 运营总览（P0）

- **页面**：`/`，eyebrow「今日运营概览」，标题「你好，{姓名}」（无姓名「欢迎回来」）。
- **区块**：状态胶囊×3（进行中任务/需关注/可用账号）→ 指标卡×4（任务总数/{范围}作品/{范围}评论/可用账号）→ 异常横幅（条件出现）→ 最近任务（前 6 条，双列卡片）。
- **关键文案**：描述「从任务调度、账号可用性到内容沉淀，一眼掌握当前运营状态；需要处理的异常会优先浮到前面。」；横幅「有 {n} 项任务需要处理 ｜ 包括等待扫码、失败或中断的任务，建议优先检查。」；空态「暂无任务，前往任务中心创建第一个任务。」
- **规则**：任务 5 秒轮询；「需关注」= failed/interrupted/waiting_login；数据范围标签按是否截断显示「近 100 个任务/全部任务」；账号/任务读取失败分别出错误块。
- **验收**：各计数与接口一致；横幅仅在需关注 >0 时出现；点击任务卡进详情。

### 4.3 采集域

#### FR-TRACK-01 赛道列表与生命周期（P0）

- **页面**：`/douyin-tracks`，eyebrow「私域增长」，标题「赛道管理」。
- **区块**：指标卡×4（运营赛道/关键词资产/赛道作品/目标评论）→ 搜索 → 赛道卡片网格（md:2/xl:3 列）。
- **赛道卡**：名称（进详情）、徽标「配置：启用/停用」「默认」、四格指标（关键词/任务/作品/评论）、「最近采集」行、按钮「▶ 运营这个赛道」（停用禁用）、菜单（编辑/启停/删除）。
- **规则**：默认赛道不可删除、必须保持启用（菜单禁用项文案「默认赛道必须启用」「默认赛道不可删除」）；删除赛道将关键词/任务/内容**完整迁移到默认赛道**（确认框明示「赛道删除后无法恢复」）；列表 10 秒轮询；支持 `?run={trackId}` 直达运营工作区。
- **空态**：「还没有运营赛道 ｜ 从一个细分市场和一组用户搜索词开始。」
- **验收**：默认赛道两项菜单禁用；删除后原赛道关键词归属默认赛道；停用赛道「运营这个赛道」禁用且 title 提示「请先启用赛道再启动采集」。

#### FR-TRACK-02 创建/编辑赛道（P0）

- **创建对话框**：赛道名称（必填）、目标与人群、创建新关键词（一行一个或逗号分隔）、赛道提示词（≤10000 字）、移动已有关键词选择器。
- **规则**：勾选现有关键词提交前 confirm「已选的 {n} 个现有关键词会从原赛道移动到新赛道…」；成功 Toast「赛道已创建，关键词已归入新赛道」。
- **编辑对话框**：仅名称与目标人群；描述明示「调整赛道定位不会改动已关联的关键词与历史任务。」

#### FR-TRACK-03 赛道运营工作区（P0）

- **入口**：赛道卡「运营这个赛道」或 `?run=` 参数；标题「{赛道名} · 运营工作区」。
- **区块**：①关键词组合（词徽章 + 补充创建 + 移动已有）②本次采集关键词（默认全选启用词，可部分勾选）③参数区（任务组织方式/执行账号/调度策略/作品与评论上限）。
- **规则**：停用词不参与任务并提示「另有 {n} 个已停用关键词，不会加入本次任务。」；每词独立任务 ≤20 词；部分选择 ≤200 词；固定参数：云端托管浏览器、云端存储、「稳 · 随机 3–6 秒」风控档（UI 明示且不可改）；赛道停用时警示「当前赛道已停用，请先启用赛道再启动采集任务。」
- **验收**：三类超限警示按规则出现；成功 Toast「已创建 {count} 个赛道采集任务」；404/409 时刷新并关闭。

#### FR-TRACK-04 赛道详情与关键词归属（P0）

- **页面**：`/douyin-tracks/{trackId}`；顶部概要 + 四格指标（可用关键词 {启用}/{总数} 等）+ 左卡「赛道关键词」+ 右卡「赛道信息与提示词」内联编辑。
- **规则**：关键词唯一归属一个赛道；「移回默认」把关键词迁移到默认赛道（历史任务/作品/评论保留）；已关联历史任务的关键词词面不可改（输入框禁用并提示）；默认赛道的关键词不能移除；赛道提示词仅沉淀策略，「暂不直接改变爬取参数」。
- **验收**：移动关键词 Toast「关键词已归入当前赛道」；移除 Toast「关键词已移回默认赛道，历史任务与内容数据已保留」。

#### FR-TASK-01 任务列表（P0）

- **页面**：`/douyin`，eyebrow「采集任务中心」，标题「抖音采集任务」；副文本「数据每 3 秒自动刷新 · 当前加载 {n} / {count} 个任务」。
- **区块**：指标卡×4（进行中/已抓作品/已存评论/互动记录）→ 筛选面板（状态组「全部/进行中/需处理{n}/已完成」+ 赛道下拉 + 搜索）→ 桌面表格 / 移动卡片。
- **表列**：任务 ｜ 所属赛道 ｜ 状态 ｜ 浏览器 ｜ 数据进度 ｜ 创建时间 ｜ 操作。
- **规则**：3 秒轮询；「需处理」= failed/interrupted/waiting_login 且带角标；search 类型行提供「同步关键词」（Toast「已新增 {created} 个关键词、{binding} 个任务绑定」）；浏览器列映射 remote→云端浏览器 / local→本机浏览器 / 其他→系统默认。
- **空态**：全空「还没有抖音任务 ｜ 点击上方“创建任务”开始第一次抓取。」；筛选空「没有匹配的任务 ｜ 试试切换状态或清空搜索条件。」
- **验收**：状态分组计数准确；角标仅在需处理 >0 时出现；轮询期间滚动与筛选不被打断。

#### FR-TASK-02 创建采集任务（P0）

- **对话框**：CreateTaskDialog（任务页/赛道页共用），标题「✦ 创建抖音采集任务」。
- **基础设置**：所属赛道（必选）、任务类型（关键词搜索/指定作品/创作者作品/当前账号点赞/当前账号收藏）、登录方式（扫码/临时凭据，仅临时账号）、浏览器（跟随服务配置/本机/云端，仅临时账号）、目标输入（按类型切换，支持每行一个批量）、执行账号（临时登录/账号/账号池）、调度策略、临时登录凭据、最大作品数（1-1000，默认 10）、同时抓取评论（默认开）。
- **高级设置（折叠）**：起始页、并发数（1-5）、风控节奏（快 1–2s/稳 3–6s/超级稳 6–12s 随机）、发布时间（不限/一天内/一周内/半年内）、抓取子评论、单作品评论上限、下载视频、生成并翻译字幕（远程 Whisper API，失败不回退本地）、处理策略（逐条异步/采集完成后批量）、视频存储（跟随服务配置/本地/云端）、视频语言。
- **规则**：选账号/账号池时强制扫码登录并清空 cookies 与浏览器模式；勾字幕强制勾下载；未开下载/翻译时处理策略为「不处理」；凭据仅任务内存使用、不落库（UI 明示）。
- **校验 Toast**：请选择任务所属赛道 / 请填写{目标标签} / 临时凭据登录必须填写登录凭据。
- **验收**：成功 Toast「抖音任务已创建」并跳转详情；各类型目标输入联动正确。

#### FR-TASK-03 任务详情与实时进度（P0）

- **页面**：`/douyin/{taskId}`，eyebrow「任务执行详情」；副行含返回、状态徽章、赛道徽章、任务编号胶囊（「任务 #XXXXXX」）、恢复徽章（「已恢复 {n} 次 · {当前阶段}」）。
- **区块**：失败 Alert（条件）→ 扫码卡（waiting_login，二维码 15 秒刷新）→ 恢复中 Alert（条件）→ 四阶段进度卡（作品采集/评论采集/视频下载/字幕处理，右上「已完成 {a} / {b}」）→ 指标卡×4 → 账号并行分片卡（条件）→ 统一内容工作区 → 互动任务面板 → 任务配置折叠卡（默认收起，「敏感登录信息不会展示」）。
- **头部操作**：刷新 / 继续任务（非活跃且可恢复）/ 取消任务（活跃时，红色，cancelling 禁用；Toast「取消请求已提交」）。
- **规则**：active 时详情与分片 2 秒轮询；媒体汇总 active 2 秒/否则 10 秒；403/404 显示「任务不存在，或当前账号无权访问。」（无重试按钮）。
- **验收**：各状态出现/消失条件正确；阶段卡文案与进度数据一致（含「未启用」「--」兜底）。

#### FR-TASK-04 断点续跑（P0）

- **对话框**：ResumeTaskDialog，标题「从断点继续任务」，描述「已保存的作品、视频和字幕不会删除；完成项会自动跳过，只处理未完成或中断项。」
- **范围**：「继续爬取」（按 can_resume_crawl 禁用）+「继续视频下载和字幕」（按 can_resume_media 禁用），可独立勾选；cookie 任务可填临时凭据（「任务受理后自动清除」）。
- **验收**：两项未选时「确认继续」禁用；成功 Toast「恢复请求已受理，后台正在从断点继续」；详情页出现恢复中 Alert 与恢复徽章。

#### FR-TASK-05 统一内容工作区（P0）

- **面板**：UnifiedWorksPanel，标题「作品、下载与字幕」；每页 20 条。
- **能力**：汇总 6 格（作品/视频完成/下载中与排队/视频失败/字幕完成与失败/本地与云端）；筛选（搜索/排序 6 项/视频状态/字幕状态/标签）；批量条（导出评论 TXT、字幕 SRT/VTT/TXT、重试本页失败项、迁移）；表内行级操作（查看评论/评论/私信/重爬评论/作者作品/预览/沉浸播放/下载/重试/重新翻译/抖音外链）。
- **规则**：活跃 2 秒/非活跃 5 秒轮询；评论弹窗打开且活跃时 3 秒轮询；导出未选拦截 Toast「请先选择至少一个作品」；下载文件命名 `douyin-{aweme_id}.mp4`。
- **验收**：processing_media 横幅文案含「页面每 2 秒自动刷新，无需重复提交。」；批量导出文件名取 Content-Disposition。

#### FR-TASK-06 任务沉浸播放（P1）

- **页面**：`/douyin/{taskId}/feed` 全屏深色沉浸流；仅已下载且可播放作品，按发布时间倒序，上限 100；支持 `?start=video-{awemeId}` 定位。
- **交互**：滚轮（450ms 节流）/触摸（>50px）/键盘（↑↓、J/K、PageUp/Down）；右下竖排赞/评/分享；字幕以 VTT data URI 注入。
- **文案**：加载「加载视频列表…」；空态「没有可播放的视频 ｜ 请先完成视频下载，或检查筛选后的媒体状态。」；底部提示「滚轮、↑↓、J/K 或上下滑动切换视频」。
- **安全**：播放前先建 preview-session，短时播放权限数分钟内自动失效；接口带 Bearer token。

#### FR-KW-01 关键词库管理（P0）

- **页面**：`/douyin-keywords`，eyebrow「选题与采集词库」；每页 50 条。
- **区块**：指标卡×5（总数/未爬取/进行中/已爬取/需要重试）→ 筛选行（搜索/赛道/状态/启用/排序 6 项）→ 批量条 → 表格。
- **能力**：批量添加（每行或逗号分隔，自动清理空格并忽略大小写重复）、同步历史任务词、批量创建任务（合并每 20 词自动分组/独立 ≤20 词）、批量删除（confirm「历史任务和作品不会被删除。」）、行内任务列表/移动赛道/启停/删除。
- **规则**：关键词唯一归属赛道；列表需先选定赛道；列表 5 秒/概览 10 秒轮询；删除/移动不删历史任务、作品与评论。
- **验收**：添加 Toast 报「新增 {created} 个，已存在 {existing} 个」；批量创建成功 Toast「已创建 {count} 个关键词任务」。


### 4.4 内容域

#### FR-LIB-01 视频资源库（P1）

- **页面**：`/douyin-library`，eyebrow「内容资产中心」；每页 32 条。
- **区块**：汇总条×4（匹配/创作者/本页本地/本页云端）→ 筛选卡（搜索 + 赛道/任务/标签/创作者/存储/字幕 6 下拉 + 排序 8 项）→ 卡片网格（sm:2/xl:3/2xl:4）→ 分页。
- **卡片**：封面（角标「云端/本地」+ 底部「发布 {日期}」）、标题、创作者（兜底「匿名创作者」）、5 格统计（点赞/评论/收藏/分享/已存）、数据行（来源关键词/抓取时间/视频文件/下载完成/字幕）、操作组（预览/下载/重新翻译/重试资源/抖音外链/进入任务/沉浸播放）。
- **顶部操作**：沉浸播放（无行禁用）；本地视频转云端（storageBackend=minio 或无匹配禁用，confirm 前置）；刷新资源。
- **规则**：仅列已下载作品；5 秒轮询；筛选变更重置页码；切换赛道重置任务/创作者/标签；迁移携带当前筛选条件。
- **空态**：「没有符合当前条件的已下载视频」；说明小字「每页展示 32 条已下载资源；卡片已汇总互动、采集、文件与字幕数据。」
- **验收**：字幕摘要格式「{状态} · {progress}% · {语言} · {时长}」正确；迁移 Toast「已将 {queued} 个本地视频加入云端上传队列」。

#### FR-LIB-02 资源库沉浸播放（P1）

- **页面**：`/douyin-library/feed`；继承资源库全部筛选与排序（默认最近下载），上限 100 条，仅保留可播放作品。
- **文案**：加载「加载筛选后的视频…」；空态「没有可播放的视频 ｜ 请返回资源库调整筛选条件或确认视频已经下载完成。」+「返回视频资源库」；底部提示「当前筛选最多加载 100 条 · 滚轮、↑↓、J/K 或上下滑动切换」。
- **规则**：返回携带除 start 外全部筛选。
- **已知问题**：浏览器标题品牌名未统一（「- Douyin Crawler」），见 10.1 #9。

#### FR-CMT-01 评论管理（P1）

- **页面**：`/douyin-comments`，eyebrow「评论洞察」；每页 50 条；**无自动轮询**（手动「刷新数据」）。
- **区块**：指标卡×5（命中评论/主评论/回复/带图评论/累计点赞）→ 赛道范围 → 可折叠「多维筛选」（正文模糊/全文/任务/作品号/作者/关键词/层级/带图/点赞区间/日期区间/排序 5 项）→ 明细表格。
- **表列**：☑ ｜ 评论内容 ｜ 所属视频 ｜ 互动数据 ｜ 所属任务 ｜ 操作（回复/任务/视频外链）。
- **批量**：全选本页 + 行选 →「导出已选（{n}）」导出 TXT（文件名取 Content-Disposition，兜底 `douyin-selected-comments.txt`）。
- **规则**：回复判定 = parent_comment_id 不在 ["", "0"]；应用筛选清空选中并重置页码；重置条件保留赛道；校验「最小点赞数不能大于最大点赞数」「评论开始日期不能晚于结束日期」。
- **验收**：筛选摘要按条件拼接（正文"{x}"、指定任务、仅主评论等）；导出 Toast「已导出 {n} 条评论」；行内「回复」打开互动撰写对话框（comment_reply）。

#### FR-TAG-01 标签管理（P1）

- **页面**：`/douyin-tags`，eyebrow「标签洞察」；每页 50 条。
- **能力**：标签列表（标签/视频/任务/最近发现）、搜索、赛道筛选、排序 4 项、「同步历史标签」从存量作品抽取、「查看视频」跳资源库并带 tag 与赛道筛选。
- **规则**：标签自动从作品描述与话题元数据提取；同步成功后失效 4 个相关查询缓存。
- **空态**：「暂无标签，可点击“同步历史标签”从已有作品中抽取」；同步 Toast「已扫描 {aweme_count} 个作品，新增 {created_count} 个标签、{binding_count} 条关联」。

### 4.5 运营与风控域

#### FR-INT-01 互动任务列表与配额（P0）

- **页面**：`/douyin-interactions`，eyebrow「互动运营与风控」。
- **区块**：配额卡网格（每账号一张：可用/暂停、「{remaining} / {limit} 今日剩余」、「操作间隔至少 {n} 秒」）→ 筛选（赛道/状态 9 项/类型 3 项）→ 任务表格。
- **表列**：类型/目标 ｜ 互动内容/目标内容 ｜ 账号 ｜ 状态 ｜ 更新时间 ｜ 操作（查看详情/实时监控/确认发送/重试/取消）。
- **批量**：「重试全部可重试项」（confirm「将重新调度全部尚未成功且当前不在发送中的互动任务；发送中的任务不会重复提交。是否继续？」）。
- **规则**：列表有 queued/running 时 2 秒、否则 5 秒轮询；配额固定 5 秒；可重试 = can_retry 且非 running/succeeded；needs_review 重试必须 confirm「请先到抖音页面确认这条内容没有发送成功。确认未发送并重试吗？」
- **验收**：批量结果 Toast 分成功/部分失败两种口径（「已重新排队 {x} 条；{y} 条请求失败，{z} 条目标不可用或内容损坏，无法安全重试」）。

#### FR-INT-02 互动撰写与两段式确认（P0）

- **对话框**：InteractionComposerDialog；按类型切换文案（评论视频/回复评论/私信作者）。
- **流程**：选择发送账号（可见「今日剩余 {a}/{b}」「最小间隔 {n} 秒」）→ 填写内容（计数 {len}/{limit}，私信 1000、其余 2200）→「发送前检查」→ 草稿 Alert「等待最终确认」→「确认并发送」/「发送并查看实时监控」/「稍后处理」。
- **规则**：内容加密保存、不写入应用日志；重复目标+内容 24 小时内拦截；作者标识仅执行时临时解析（均为 UI 明示承诺）；无可用账号提示「没有可选账号，请先在账号池页面添加并登录账号。」
- **验收**：未 prepared 时仅显示「发送前检查」；检查通过 Toast「发送前检查通过，草稿已保存，请再次确认发送」；确认 Toast「互动任务已确认并进入浏览器执行队列」。

#### FR-INT-03 互动详情与操作留痕（P0）

- **详情对话框**：完整互动内容（「仅对任务所有者可见，不会写入应用日志。」）、外链「打开抖音视频 {aweme_id}」、实时监控入口、浏览器操作日志时间线（21 种事件文案）、截图证据（可放大，「截图仅任务所有者可查看」）。
- **needs_review Alert**：标题「需要人工检查」，描述「请先打开抖音确认内容没有发送成功，再执行重试，以免重复发送。」
- **验收**：截图加载/失败两态文案正确；事件行内显示「第 {n} 次执行」。

#### FR-INT-04 实时监控抽屉（P0）

- **抽屉**：InteractionLiveMonitor（右侧 Sheet），标题「评论实时监控」，描述「实时观察账号浏览器和执行链路；关闭监控不会中断评论任务。」
- **能力**：嵌入浏览器实时画面（云端槽位；本机浏览器显示「暂无可嵌入的实时浏览器」及说明）、延迟徽标「{n} ms」、执行链路每秒刷新（空态「正在等待任务进入执行队列…」）、按钮「打开抖音原视频/新窗口监控/刷新」。
- **规则**：详情 1 秒、槽位 2 秒轮询（仅打开时）；无障碍 live 文本播报最新步骤。

#### FR-ACCT-01 托管账号管理（P0）

- **页面**：`/douyin-accounts`，eyebrow「账号与浏览器身份」，标题「抖音账号池」。
- **区块**：指标卡×4（托管账号/当前可用/执行中/远程槽位可用）→ 远程浏览器槽位卡 → 账号表格 → 账号池卡片网格。
- **添加账号**：别名（必填，「不需要填写抖音号」）+ 浏览器位置（云端托管/本机专属）；远程模式自动分配第一个可用槽位（「留在“自动分配”即可…」）；无槽位错误提示三种解法。
- **账号生命周期**：登录（有 viewer 新窗口打开，Toast 提示允许弹窗）→ 验证（Toast「登录验证成功，账号已进入可用池」）→ 停用/启用 → 删除（confirm「确认删除账号“{name}”及其专属浏览器空间？」）。
- **规则**：执行中（active_leases>0）禁止登录/验证/启停/删除；列表/槽位 5 秒轮询、retry: false；隐私承诺「平台账号标识经过不可逆脱敏，敏感登录信息不会在内容数据、操作日志或页面中暴露。」
- **验收**：账号 7 态徽标文案正确（待登录/待验证/可用/执行中/冷却中/异常/已停用）。

#### FR-ACCT-02 并行账号池（P0）

- **创建对话框**：账号池名称 + 加入账号（复选，含状态）+ 调度策略（最少负载/顺序轮询/加权轮询）+ 最大并行账号（1–20，夹在已选数内）。
- **规则**：任务按目标拆分分片、独立浏览器空间并行执行；删除池不影响账号与登录状态（Toast「账号池已删除，账号与登录状态不受影响」）。

#### FR-BR-01 浏览器监控中心（P1）

- **页面**：`/douyin-browsers`，eyebrow「浏览器实时运营」。
- **区块**：指标卡×4（常驻浏览器/连接状态 {healthy}/{total}/已绑定账号/活动页面）→ 左槽位列表 + 右实时画面（iframe）→ 底部当前活动页面。
- **规则**：5 秒轮询、retry: false；默认选中第一个槽位，失效回落；iframe allow 剪贴板与全屏；无 viewer 时显示「该槽位暂未提供实时操作画面 ｜ 浏览器连接状态仍会持续监控。」
- **验收**：健康点绿/红与「浏览器在线/离线」徽标一致；「新窗口操作」「打开页面」外链正确。

### 4.6 系统域

#### FR-DEV-01 开发者中心（P1）

- **页面**：`/developer-tools`，eyebrow「API 与 MCP 集成目录」；数据 staleTime 30 秒。
- **能力**：API 文档标签页（方法徽标/路径/summary/鉴权徽标/请求参数/请求体/响应码，分组筛选 + 搜索）+ MCP 工具标签页（STDIO 与 Streamable HTTP 接入卡、端点复制、工具参数明细）+ 内嵌 Swagger 对话框（「调用受保护接口前请在文档中完成 Authorize。」）。
- **规则**：文档完全由运行时 OpenAPI/MCP 注册表生成，无需手工维护；错误态「文档加载失败 ｜ 无法从后端读取集成目录，请确认 API 服务已启动。」+「重新加载」。

#### FR-ADMIN-01 用户管理（P0，仅超管）

- **页面**：`/admin`，eyebrow「系统权限」；路由级超管守卫。
- **能力**：用户列表（Full Name/Email/Role/Status/Actions，一次拉取 100 条前端分页）、新增、编辑（密码留空即不修改）、删除（级联删除其全部条目，确认文案明示）。
- **规则**：当前登录管理员行无操作菜单（不能改/删自己）；角色徽标 Superuser/User；状态 Active/Inactive。
- **已知缺口**：模块内部文案全英文未中文化，见 10.1 #4。

#### FR-SET-01 个人设置（P1）

- **页面**：`/settings`，eyebrow「账号与安全」；三标签页。
- **个人资料**：姓名/邮箱查看-编辑切换；仅提交变更字段；保存仅 dirty 可用；姓名 ≤30 字（**校验提示缺中文**，见 10.1 #8）；成功 Toast「个人资料已更新」。
- **登录密码**：当前/新/确认三框；新密码 ≠ 当前密码（后端校验前端翻译）；成功 Toast「密码已更新」。
- **危险操作**：删除账号（红色卡 + 确认对话框「账号与全部关联数据将被永久删除。此操作无法撤销，请确认后继续。」）；成功后登出。
- **已知问题**：超管同样可见危险操作（疑似应隐藏的 no-op 逻辑），见 10.1 #2。


---

## 5. 状态机与核心业务规则

### 5.1 采集任务状态机（9 态）

`queued 排队中` / `waiting_login 等待扫码` / `running 运行中` / `processing_media 处理视频/字幕` / `cancelling 取消中` → **活跃**；`succeeded 已完成` / `failed 失败` / `cancelled 已取消` / `interrupted 已中断` → **终态**。

- 活跃 → 可「取消任务」，详情/分片 2 秒轮询；
- 终态 → 可「继续任务」（断点续跑，分「继续爬取」与「继续视频下载和字幕」两独立范围，记录 resume_count / last_resumed_at / checkpoint_phase）；
- failed/interrupted/waiting_login 计入列表「需处理」与工作台「需关注」；
- waiting_login 触发二维码卡片（15 秒刷新，扫码成功自动继续）。

### 5.2 关键词状态（4 态 + 启停位）

`unprocessed 未爬取` / `active 进行中` / `crawled 已爬取` / `failed 需要重试`（赛道详情页写作「需重试」，见 10.1 #13）；停用优先显示「已停用」。唯一归属一个赛道；删除/移动不删历史任务与内容。

### 5.3 媒体下载 / 字幕 / 迁移状态机

| 域 | 流转 | 界面文案 |
| --- | --- | --- |
| 下载 | queued → downloading → downloaded / failed | 等待中 / 下载中 / 已下载 / 失败 |
| 字幕 | pending → running → completed / failed | 等待字幕 / 字幕处理中 / 字幕完成 / 失败 |
| 迁移 | idle → queued → uploading → verifying → switching → cleanup_pending → completed / failed | 无需迁移·未迁移 / 等待上传 / 上传中 / 完整性校验中 / 切换存储中 / 云端存储已生效，等待清理本地文件 / 迁移完成 / 迁移失败 |

- **迁移安全承诺（产品红线）**：完整回读校验通过后才删除本地文件；任何一步失败本地保留、可稍后重试。
- 字幕由远程 Whisper API 生成，失败记录错误、**不回退本地模型**。
- 重试 = 下载 + 字幕同时重排队；重新翻译 = 单独重提字幕。

### 5.4 互动任务状态机（8 态）

`pending_confirmation 待确认` → 人工确认 → `queued 排队中` → `running 发送中` → `succeeded 已成功` / `failed 失败` / `blocked 已暂停` / `needs_review 待人工核对` / `cancelled 已取消`。

- 两段式人工确认（发送前检查 → 草稿 → 确认发送）为产品级风控红线；
- needs_review 必须先人工确认「未发送」再重试（防重复发送）；
- 同目标+同内容 24 小时内拦截；
- 内容加密保存、不写日志；完整内容与截图仅任务所有者可见。

### 5.5 账号与槽位

- 账号 7 态：待登录 / 待验证 / 可用 / 执行中 / 冷却中 / 异常 / 已停用；
- 槽位：配置异常 / 可用 / 已占用，一对一绑定账号；
- 调度策略：最少负载 / 顺序轮询 / 加权轮询；最大并行 1–20；
- 执行中（active_leases>0）禁止登录/验证/启停/删除。

### 5.6 赛道规则

- 默认赛道不可删除、必须保持启用；
- 删除赛道 → 关键词/任务/内容完整迁移到默认赛道；
- 停用赛道不可启动采集；工作区固定云端浏览器 + 云端存储 + 稳档风控。

---

## 6. 数据约束与限制

| 限制项 | 取值 | 限制项 | 取值 |
| --- | --- | --- | --- |
| 任务并发数 | 1–5 | 互动内容长度 | 评论/回复 ≤2200 字，私信 ≤1000 字 |
| 最大作品数 / 单作品评论上限 | 1–1000 | 赛道提示词 | ≤10000 字 |
| 批量任务合并模式 | 每 20 词自动分组 | 账号池最大并行 | 1–20 |
| 批量任务独立模式 | 一次 ≤20 词 | 资源库每页 | 32 条 |
| 工作区部分选择 | 一次 ≤200 词 | 评论/标签/关键词每页 | 50 条 |
| 统一工作区每页 | 20 条 | 沉浸播放上限 | 100 条 |
| 工作台/用户/条目拉取上限 | 100 条 | 互动防重 | 同目标+内容 24 小时 |
| 预览会话 | 数分钟内自动失效 | 登录二维码 | 15 秒刷新 |

---

## 7. 非功能需求

### 7.1 性能与实时性

- 自动刷新分级：实时监控 1 秒级；执行中任务/互动 2 秒级；列表概览 3–5 秒级；元数据 10–30 秒级（staleTime）；全量轮询表见 7.2。
- 列表大数据量采用服务端分页 + `placeholderData` 保留上一页，翻页无闪空。
- 视频播放按需分段读取（preview-session + range），关闭播放器即停止读取。

### 7.2 轮询策略一览

| 位置 | 间隔 | 位置 | 间隔 |
| --- | --- | --- | --- |
| 互动实时监控详情 | 1 秒 | 互动任务列表/面板 | 2 秒（有进行中）/ 5 秒 |
| 任务详情/分片（活跃） | 2 秒 | 工作台任务 | 5 秒 |
| 统一工作区（活跃/非活跃） | 2 / 5 秒 | 视频资源库 | 5 秒 |
| 任务列表 | 3 秒 | 账号池/槽位/浏览器监控 | 5 秒 |
| 作品评论弹窗（open+活跃） | 3 秒 | 关键词列表/概览 | 5 / 10 秒 |
| 执行进度媒体汇总 | 2 / 10 秒 | 赛道列表/详情 | 10 秒 |
| 互动配额 | 5 秒 | 评论管理 | 不轮询（手动刷新） |

### 7.3 安全与隐私

- 全站登录守卫（localStorage token），认证页反向守卫，401 触发登出；
- 受保护媒体/导出/截图接口携带 `Authorization: Bearer {token}`，预览另建短时会话（数分钟失效）；
- 临时登录凭据仅内存使用、不落库、不写日志，任务受理后自动清除（UI 明示）；
- 互动内容加密保存、不写入应用日志；账号平台标识不可逆脱敏；
- 互动完整内容与操作截图仅任务所有者可见；
- 任务配置卡默认收起且敏感登录信息不展示。

### 7.4 可用性与无障碍

- 全站「跳到主要内容」链接；图标按钮 aria-label；实时监控含无障碍 live 播报；
- 支持 prefers-reduced-motion；明暗模式跟随系统；
- 表单校验 onBlur/onSubmit 分级，错误信息中文（已知缺口见 10.1）。

### 7.5 兼容性

- 桌面优先（最大内容宽 1600px），移动端断点 `md` 表格降级为卡片；
- 沉浸播放支持触摸滑动与键盘（↑↓/J/K/PageUp/PageDown）。

---

## 8. 埋点与度量（现状说明）

当前版本以前端轮询与状态外显为运营度量手段（指标卡、需处理角标、配额卡），**未接入独立埋点系统**。后续迭代如需转化漏斗与功能使用率度量，建议基于第 4 章需求编号建立事件字典。

---

## 9. 边界与不做的事（Non-Goals）

| 边界 | 说明 |
| --- | --- |
| 字幕不回退本地模型 | 远程 Whisper API 失败仅记录错误，由用户重试或重新翻译 |
| 互动不自动发送 | 所有互动必须人工两段式确认；无定时/批量自动互动能力 |
| 账号不存敏感信息 | 不保存抖音号/密码；凭据仅内存级临时使用 |
| 评论管理不实时 | 该页手动刷新，不做轮询 |
| 资源库仅已下载 | 未下载作品不进入资源库视图（在任务详情工作区管理） |
| 通用条目模块 | 模板示例残留，非业务能力，规划下线 |

---

## 10. 已知问题与迭代建议

### 10.1 已知问题清单（与原型现状一致，按严重度排序）

| # | 级别 | 问题 | 位置/说明 |
| --- | --- | --- | --- |
| 1 | 功能缺陷 | 顶栏「服务运行正常」为静态假数据 | 全局顶栏；不接健康检查，服务宕机仍显示正常，对运营有误导性 → 建议接真实健康接口或下线 |
| 2 | 功能缺陷 | 个人设置「危险操作」对超管不隐藏 | `/settings`；`slice(0,3)` no-op 逻辑，与用户管理「不能删自己」规则矛盾 → 建议对超管隐藏或禁用自助删号 |
| 3 | 技术债 | MediaPipelinePanel 死代码 | 全项目无引用（已被 UnifiedWorksPanel 取代），建议清理 |
| 4 | 本地化 | 用户管理模块内部文案全英文 | 按钮/表单/对话框/校验/Toast 模板残留 |
| 5 | 本地化 | 通用条目模块内部文案全英文 | 同上（模块本身建议下线） |
| 6 | 本地化 | DataTable 分页/空态英文 | `No results found.`、`Showing {a} to {b} of {n} entries` 等 |
| 7 | 本地化 | PasswordInput aria-label 英文 | `Show password`/`Hide password` |
| 8 | 本地化 | 姓名超长校验无中文提示 | zod 默认英文错误信息 |
| 9 | 品牌一致 | 资源库沉浸播放页标题未统一 | 「资源库沉浸播放 - Douyin Crawler」，应为「- 灵感采集台」 |
| 10 | 文案一致 | 任务页三种叫法 | 侧边栏「抖音任务」/ 顶栏「爬取任务」/ 工作台「进入任务中心」 |
| 11 | 文案一致 | 账号池命名不一 | 「账号池」/「抖音账号池」/「账号与风控」/「管理账号池」 |
| 12 | 文案一致 | 工作台命名不一 | 「工作台」/「运营工作台」/「内容运营工作台」 |
| 13 | 文案一致 | 关键词 failed 两写法 | 「需要重试」/「需重试」 |
| 14 | 体验 | `/items` 模板残留模块无入口但可直达 | 建议下线或隐藏 |
| 15 | 规模隐患 | 用户/条目列表一次拉取上限 100 条 | 超出后无法翻页获取 |
| 16 | 安全依赖 | 任务配置卡敏感字段依赖后端剔除 | 前端默认收起但明文展示返回内容 |

### 10.2 后续迭代建议（ backlog ）

| 优先级 | 建议 |
| --- | --- |
| 高 | 顶栏服务状态接入真实健康检查；修复超管危险操作可见性；清理死代码 |
| 高 | Admin/Items/DataTable 中文化；统一「抖音任务/账号池/工作台」命名口径 |
| 中 | 用户管理服务端分页；埋点事件字典；资源库封面代理稳定性（当前依赖抖音 CDN 直链） |
| 中 | 互动任务通知能力（失败/待核对外推到工作台横幅或邮件） |
| 低 | 通用条目模块下线；自定义轮询频率设置 |

---

## 附录 A：页面-路由-需求编号对照

| 页面 | 路由 | 需求编号 |
| --- | --- | --- |
| 登录/注册/找回/重置 | `/login` `/signup` `/recover-password` `/reset-password` | FR-AUTH-01~04 |
| 工作台 | `/` | FR-DASH-01 |
| 赛道管理 | `/douyin-tracks` | FR-TRACK-01/02/03 |
| 赛道详情 | `/douyin-tracks/{trackId}` | FR-TRACK-04 |
| 抖音任务 | `/douyin` | FR-TASK-01/02 |
| 任务详情 | `/douyin/{taskId}` | FR-TASK-03/04/05 |
| 任务沉浸播放 | `/douyin/{taskId}/feed` | FR-TASK-06 |
| 关键词管理 | `/douyin-keywords` | FR-KW-01 |
| 视频资源库 | `/douyin-library` | FR-LIB-01 |
| 资源库沉浸播放 | `/douyin-library/feed` | FR-LIB-02 |
| 评论管理 | `/douyin-comments` | FR-CMT-01 |
| 标签管理 | `/douyin-tags` | FR-TAG-01 |
| 互动任务 | `/douyin-interactions` | FR-INT-01~04 |
| 账号池 | `/douyin-accounts` | FR-ACCT-01/02 |
| 浏览器监控 | `/douyin-browsers` | FR-BR-01 |
| 开发者中心 | `/developer-tools` | FR-DEV-01 |
| 用户管理 | `/admin` | FR-ADMIN-01 |
| 个人设置 | `/settings` | FR-SET-01 |

## 附录 B：参考附件

- 《前端逆向产品能力文档》（同目录 `.md`/`.html`）：全量逐字 UI 文案、ASCII 线框、组件级能力清单、路由文件索引、技术栈说明。
- 代码基线：`refactor/uv-workspace-modules @ 7bbf0af`。

---

*本 PRD 与线上原型逐一对齐；后续任何前端需求变更应先更新本文档对应需求条目，再进入开发。*
